export const PODEX_TITLE = "Podex — coming soon";

export const PODEX_BODY = `Podex is not available to install yet.

This button is honest: there is no download URL, no installer, and no feed in this release. When a first-party Podex feed exists, it will follow the same explicit pattern as \`openburnbar app install\` — you will opt in, and npm will not fetch it for you.

Until then, use OpenBurnBar for burn/quota/Hermes and this gateway for local loopback relay.`;
