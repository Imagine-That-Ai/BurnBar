<?xml version="1.0" encoding="utf-8"?>
<!--
  Windows render of the Mission Control console (MissionControlConsoleView.swift). The shell's
  "missionControl" destination: an ambient particle backdrop (landed Win2D SwarmCanvasHost),
  the hero strip, and an adaptive split — composer column (scroll) on the left, live situation
  room (~380px) on the right, collapsing to a single column under 900px. Binds a
  MissionConsoleViewModel over an IMissionDispatchHost (dev-host demo today; Firestore later).
-->
<Page
    x:Class="OpenBurnBar.App.MissionControl.MissionControlPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenBurnBar.App.MissionControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="Transparent">
        <!-- Ambient particle backdrop (added in code-behind: SwarmCanvasHost.Control). -->
        <Border x:Name="BackdropHost" IsHitTestVisible="False" Opacity="0.7" />

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Hero -->
            <local:MissionConsoleHeroView x:Name="Hero" Grid.Row="0" />

            <!-- Split -->
            <Grid x:Name="SplitGrid" Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="1" />
                    <ColumnDefinition Width="380" x:Name="SituationColumn" />
                </Grid.ColumnDefinitions>

                <ScrollViewer Grid.Column="0" Padding="28,20"
                              VerticalScrollMode="Auto"
                              VerticalScrollBarVisibility="Auto">
                    <local:MissionComposerView x:Name="Composer" MaxWidth="720" HorizontalAlignment="Left" />
                </ScrollViewer>

                <Border Grid.Column="1" Background="{StaticResource PensieveColorGlassLineBrush}" Opacity="0.6" />

                <local:MissionSituationRoomView x:Name="SituationRoom" Grid.Column="2"
                                                Background="Transparent" />
            </Grid>
        </Grid>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Wide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="900" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="Narrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!-- Collapse the situation room column under 900px (single column). -->
                        <Setter Target="SituationColumn.Width" Value="0" />
                        <Setter Target="SituationRoom.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
