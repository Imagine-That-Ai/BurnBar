<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="OpenBurnBar.App.Components.UnifiedSkeletonView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--
      Windows peer of OpenBurnBarCore/.../UnifiedSkeletonView.swift — an ember-tinted shimmer
      loading placeholder. The moving gradient rectangle is translated by a code-behind
      Storyboard; the Border clips it to the rounded corners. Respects Reduce Motion
      (UISettings.AnimationsEnabled) by holding the shimmer static.
    -->
    <Border x:Name="TrackBorder"
            CornerRadius="8"
            Background="{ThemeResource AuroraGlassTintClearBrush}"
            SizeChanged="OnRootSizeChanged">
        <Rectangle x:Name="Shimmer"
                   HorizontalAlignment="Left"
                   Width="1">
            <Rectangle.RenderTransform>
                <TranslateTransform x:Name="ShimmerTransform" />
            </Rectangle.RenderTransform>
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                    <GradientStop Offset="0.0" Color="#00000000" />
                    <GradientStop Offset="0.4" Color="{ThemeResource AuroraGlassSelectionFillBrush}" />
                    <GradientStop Offset="0.6" Color="#14FDC42C" />
                    <GradientStop Offset="1.0" Color="#00000000" />
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>
    </Border>
</UserControl>
