<?xml version="1.0" encoding="utf-8"?>
<!--
  AuroraLayoutView — provider list (left) + open hero swarm field (centre, transparent
  so the backdrop reads through) + a bottom data band. Windows peer of AuroraLayoutView.swift.
-->
<UserControl
    x:Class="OpenBurnBar.App.Dashboard.Layouts.AuroraLayoutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:components="using:OpenBurnBar.App.Components"
    xmlns:layouts="using:OpenBurnBar.App.Dashboard.Layouts"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" ColumnSpacing="20" RowSpacing="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Provider list panel. -->
        <components:UnifiedGlassCard Grid.Row="0" Grid.Column="0">
            <StackPanel Spacing="10">
                <TextBlock Text="Providers" FontSize="13" Opacity="0.6" FontWeight="SemiBold" />
                <TextBlock Text="Anthropic — $612" FontSize="14" />
                <TextBlock Text="OpenAI — $438" FontSize="14" />
                <TextBlock Text="Google — $142" FontSize="14" />
                <TextBlock Text="xAI — $92" FontSize="14" />
            </StackPanel>
        </components:UnifiedGlassCard>

        <!-- Open hero swarm field: transparent so the DashboardBackdrop reads through. -->
        <Grid Grid.Row="0" Grid.Column="1">
            <TextBlock Text="Aurora"
                       VerticalAlignment="Top"
                       HorizontalAlignment="Left"
                       FontSize="15"
                       Opacity="0.6"
                       FontWeight="SemiBold" />
        </Grid>

        <!-- Bottom data band. -->
        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" Spacing="16">
            <layouts:ConceptStatTile Value="$1,284" Caption="Spend"
                                     AccentBrush="{StaticResource PensieveColorBrassCoreBrush}" />
            <layouts:ConceptStatTile Value="18.4M" Caption="Tokens"
                                     AccentBrush="{StaticResource PensieveColorMercuryCoreBrush}" />
            <layouts:ConceptStatTile Value="342" Caption="Sessions"
                                     AccentBrush="{StaticResource OBBAccentBrush}" />
        </StackPanel>
    </Grid>
</UserControl>
