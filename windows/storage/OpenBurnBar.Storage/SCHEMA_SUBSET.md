<!-- GENERATED BY `node scripts/ci/generate-windows-subset-doc.mjs`.
     DO NOT EDIT: regenerate after any migrator, Windows schema, or
     parity-baseline change; CI fails on any drift. -->
# Windows Storage Schema Subset (experimental)

Canonical endpoint: `v70_agent_memories_index_backfill` (71 migrations). Windows journal: 71 identifiers ending at `v70_agent_memories_index_backfill` (count 71).

Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Tables outside that subset are NOT created by the provisioner; Windows reads them only from Mac-transferred databases where the Mac migrator created them. The full canonical schema is `docs/SCHEMA_SQLITE.sql` (generated from the live migrator); per-object drift is enforced by `scripts/check-migrator-parity.mjs` with annotated reasons in `budgets/migrator-parity-baseline.json` — this doc renders that fence readably, it does not define it.

## Ported tables (28)

| Table | Columns (as provisioned) | Notes |
|---|---|---|
| `agent_memory_bodies` | `memory_id`, `project_id`, `engine_memory_id`, `body`, `body_hash`, `created_at`, `updated_at` |  |
| `agent_memory_inbox` | `doc_id`, `user_id`, `engine_memory_id`, `payload_json`, `remote_updated_at`, `received_at`, `applied_at` |  |
| `ai_inbox_item_state` | `item_id`, `read_at`, `archived_at`, `snoozed_until`, `feedback`, `updated_at` |  |
| `ai_inbox_items` | `id`, `fingerprint`, `kind`, `priority`, `state`, `title`, `summary_md`, `payload_json`, `project_id`, `project_name`, `occurrence_count`, `first_seen_at`, `last_seen_at`, `resolved_at`, `resolution_note`, `tick_id`, `model_provenance` |  |
| `ai_inbox_memory_export` | `memory_id`, `provenance`, `snippet_md`, `approved_at`, `exported_at` |  |
| `ai_inbox_plan_events` | `id`, `plan_id`, `step_id`, `event`, `detail_json`, `created_at` |  |
| `ai_inbox_plan_steps` | `id`, `plan_id`, `parent_step_id`, `ordinal`, `title`, `body_md`, `status`, `next_move_md`, `evidence_ids_json`, `mission_id`, `followup_id`, `inbox_fingerprint`, `grade`, `grade_note_md`, `graded_at`, `created_at`, `updated_at`, `completed_at` |  |
| `ai_inbox_plans` | `id`, `title`, `horizon`, `pack`, `status`, `summary_md`, `created_at`, `updated_at`, `origin_fingerprint`, `memory_id`, `pensieve_vector_id`, `grade_avg`, `metrics_json` |  |
| `ai_inbox_runs` | `tick_id`, `started_at`, `finished_at`, `gate_result`, `gate_signature`, `egress_mode`, `llm_calls`, `input_tokens`, `output_tokens`, `cost_usd`, `items_new`, `items_updated`, `items_resolved`, `error` |  |
| `ai_inbox_state` | `key`, `value_json`, `updated_at` |  |
| `ai_inbox_thread_messages` | `id`, `fingerprint`, `role`, `body_md`, `plan_candidates_json`, `model_provenance`, `cost_usd`, `created_at` |  |
| `ai_inbox_threads` | `fingerprint`, `item_id`, `created_at`, `updated_at`, `turn_count`, `total_cost_usd` |  |
| `app_state` | `key`, `value`, `updatedAt` |  |
| `budget_events` | `id`, `ruleID`, `kind`, `source`, `amountAtEvent`, `limitAtEvent`, `detailJSON`, `occurredAt`, `syncedAt`, `sourceDeviceID` |  |
| `budget_rules` | `id`, `scope`, `identifier`, `providerID`, `accountID`, `projectName`, `label`, `amountUSD`, `period`, `behavior`, `fallbackCredentialIDsJSON`, `pausedUntil`, `createdAt`, `updatedAt`, `syncedAt`, `sourceDeviceID`, `isEnabled` |  |
| `chat_messages` | `id`, `threadId`, `role`, `content`, `timestamp`, `cliUsed`, `transcriptPiecesJSON`, `attachmentsJSON`, `errorKind`, `errorMessage`, `retrievalStateJSON`, `modelUsed`, `backend`, `isStreaming`, `metadata` | Windows chat pipeline stores additional per-chat_message fields (backend/error/streaming metadata) that Mac keeps in transcriptPiecesJSON/attachments or in memory. |
| `chat_retrieval_events` | `id`, `threadId`, `messageId`, `kind`, `detail`, `createdAt` |  |
| `chat_stream_failures` | `id`, `threadId`, `messageId`, `kind`, `message`, `createdAt` |  |
| `chat_threads` | `id`, `title`, `backend`, `createdAt`, `updatedAt`, `projectPath`, `sessionId` | Windows chat pipeline stores additional per-chat_thread fields (backend/error/streaming metadata) that Mac keeps in transcriptPiecesJSON/attachments or in memory. |
| `conversations` | `id`, `provider`, `sessionId`, `projectName`, `inferredTaskTitle`, `fullText`, `indexedAt`, `messageCount`, `deletedAt`, `workingDirectory` | Windows provisions the conversation-transfer subset only: the sync/summary/word-count/key-* columns are Mac-side derived metadata the Windows read seams never touch. |
| `memory_quarantine_bodies` | `memory_id`, `project_id`, `body`, `created_at`, `updated_at` |  |
| `parser_checkpoint_files` | `provider`, `path`, `fileSizeBytes`, `modificationDate`, `creationDate`, `fileSystemNumber`, `fileNumber` |  |
| `parser_checkpoints` | `provider`, `checkpointToken`, `lastProcessedFilePath`, `lastProcessedAt`, `version` |  |
| `standing_orders` | `id`, `title`, `instruction`, `cadenceKind`, `cadenceMinutes`, `cadenceHour`, `cadenceMinute`, `cadenceWeekday`, `targetBodyId`, `requiredCapabilities`, `isEnabled`, `lastFiredAt`, `createdAt`, `updatedAt` |  |
| `switcher_active_profile` | `activeProfileID`, `providerID`, `updatedAt` |  |
| `switcher_profiles` | `id`, `targetKind`, `browserType`, `browserMetadataJSON`, `cliType`, `cliMetadataJSON`, `sortKey`, `createdAt`, `updatedAt` |  |
| `token_usage` | `id`, `provider`, `sessionId`, `projectName`, `model`, `inputTokens`, `outputTokens`, `cacheCreationTokens`, `cacheReadTokens`, `reasoningTokens`, `totalTokens`, `cost`, `startTime`, `endTime`, `createdAt`, `usageSource`, `executionSourceID`, `executionSourceName`, `executionSourceKind`, `executionSourceConfidence`, `sourceDeviceId`, `sourceDeviceName`, `isRemote`, `providerID`, `providerAccountID`, `providerAccountLabel`, `providerAccountSource`, `provenanceMethod`, `provenanceConfidence`, `estimatorVersion`, `parentRequestID`, `billingKind`, `originatorKind`, `originatorRef` | token_usage.syncedAt is Mac cloud-sync bookkeeping (v22 cross-device sync); the Windows port has no sync engine yet and its write seams never populate it. |
| `conversations_fts` (FTS virtual) | `inferredTaskTitle`, `fullText` |  |

## Not ported (44)

| Table | Reason (from the parity baseline) |
|---|---|
| `agent_memories` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table agent_memories belongs to chat-memory authority (v51-v53), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `artifact_permissions` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table artifact_permissions belongs to cross-device sync + artifact registry (v15-v17/v22/v30/v33), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `audit_events` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table audit_events belongs to cross-device sync + artifact registry (v15-v17/v22/v30/v33), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `backfill_cursors` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table backfill_cursors belongs to cross-device sync + artifact registry (v15-v17/v22/v30/v33), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `chunk_embeddings` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table chunk_embeddings belongs to local-search substrate (v14/v21/v31/v34), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `code_artifacts` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table code_artifacts belongs to project code memory (v50), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `code_call_edges` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table code_call_edges belongs to project code memory (v50), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `code_diagnostics_cache` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table code_diagnostics_cache belongs to project code memory (v50), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `code_index_checkpoints` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table code_index_checkpoints belongs to project code memory (v50), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `code_references` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table code_references belongs to project code memory (v50), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `code_symbols` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table code_symbols belongs to project code memory (v50), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `controller_runtime_cache` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table controller_runtime_cache belongs to parser checkpoints / operating history / controller cache (v25/v26/v29), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `devices` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table devices belongs to cross-device sync + artifact registry (v15-v17/v22/v30/v33), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `embedding_models` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table embedding_models belongs to local-search substrate (v14/v21/v31/v34), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `embedding_versions` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table embedding_versions belongs to local-search substrate (v14/v21/v31/v34), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `memory_audit` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table memory_audit belongs to chat-memory authority (v51-v53), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `memory_body_snapshots` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table memory_body_snapshots belongs to chat-memory authority (v51-v53), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `memory_embedding_refs` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table memory_embedding_refs belongs to chat-memory authority (v51-v53), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `memory_extraction_jobs` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table memory_extraction_jobs belongs to chat-memory authority (v51-v53), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `memory_fact_tombstones` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table memory_fact_tombstones belongs to chat-memory authority (v51-v53), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `memory_links` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table memory_links holds the v61 usage-memory typed consolidation edges, written only by the Mac-side consolidation worker; the usage-memory pipeline is not ported, so the Windows v61 upgrade step is stamp-only (WindowsSqlCipherProvisioning.Upgrades.cs). |
| `memory_provenance` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table memory_provenance belongs to chat-memory authority (v51-v53), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `memory_salience` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table memory_salience is the v61 usage-memory salience sidecar, written only by the Mac-side consolidation worker; the usage-memory pipeline is not ported, so the Windows v61 upgrade step is stamp-only (WindowsSqlCipherProvisioning.Upgrades.cs). |
| `memory_source_tombstones` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table memory_source_tombstones belongs to chat-memory authority (v51-v53), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `memory_usage_candidates` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table memory_usage_candidates is the v61 usage-memory Stage-0 candidate spool, written only by the Mac-side session miner; the usage-memory pipeline is not ported, so the Windows v61 upgrade step is stamp-only (WindowsSqlCipherProvisioning.Upgrades.cs). |
| `operating_action_history` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table operating_action_history belongs to parser checkpoints / operating history / controller cache (v25/v26/v29), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `pcm_file_manifest` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table pcm_file_manifest belongs to project code memory (v50), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `pcm_project_aliases` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table pcm_project_aliases belongs to project code memory (v50), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `pcm_projects` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table pcm_projects belongs to project code memory (v50), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `project_memory_snapshots` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table project_memory_snapshots belongs to summaries/memory snapshots (v4/v18/v39), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `projection_jobs` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table projection_jobs belongs to local-search substrate (v14/v21/v31/v34), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `provider_accounts` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table provider_accounts belongs to provider accounts (v35), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `provider_quota_snapshots` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table provider_quota_snapshots belongs to provider quota snapshots (v54), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. The journal still marks v54 applied so a transferred-back DB is not misread as pre-v54; Windows must create this table before it ships quota snapshot writes. |
| `remote_sync_watermarks` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table remote_sync_watermarks belongs to cross-device sync + artifact registry (v15-v17/v22/v30/v33), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `retrieval_health` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table retrieval_health belongs to local-search substrate (v14/v21/v31/v34), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `search_chunks` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table search_chunks belongs to local-search substrate (v14/v21/v31/v34), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `search_chunks_fts` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table search_chunks_fts belongs to local-search substrate (v14/v21/v31/v34), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `search_documents` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table search_documents belongs to local-search substrate (v14/v21/v31/v34), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `search_documents_fts` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table search_documents_fts belongs to local-search substrate (v14/v21/v31/v34), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `shared_artifact_sync_state` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table shared_artifact_sync_state belongs to cross-device sync + artifact registry (v15-v17/v22/v30/v33), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `source_artifacts` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table source_artifacts belongs to cross-device sync + artifact registry (v15-v17/v22/v30/v33), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `summary_runs` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table summary_runs belongs to summaries/memory snapshots (v4/v18/v39), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
| `text_expansion_snippets` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table text_expansion_snippets belongs to text expansion snippets (v43), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. NOTE: TextExpansionSnippetWriteSeam.cs already reads/writes this table, so it only works on Mac-transferred databases; a fresh Windows-provisioned DB lacks it. Port the CREATE TABLE (+ text_expansion_snippets_trigger_active_idx) when the seam ships to fresh installs. |
| `vector_index_snapshots` | Windows fresh-install provisioning mirrors only the shipped Windows subsystems (token usage, conversations + standalone FTS, chat threads/messages, budgets, switcher). Table vector_index_snapshots belongs to local-search substrate (v14/v21/v31/v34), which is not ported; Windows currently reads it only from Mac-transferred databases where the Mac migrator created it. |
