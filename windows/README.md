# OpenBurnBar — Windows source tree

Greenfield home for the Windows port (see [`docs/WINDOWS_PORT_MASTER_PLAN.md`](../docs/WINDOWS_PORT_MASTER_PLAN.md)).
This tree is **new and separate** from the macOS app (`AgentLens/`, `OpenBurnBarCore/`,
`OpenBurnBarDaemon/`), the iOS app (`OpenBurnBarMobile/`), the Android app (`android/`), and the
shared `packages/` — nothing here collides with those trees.

This tree now carries **real product code** (~950 `.cs`/`.xaml` source files, ~126K LOC): a full
WinUI 3 shell + views, portable `net8.0` cores for every subsystem, and ~27 test projects that run
on real Windows CI (x64 + ARM64, [`pr-windows-full.yml`](../.github/workflows/pr-windows-full.yml)).
**Current status:** the portable-logic layer is broad and genuinely tested, but the Windows product
does not yet have full macOS parity. The independent source audit is the current gap and remediation
plan: [`WINDOWS_MACOS_PARITY_INDEPENDENT_AUDIT_2026-07-09.md`](../docs/windows-port/WINDOWS_MACOS_PARITY_INDEPENDENT_AUDIT_2026-07-09.md).
The [`PARITY_CERTIFICATION_BUNDLE.md`](../docs/windows-port/PARITY_CERTIFICATION_BUNDLE.md) and
[`WINDOWS_PARITY_LEDGER.yml`](../docs/windows-port/WINDOWS_PARITY_LEDGER.yml) remain scoped evidence,
not end-to-end product certification.

## Layout

| Path | Contents | Workstream |
|------|----------|------------|
| [`OpenBurnBar.sln`](OpenBurnBar.sln) | The aggregating Visual Studio solution. Now registers the WinUI shell spike (`app/OpenBurnBar.App`, **WINUI-016**); the PAL, native shim, and test projects register into it as they land. | W10 |
| [`app/`](app/) | The **WinUI 3 (C#/.NET) shell** — tray flyout, main window, Mica/Acrylic glass, design-token theme, Pretext host. Also hosts `app/OpenBurnBar.App.Presentation` (a **portable net8.0 view-model layer**, no WinUI) with the SessionLogs + Memory + account-**Switcher** view-models, ported record types, grouping/routing/form passes, and transcript parser; the WinUI views under `app/OpenBurnBar.App/SessionLogs/` + `.../Memory/` + `.../Switcher/` render it. It also carries the **portable provider-quota parse core** (`app/OpenBurnBar.App.Presentation/Quota/`, no WinUI): the four macOS quota mechanisms — the Claude statusline-hook payload, the Cursor `state.vscdb`-sourced usage-summary, the Codex `wham/usage` API response, and the Anthropic `anthropic-ratelimit-*` response headers — each parsed value-for-value into the shared `ProviderQuotaSnapshot`/`ProviderQuotaBucket` model the landed Quota surface under `app/OpenBurnBar.App/Quota/` renders. The **acquisition halves** that feed those parsers now ship as the portable `app/OpenBurnBar.App.Quota.Acquisition` (net8.0, no WinUI): the statusline snapshot-file source + freshness gate, the Cursor `state.vscdb` read (`Microsoft.Data.Sqlite`, read-only), the Codex `wham/usage` HTTP source, the Anthropic rate-limit-header probe + capturing handler, the statusline `.cmd`/PowerShell hook installer, and the `QuotaAcquisitionCoordinator` (fan-in → snapshots with the Mac debounce/retry/interval constants on an injectable clock) — all unit-tested on macOS (`tests/quota-acquisition`). Its net8.0 `.Windows` sibling (`app/OpenBurnBar.App.Quota.Acquisition.Windows`) carries the real `FileSystemWatcher`, the `%LOCALAPPDATA%/%APPDATA%` path defaults, and the `WindowsQuotaAcquisitionHost` the app configures at launch; `QuotaAccountsSource` prefers this live coordinator over B4 Firestore over sample data. **Windows-runner-deferred (WS-D):** the live `FileSystemWatcher` + real `%APPDATA%` paths, hook installation against a real `~/.claude/settings.json`, and the WinUI XAML build of the page (XamlCompiler is Windows-only). Also hosts `app/OpenBurnBar.App.Pet` (a **portable net8.0 PetCompanion core**, no WinUI): the behavior-graph interpreter + seeded RNG, the reaction brain, the petdef parser, the chat-event bridge that drives the graph from the landed Chat state machine, and the transport-agnostic glTF scene controller + embedded three.js shell; the WinUI pet shell under `app/OpenBurnBar.App/Pet/` (overlay window + WebView2 three.js host + chat bubble) renders it. | W6–W7, G4 |
| [`pal/`](pal/) | The **Platform Abstraction Layer** — FS paths, secret store (CNG/TPM + DPAPI), process/ConPTY, named-pipe IPC, toasts, tray, watchers, autolaunch, hotkey, single-instance, mDNS/Bonjour, self-signature check. Also hosts the **ViGEm virtual-HID computer-use input path** (`pal/input` portable core + `pal/input-windows` ViGEmBus/SendInput adapter, **W5 / R17**): the advisory-vs-non-bypassable action classification + capability-token gate-before-dispatch + Ed25519 token verify + triple kill-switch, unit-tested on macOS (`tests/input`). Includes `pal/overlay` (`OpenBurnBar.Pal.Overlay`, net8.0): the transparent, click-through pet overlay window (`WS_EX_LAYERED\|TRANSPARENT\|TOOLWINDOW\|NOACTIVATE` + per-pixel `UpdateLayeredWindow` + `WM_NCHITTEST`); its portable `OverlayWindowStyle` flag math + `PerPixelHitTest` are macOS-tested, its Win32 host is dev-host-deferred. | W1, W5, G4 |
| [`native/`](native/) | The **native shim** — the managed↔Rust FFI bridge that surfaces the Rust crates (`crates/openburnbar-iroh`, `crates/burnbar-remote`) to the WinUI shell. Ships `OpenBurnBar.Native` (the hardened `NativeLibraryLocator` + `NativeShimLoader` + `NativeShimUnavailableException` — graceful `NotSupported` when the cdylib is absent so dev-host/macOS still compiles) and the two binding projects `OpenBurnBar.Native.Iroh` + `OpenBurnBar.Native.BurnBarRemote` over the pinned UniFFI-generated C# (drift-gated by [`scripts/windows-port/check-csharp-binding-drift.sh`](../scripts/windows-port/check-csharp-binding-drift.sh) + [`csharp-binding-drift.yml`](../.github/workflows/csharp-binding-drift.yml)). Proven by `tests/native` — surface tests run everywhere; `[NativeFact]` loopback tests exercise a **real cargo-built cdylib** (13 FFI round-trips: iroh ALPN/secret-key/blob-ticket, burnbar-remote quality-decision encode/decode vs committed golden + ordered callbacks) and skip cleanly where no Rust build exists. | W2 |
| [`storage/`](storage/) | The **SQLCipher storage layer** — opens the *same* Mac-produced encrypted database with the pinned compatibility-4 cipher profile and exposes the read + write DataStore-shaped seam the Engine calls via the PAL. The R2 (DB byte-compat) un-prune. Includes `storage/OpenBurnBar.Storage.SessionLogs`, the net10.0 adapter mapping the read seam onto the presentation `ISessionLogReadSource` for the SessionLogs surface. | W2–W7 |
| [`cloudsync/`](cloudsync/) | The **cloud sync + crypto + auth** layer — portable, dependency-light `net8.0` C# ports of the macOS Firebase/CloudSync seam (unavailable off-Apple, where the Firebase SDK is gone). Ships `OpenBurnBar.CloudSync` — the **Firestore CloudSync REST gateway** (parity with `AgentLens/Services/CloudSync/*` / `CloudSyncFirestoreGateway.swift`): the `ICloudSyncGateway` abstraction as a REST/RPC v1 client over `documents.get`/`.patch`/`:commit`/`:runQuery`/`.delete` behind an `ICloudSyncHttpTransport` seam, the Firestore typed-value document codec + canonical (sorted-key) JSON, the Firebase callable envelope (`mintWindowsAppCheckToken`), the 14 Firestore model domains, a **FakeGateway** driving the offline-write-queue + snapshot-listener state machines, and the **E2EE fail-closed** write guard (proven by `cloudsync/OpenBurnBar.CloudSync.Tests`). Ships `OpenBurnBar.CloudSync.Crypto` — the zero-knowledge **CloudVault E2EE crypto** (parity: `CloudVaultCrypto.swift` + `apps/console/lib/escrow.ts`): P-256 ECDH + HKDF-SHA256 + AES-256-GCM escrow key-wrap, `sealText`/`sealBlob`/`sealPayload` (v1 no-AAD + v2 path-bound AAD), symmetric recovery-key wrap, vault-keyed HMAC hashes, `vaultKeyID`/SHA-256 — all **fail-closed**, with byte-parity in BOTH directions against a committed Apple-CryptoKit-origin KAT (`tools/CloudVaultVectorGen`). Ships `cloudsync/appcheck/` — the **Windows App Check client** (Phase 2 · AC-011): a portable core (`OpenBurnBar.CloudSync.AppCheck` — the `IAttestationProducer` seam + `MockAttestationProducer` server-parity, the Firebase-callable mint client + `AppCheckToken` TTL model + `WindowsAppCheckProvider` install/TTL-refresh/single-flight/fail-closed `X-Firebase-AppCheck` attach), a `net10.0` HttpClient transport (`.Net`), and a `net8.0-windows` real TPM producer (`.Windows`: CNG `NCryptCreateClaim`, R14/dev-host-deferred). The live authenticated Firestore/callable + live App Check round-trips are dev-host/CI-deferred (no Firebase SDK to bind on macOS); the tests prove request SHAPES + response PARSING + byte-parity vectors. | W2, W-Cloud, W-CloudSync, R14, G2 |
| [`particles/`](particles/) | The **Win2D particle-engine renderer** — the platform-agnostic swarm/substrate engine library (`OpenBurnBar.Particles`: substrate math, frame model, FFI spec) + a headless perf harness. The WinUI-facing host lives in `app/OpenBurnBar.App/Particles/`. | W6 |
| [`pretext/`](pretext/) | The **Pretext text-layout engine** — the managed `OpenBurnBar.Pretext` engine + JS bridge that drive an offscreen WebView2 host for text metrics, with a Mac-golden metric-parity harness. The WinUI host lives in `app/OpenBurnBar.App/Pretext/`. | W6 |
| [`computeruse/`](computeruse/) | The **Computer-Use subsystem**. `OpenBurnBar.ComputerUse.Core` is the portable (net8.0) policy + crypto core — capability-token issuer/verifier (Ed25519, attestation-bound, single-use nonce), the tamper-evident audit hash-chain + Ed25519 signed head, the triple-independent kill-switch state machine (hotkey + workspace-lock + Remote-Config, **fail-closed-on-RC-error**, R17), the deny-region / scope matcher, the capability-gate decision tree, and the watchdog command protocol. `OpenBurnBar.ComputerUse.Windows` (net10.0-windows) implements the SendInput / IUIAutomation / Windows.Graphics.Capture / named-pipe adapters behind the Core's interfaces (consuming the landed `pal/ipc` peer-auth harness). | W5 |
| [`integrations/`](integrations/) | **Third-party device / service integrations.** Each integration is a portable, dependency-free `net8.0` library (protocol/wire codecs, discovery record parse/build, session + wizard state machines, consent/budget stores) proven by `dotnet test` on macOS, plus a thin `net8.0-windows` adapter (real sockets / OS discovery / capture) behind interfaces. Ships: `integrations/cast/` — **Google Cast** (CASTV2 protobuf framing, `_googlecast._tcp` mDNS parse/build, receiver launch + reconnect state machines, setup-wizard state machine + view-model in `OpenBurnBar.Integrations.Cast`; TLS socket + live Dnssd browser in `OpenBurnBar.Integrations.Cast.Windows`); `integrations/homeassistant/` + `integrations/smarthub/` — **Home Assistant + SmartHub** (HA REST message model + response/error mapping + entity-state→MediaPlayer Cast projection + webhook-id + blueprint installer + token/config stores, with an `HttpClient` transport adapter; the Nest-Hub bridge route dispatcher + `/state.json` contract + constant-time bridge-token auth + snapshot/display-config models + DNS-SD/mDNS record build/parse, with a live `HttpListener` host + `UdpClient` mDNS responder + `NetworkInterface` LAN enumerator; parity `AgentLens/Services/HomeAssistant/*` + `SmartHub/*`); `integrations/mercury/` — **Mercury media pipeline** (RFB/VNC + media-frame packet wire codecs, media-session state machine, consent ledger, media-budget capability gate, file-transfer chunker; Windows.Graphics.Capture / WASAPI / MediaCapture / MediaFoundation / WNS adapters). | W7, W-Mercury |
| [`packaging/`](packaging/) | **Distribution manifests + signed auto-update** (Phase 5 · W10). Mirrors the macOS release channel set — MSIX + portable zip; winget + Chocolatey — with auto-update from the **Ed25519-pinned** feed (feed key pinned independent of the Authenticode cert). Ships `packaging/msix/` (the MSIX `Package.appxmanifest` — identity, capabilities, and the `windows.protocol` `openburnbar://` / `windows.fileTypeAssociation` `.burnbarchat`+`.burnbarpane` / `windows.startupTask` / `windows.toastNotificationActivation` extensions — plus the `OpenBurnBar.Packaging.wapproj` WAP project that wraps the **unpackaged** app into a signable MSIX), `packaging/portable/` (the `portable-layout.json` layout + schema and the `New-PortableZip.ps1` PS7 stage/zip/SHA256 script), `packaging/winget/` (the `version`/`installer`/`defaultLocale` manifest set in winget-pkgs schema **1.6.0** for `ImagineThat.OpenBurnBar`), and `packaging/chocolatey/` (the `openburnbar.nuspec` + portable-zip install/uninstall scripts). `packaging/updater/` mirrors the shipped macOS Sparkle pipeline for Windows (**R19**): the **Ed25519-pinned update feed** format (`feed/appcast-windows.xml` + `feed/latest-windows.json` — a real signed sample verifiable end-to-end, plus the pinned `feed/pinned-update-key.pub`); `OpenBurnBar.Updater.Core` — the **portable `net8.0` updater-core** that parses the feed, **verifies the Ed25519 signature over the artifact bytes against a PINNED public key independent of the Authenticode certificate**, checks the SHA-256, blocks downgrades, and fails closed on any mismatch (proven by `tests/updater`); `OpenBurnBar.Updater.Generator` — the release-side appcast generator (mirror of [`scripts/generate-macos-appcast.mjs`](../scripts/generate-macos-appcast.mjs)); and `OpenBurnBar.Updater.Windows` — the `net8.0-windows` **WinSparkle host adapter** behind the core's `IUpdaterHost` seam (pins the same EdDSA key natively). The manifest data/config files validate on macOS (`packaging/scripts/verify.sh` — xmllint + winget schema + nuspec/JSON/PowerShell); the **actual MSIX build/sign, real artifact hashes, live WinSparkle round-trip, and Store/winget/Choco submission are Windows-runner + W0-cert-deferred**. | W10, W0, G5 |
| [`dist/`](dist/) | **Signed-distribution + supply-chain core** (Phase 5 · G5). Portable `net8.0`: `OpenBurnBar.Dist.UpdateFeed` — the **update-feed verification kernel** (parse the direct-download feed; verify a detached **Ed25519 signature over a canonical release descriptor under a PINNED key that is independent of the Authenticode cert**; strict feed parse; fail-closed decision) — the Windows analog of Sparkle EdDSA + pinned `SUPublicEDKey`; `OpenBurnBar.Dist.UpdateFeed.Tool` — the release-pipeline signer CLI (`gen-key`/`derive-pubkey`/`sign-entry`/`build-feed`/`verify-feed`) sharing the exact canonicalizer + BouncyCastle Ed25519 the client verifies with; `OpenBurnBar.Dist.Hardening` — the **R19 DLL-load hardening** library + `HardeningPolicy` (runtime `SetDefaultDllDirectories` shim + the required native/managed flags). `props/` holds the packaging props (`OpenBurnBar.Windows.Hardening.props` imported by the app; `OpenBurnBar.Windows.NativeHardening.props` for native `.vcxproj`). Proven on macOS by `tests/dist` + [`pr-windows-dist.yml`](../.github/workflows/pr-windows-dist.yml); the release pipeline is [`openburnbar-release-windows.yml`](../.github/workflows/openburnbar-release-windows.yml) (Authenticode + Store/winget are W0-cert/Windows-runner-deferred). See [`dist/README.md`](dist/README.md), [`dist/DLL_HARDENING.md`](dist/DLL_HARDENING.md), [`dist/WINDOWS_RELEASE.md`](dist/WINDOWS_RELEASE.md). | W0, G5 |
| [`tests/`](tests/) | **Unit, integration, and parity test projects** for the app, PAL, native shim, and integrations. Includes `tests/presentation` (net10.0, xUnit) — the macOS-runnable proof of the SessionLogs + Memory + Switcher view-models: the inbox model, grouping/filter, transcript parser, an end-to-end SessionLogs read through the SQLCipher adapter against the committed byte-compat fixture, and the account-switcher destination routing / grouping / form-validation / add-switch-drain state machine. Also `tests/quota` (net10.0, xUnit) — the portable provider-quota parse core: one representative adapter per quota mechanism (Claude statusline hook, Cursor `state.vscdb` usage-summary, Codex `wham/usage` API, Anthropic rate-limit headers) parsed value-for-value against committed recorded input + expected-snapshot fixtures (remaining/limit/reset/window/unit). Also `tests/quota-acquisition` (net10.0, xUnit) — the **acquisition** halves feeding those parsers: the statusline file source freshness gate, the hook installer's settings.json edit / original-statusline preservation / wrapper contract / uninstall restore, the Cursor `state.vscdb` read against an **in-test-constructed** SQLite fixture (ItemTable keys → cookie), the Cursor/Codex/Anthropic HTTP sources through the transport seam (bearer/cookie/probe request shapes + the recorded fixtures round-tripped value-for-value), the Codex refresh-nudge retry, the Anthropic capturing `DelegatingHandler`, the `QuotaAcquisitionCoordinator` fan-in/retry/sticky-snapshot/debounce/auto-refresh determinism on the injectable clock, and the real `FileSystemWatcher` adapter contract. Also `tests/computeruse` (net10.0, xUnit) — the Computer-Use policy/crypto core: token issue→verify→replay-rejected, audit-chain tamper detection, the triple kill-switch (incl. fail-closed-on-RC-error), the scope/deny-region matcher, and the capability-gate decision matrix. Also `tests/pet` (net10.0, xUnit) — the PetCompanion core: behavior-graph transitions on the **real committed claudecode petdef**, the chat-event bridge, the reaction brain, the petdef parser, deterministic RNG, the glTF host protocol, and the overlay style/hit-test math. Also `tests/integrations-cast` — the portable Cast protocol/discovery/wizard libs. Also `tests/integrations` — the HA REST round-trips against recorded fixtures, the SmartHub bridge route dispatch + `/state.json` contract + auth, the DNS-SD/mDNS record bytes, and real HTTP round-trips through the live `HttpListener` host + `HttpClient` transport on loopback. Also `tests/mercury` — byte-exact packet + RFB/ARD wire codecs, the capability/consent/budget invariant, the session state machine, and file-transfer chunk/reassembly. Also `tests/cloudsync` (net10.0, xUnit) — the CloudVault E2EE crypto: cross-platform byte-parity in BOTH directions against the committed CryptoKit-origin KAT (escrow wrap/unwrap, sealText/blob/payload v1+v2, recovery wrap, keyed hashes, vaultKeyID, SHA-256), the BouncyCastle↔.NET `DeriveRawSecretAgreement` ECDH anchor, and the fail-closed invariants (tampered tag, wrong key, relocated/missing/legacy-v1 AAD). Also `tests/cloudsync-appcheck` (net10.0, xUnit) — the Windows App Check client: the mock-attestation MAC parity vectors, the mint request-encode/response-decode + fail-closed classification, the `AppCheckToken` TTL model, `WindowsAppCheckProvider` install/refresh/single-flight/header-attach, and a live-loopback `HttpListener` mint round-trip. Also `tests/updater` (net10.0, xUnit) — the auto-update pin-verify core (**R19**): real RFC-8032 Ed25519 sign/verify, the SHA-256 + dotted-numeric version compare, appcast-XML + JSON feed parse (incl. XXE-rejection + fail-closed malformed), a generate→parse→verify round trip, the full pinned-key decision matrix (valid accepted; tampered / wrong-key / re-signed-with-a-different-cert / downgrade / sha256-mismatch / length-mismatch / missing-signature / malformed-feed all rejected; **feed-sha256-rewritten-to-match-tampered-payload STILL rejected by the pin**), and the committed sample feed verified end-to-end. Also `tests/dist` (net10.0, xUnit) — the signed-distribution core: pinned-Ed25519 sign→verify round-trips, per-field tamper + wrong-key + malformed-signature fail-closed, the all-zero-placeholder-pin refusal, content-`sha256` integrity, strict feed parse, the **fail-closed update decision** (an unsigned/forged entry is never "available"), version ordering, and the DLL-hardening **policy↔props cross-check**. | W0, W5, W7, W10, W11, W-Cloud, W-CloudSync, R14, R19, G4, G5 |

## The aggregating solution

`OpenBurnBar.sln` is the single entry point `msbuild` / `dotnet` / Visual Studio open. It now
registers **~65 projects**: the WinUI app, the app satellite libraries
(Presentation/Settings/Dashboard/Pet/CloudSync/Configuration), PAL (+ Windows variants), storage
(+ SessionLogs), particles (+ perf harness), pretext, computeruse (core + windows), all
integrations (cast/homeassistant/smarthub/mercury + variants), cloudsync (+ crypto + appcheck),
dist, packaging/updater, and the test projects. Each workstream adds its `Project(...)` stanza +
per-config platform mappings as it lands, so PRs only *add* to a known-good aggregator.

## Where new Windows source goes

Every Windows **source file** (`.cs`, `.xaml`, `.cpp`, `.cxx`, `.cc`, `.c`, `.h`, `.hpp`, `.rs`)
must live under one of the twelve documented sub-trees above. The per-tree budget
([`scripts/debt/check-windows-tree-budget.sh`](../scripts/debt/check-windows-tree-budget.sh))
fails a PR that drops source directly under `windows/` outside those areas, so the layout stays
self-enforcing.

## CI + budgets

- **PR gate (blocking, today):** [`.github/workflows/fast-feedback.yml`](../.github/workflows/fast-feedback.yml)
  → the `debt-budgets` job runs the Windows per-tree ratchet on every PR.
- **PR gate (skeleton lane):** [`.github/workflows/pr-windows-fast.yml`](../.github/workflows/pr-windows-fast.yml)
  → a `windows/`-scoped path filter + a skeleton `windows-latest` build/lint/test lane. Real
  Windows-runner execution is filled in by **CI-003**.
- **Full harness (post-merge / nightly):** [`.github/workflows/openburnbar-pr-harness.yml`](../.github/workflows/openburnbar-pr-harness.yml)
  → the `windows` job mirrors the `android` job (build + lint + unit + coverage + artifact) and is
  registered in `platform-confidence-gate.needs`.
- **Rust crate builds:** the `x86_64-pc-windows-msvc` / `aarch64-pc-windows-msvc` crate workflows
  (`build-iroh-windows.yml`, `build-burnbar-remote-windows.yml`) are owned by **W-RUST / RUST-005**;
  this tree only *references* them.
- **Per-tree budget:** [`budgets/windows-tree-baseline.json`](../budgets/windows-tree-baseline.json),
  partitioned by sub-tree so a macOS/Android change never moves a Windows counter and app growth
  never moves the PAL/native/test counters.

## Ownership

`windows/` is owned in [`.github/CODEOWNERS`](../.github/CODEOWNERS); PRs to this tree carry the
`area: Windows` label ([`.github/labels.yml`](../.github/labels.yml)).

## Full-suite gate note (WS-D D3 proof closure)

The blocking full-suite gate is [`.github/workflows/pr-windows-full.yml`](../.github/workflows/pr-windows-full.yml):
two parallel legs (`windows-latest` x64 + `windows-11-arm` ARM64) that run `dotnet restore`, a full
`dotnet build` of [`OpenBurnBar.sln`](OpenBurnBar.sln), and the full `dotnet test` suite (~2,545 C#
fixtures) on each architecture, aggregating into the single `PR Windows Full Gate` status.

Its `detect-windows-full-changes` job is path-filtered: the x64 + ARM64 legs run only when a change
touches `windows/**`, the crate-side C# uniffi bindings, `OpenBurnBarCore/.../Resources/`,
`AgentLensTests/Fixtures/`, or the workflow file itself. A bare `workflow_dispatch` against `main`
resolves `git diff HEAD^1 HEAD` — which on `main` sees only the last merge commit — so it reports
`windows=false` and the legs skip. To exercise both full legs on main-equivalent code, land a change
that touches `windows/**` (this note does exactly that); the ARM64 leg's first green run must happen
on the hosted `windows-11-arm` runner because it cannot be exercised on the macOS/x64 dev host.
