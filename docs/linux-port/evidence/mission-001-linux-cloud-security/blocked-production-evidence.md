# Blocked Production Evidence

| Surface | Status | Reason | Local/Staging Evidence |
| --- | --- | --- | --- |
| Linux App Check production attestation | Blocked | No production Linux attestation provider credentials/config are available in this workspace. Non-production mock verification is explicitly disabled when `runtime.env === "Production"`. | `linuxAppCheck.test.ts` verifies fixture claims mint in non-production and cannot mint in production. |
| GNOME Secret Service / libsecret session | Blocked locally | `secret-tool` and a DBus Secret Service session are not available in this worker container. | `secret-store-setup-proof.json` records the blocker, setup UX, KWallet command fixture, systemd credential fallback, and high-value plaintext fallback refusal. |
| TPM-backed secret sealing | Blocked optional hardening | `tpm2_getcap` and `/dev/tpm0`/`/dev/tpmrm0` are not available in this worker container. | `secret-store-setup-proof.json` records the optional TPM blocker and approved Secret Service/KWallet/systemd/headless alternatives. |
| Stripe live checkout restore | Blocked | No live Stripe account/session credentials are available in this worker environment. | `membership-stripe-protocol-cache-ui-transcript.json` records deterministic Stripe test-mode checkout/portal request/response fixtures, API/DB entitlement restore rows, daemon/shell cache updates, and UI SVG artifacts. |
| Firebase REST/Auth live exchange | Blocked | No live Firebase web API key/session credentials are provided to this worker. | `auth-protocol-fixture.json` records PKCE/browser launch metadata, Firebase `signInWithIdp` and revocation request/response fixtures, state-mismatch rejection, and SecretStore token custody metadata. |
| Firebase staging cloud sync emulator trace | Blocked | No authenticated Firebase emulator/staging credentials are provided to this worker. | `cloud-sync-local-staging-transcript.json` exercises the local-staging product guard for callable, REST transform/update, rules allow/deny, Listen/WS frame, retry/backoff, conflict, and watermark semantics. |
