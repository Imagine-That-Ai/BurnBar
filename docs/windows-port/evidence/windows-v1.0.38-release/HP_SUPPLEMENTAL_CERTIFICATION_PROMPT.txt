You are the native physical Windows x64 supplemental-certification operator for OpenBurnBar v1.0.38.

Finish every physically available remaining protocol end to end on the HP ENVY Intel/AMD64 laptop. This is native physical Windows x64 evidence. It is not VM, hosted-runner, x86/32-bit, or ARM64 evidence. Do not open or use any VM. Physical ARM64 remains BLOCKED as an explicit beta limitation.

Authoritative candidate identity:
- Tag: windows-v1.0.38
- Candidate commit: 48837746490b6468efa4dc06a476f305d496039c
- Release workflow: 29676266545
- Staging workflow: 29675617068
- x64 direct MSIX SHA-256: fd14adb0870473f907c10f342eb3ee300cebf39c500848505a5887ad89602e69
- Expected signer: CN=Imagine That AI LLC, O=Imagine That AI LLC, L=Little Rock, S=Arkansas, C=US
- Package version/architecture: 1.0.38.0 / X64
- Staging project: burnbar-staging only
- Existing physical retry ZIP SHA-256: bf10318474556b1ea3f69b05831fa09dfc4f8b36ac9470fb113092ec4f7ed876

Independent harness requirement:
- Use exact clean harness commit 37d88056066d876570df4b2887f33df7af7ebe56.
- That commit is the merge of PR #1867 and includes both the PR #1862 supplemental-harness corrections and the reviewed Remote Config gzip/ETag repair. GitHub and GitLab main both resolved to that exact commit when this handoff was updated.
- Do not use the old harness commit 00d0751f1c671d99fe7ef8f4059e91d689a30f44 for the new baseline. It coupled valid semantic UIA evidence to an unreliable scheduled-task desktop bitmap.
- The new harness keeps routed RenderTargetBitmap screenshots fail-closed, uses UI Automation for the semantic deny-region verdict, reports external desktop capture separately, and discovers visible WinUI windows by PID when Process.MainWindowHandle is zero.
- The Remote Config publisher now requests gzip on both GET and PUT so Firebase returns the ETag required for exact optimistic concurrency. Never replace the returned ETag with If-Match: *.

Known verified starting point:
- Artifact hash/source/signature/timestamp: PASS.
- Physical AMD64 identity and clean candidate checkout: PASS.
- Cold launch 20/20, warm launch 20/20, uninstall/reinstall/second launch 20/20: PASS.
- Idle capture 300/300 seconds: PASS as an observation.
- Soak 1,800 samples over 1,822 seconds with zero crashes, hangs, or unresponsive samples: PASS as an observation.
- All 26 route screenshots and route-root UIA anchors: PASS.
- gcloud authentication, burnbar-staging access, staging environment enable/restore: PASS.
- Existing seven-receipt NO-GO evidence validator: PASS.

Current blockers to close:
1. Accessibility/display: corrected semantic rerun plus manual Narrator, keyboard-only, focus, announcements, 150/200 percent DPI, and mixed-DPI proof.
2. Physical performance x64: all 18 active-budget measurements, including canonical surface latency, frame pacing, and sleep/wake.
3. Staging cloud: OAuth PKCE/failure/refresh/revocation/offline/sign-out, valid and invalid App Check, physical TPM claim, CloudVault round trip/conflict/replay, and secret leak scan.
4. Media/Computer Use safety: paired-device permissions, interruption/recovery, quarantine, exact approval, trust downgrade, all panic paths, watchdog, remote kill switches, audit tamper, phone authorization, and replay denial.
5. Store/update lifecycle: private flight only, and only after gates 1-4 PASS. Public publication is forbidden.
6. Physical ARM64: keep BLOCKED beta limitation. Do not substitute VM or CI evidence.

Safety rules:
- Work from a normal signed-in native PowerShell 7 desktop session with CIM, AppX, TPM, UI Automation, camera/microphone, and interactive desktop access.
- Fail closed. Never hand-author a PASS receipt, weaken a validator or budget, reuse stale evidence, or mark an unobserved manual assertion PASS.
- Do not change production Firebase, a public Store listing, public update feed, disks, partitions, firmware, or unrelated data.
- Preserve every earlier evidence directory. Use a new timestamped root and result directory.
- Preserve any package that predated the run. Remove only the exact package installed by this run, after evidence capture.
- Do not print or retain OAuth codes, ID/refresh/App Check tokens, TPM claims, authorization headers, client values, vault keys, or secrets.
- Ask Alberto only for actions that genuinely require human observation, device unlock/trust, Narrator listening, physical sleep/wake, monitor movement, or Partner Center approval. Continue all independent work before and after each human action.

Phase 1 - verify transfer and establish clean roots:
1. Resolve the flash drive whose volume label is BURNBAR. The original signed handoff is BurnBar-cert\windows-v1.0.38-final-handoff. The physical retry evidence is in the latest windows-v1.0.38-development-handoff-* directory.
2. Run the original VERIFY-TRANSFER.ps1 and require PASS. Independently hash the physical retry ZIP and require bf10318474556b1ea3f69b05831fa09dfc4f8b36ac9470fb113092ec4f7ed876. Test the ZIP and its internal SHA256SUMS.
3. Create a new timestamped C:\BurnBar-cert\windows-v1.0.38-supplemental-* root. Never overwrite C:\BurnBar-cert\windows-v1.0.38 or an earlier run.
4. Clone the repo twice into candidate and harness. Check candidate out detached at windows-v1.0.38 and require exact commit 48837746490b6468efa4dc06a476f305d496039c. Check the harness out detached at 37d88056066d876570df4b2887f33df7af7ebe56 and require both trees clean.
5. Copy the exact verified release/provenance handoff into the new root. Reverify the MSIX hash, signed-artifact manifest, package identity/version/architecture, Authenticode signer/timestamp, checksums, and supplied provenance before installation.

Phase 2 - rerun the baseline with the corrected harness:
1. Generate a fresh physical x64 attestation with new-physical-hardware-attestation.ps1. Use only live CIM fallback logic; never fabricate an identifier.
2. Run run-physical-release-certification.ps1 with no SkipAutomatedTests and no SkipUiAutomation, candidate RepoRoot, exact artifact manifest, fresh attestation, Platform x64, and PhysicalHardware.
3. Run validate-release-certification-evidence.mjs with --write-sums, --expected-commit 48837746490b6468efa4dc06a476f305d496039c, and --expected-harness-commit set to the PR #1862 merge commit.
4. Require all routed screenshots and UIA anchors to remain PASS. The semantic UIA verdict must PASS. If external capture is Skipped, retain its diagnostic status; do not relabel it as a screenshot PASS.

Phase 3 - complete accessibility-display:
1. Initialize a fresh accessibility-display results file with new-release-certification-supplemental-receipt.ps1 -Initialize.
2. Run the all-profile UI Automation harness in the signed-in desktop session and retain summary JSON, JUnit, HTML, UIA, and screenshot evidence.
3. Perform every catalog assertion: UIA profile, Narrator names/roles/values/state, keyboard navigation, focus order, visible focus, live announcements, 100/150/200 percent scaling, high contrast, reduced motion, reduced transparency, narrow/wide windows, and mixed-DPI movement.
4. For Narrator and subjective focus/announcement checks, show Alberto one short checklist and wait for explicit observations. Do not infer them from screenshots.
5. For mixed-DPI, use the laptop panel and a physically attached second display only if available. If no qualifying second display exists, keep that assertion and the gate BLOCKED.
6. Attach raw evidence to every assertion and finalize only if every assertion genuinely PASSes.

Phase 4 - complete physical-performance-x64:
1. Initialize a fresh physical-performance-x64 results file.
2. Read the exact active release-performance-budgets.json from the clean harness. Do not edit thresholds.
3. Capture complete numeric sample arrays for every measurement: cold and warm launch; tray/flyout, dashboard, search, settings, chat acknowledgement; representative incremental scan; idle and active CPU; private memory; GPU; disk writes; frame-time p95; dropped-frame rate; soak growth/crashes/hangs.
4. Use an official, signed PresentMon build or an approved equivalent for composition frame data. Record tool version/hash, GPU/driver, WebView2, power/thermal, display, workload, and sampling context. Do not invent frames from screenshots.
5. Perform physical sleep/wake and Explorer restart with Alberto only where physical observation is required. Confirm the app, tray, activation, and state recover.
6. Run a fresh 30-minute soak if the earlier samples are outside the supplemental finalizer time window. The finalizer rejects stale protocol intervals.
7. Keep complete raw CSV/JSON/ETL evidence, derive each statistic from its sample series, run release-performance-budget.mjs, and finalize only when all 18 measurements and all assertions pass.

Phase 5 - complete staging-cloud against burnbar-staging only:
1. Verify the exact staging deployment receipts bind all four scoped Functions and the Azure TPM verifier to candidate commit 48837746490b6468efa4dc06a476f305d496039c.
2. Enable the approved staging public-client environment without printing values. Always restore it in finally and verify no staging environment remains afterward.
3. Before any mutation, run publish-staging-remote-config-fixture.ps1 from the exact harness with -Fixture Baseline -ConfirmStagingMutation. Require a non-empty Firebase ETag from the gzip GET, exact If-Match reuse on PUT, and successful Baseline verification. If Firebase still returns no ETag, stop this gate and retain the sanitized response metadata; never use If-Match: *.
4. Complete OAuth PKCE success, cancellation, malformed callback/state mismatch, refresh/expiry, revocation, offline queue/reconnect, and sign-out cleanup in the signed app.
5. Prove a physical TPM claim obtains valid staging App Check and an enforced callable accepts it. Missing, malformed, expired, wrong-app, and invalid claims/tokens must be rejected.
6. Prove CloudVault authenticated upload/download, conflict, duplicate, ordering, replay, cleanup, and sign-out behavior using harmless staging-only records.
7. Run a secret leak scan over config, logs, diagnostics, evidence, and child environments before finalization.

Phase 6 - complete media-computer-use-safety with harmless fixtures:
1. Use the connected iPhone only after Alberto unlocks it and confirms trust/pairing. Device inventory alone is not PASS evidence.
2. Cover camera, microphone, screen capture grant/deny; call/share/transfer interruption, recovery, retry, and cancellation; snapshot retention/redaction/cleanup; and Mark of the Web/quarantine.
3. Cover protected-target and secure-desktop denial; exact current approval and stale-approval rejection; phone/runtime trust downgrade with no silent elevation; global hotkey panic, phone three-finger panic, auth-gate panic, watchdog loss; audit terminal-entry tamper; intended-session phone authorization; and replay rejection.
4. Use scripts\windows-port\publish-staging-remote-config-fixture.ps1 from the clean harness. It derives Baseline, ComputerKill, MediaKill, and MalformedSystem from one committed catalog and refuses every project except burnbar-staging.
5. For every non-baseline Remote Config drill, publish Baseline in a finally block and verify the returned parameters. ComputerKill must halt within 60 seconds; MediaKill must deny admission; malformed system config must fail closed. App exit must revoke privileged input after the maximum 90-second lease.
6. Finalize only if every media/Computer Use assertion has complete, sanitized evidence.

Phase 7 - private Store/update lifecycle, conditional:
1. Do not create or upload a Store flight until accessibility, physical x64 performance, staging cloud, and media/Computer Use gates are PASS and Alberto explicitly confirms the internal flight.
2. Never select a public audience. Verify the reserved Partner Center identity and store-submission-v1.0.38.json before upload.
3. When authorized, test private-flight clean install, predecessor upgrade, repair, rollback/recovery, uninstall/reinstall, launch/protocol/file/toast/startup activation, single instance, valid/tampered/downgrade/offline feed behavior, Store/direct coexistence, and winget eligibility.
4. If Microsoft certification, private audience, or predecessor flight is unavailable, keep the gate BLOCKED with the exact Microsoft-side dependency.

Phase 8 - final evidence:
1. Finalize every genuinely complete supplemental receipt against the new validator-clean baseline and exact live hardware.
2. Rerun the baseline with SupplementalReceiptDirectory, then rerun the final validator with --write-sums and both expected commits.
3. Create a content-addressed ZIP without deleting its source evidence. Copy the ZIP, SHA-256 receipt, validator log, and concise result into a new versioned directory on BURNBAR.
4. Remove only the exact test package installed by this run after evidence capture. Confirm staging environment and Remote Config Baseline are restored.

Return:
- final evidence ZIP path and SHA-256
- validator PASS/FAIL and overall GO/NO-GO
- exact candidate and new harness commits
- artifact hash and signer/timestamp verdict
- physical machine and AMD64 proof
- every gate and assertion status
- exact recovery for each remaining FAIL/BLOCKED item
- proof staging environment and Remote Config Baseline were restored
- private Store flight state
- confirmation that production, public rollout, disks, partitions, firmware, and VMs were untouched

Do not stop after preflight. Continue through every physically available assertion and fail closed only on a real external or human-observation blocker.
