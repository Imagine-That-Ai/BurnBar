# Ledger rows: F1 H8 integrations (DeferredApproved)

**What this proves:** Master-plan H8 OS/ecosystem integrations are inventoried as
explicit ledger rows with WPD revive triggers — not silent gaps. Portable cores
or partial clients may exist under `windows/integrations` / app surfaces, but
production Real requires Windows host proofs (live device, SendInput, overlay,
proxy process, notifications). Until revive, rows stay DeferredApproved under
WPD-0008 (host) / WPD-0010 (depth). Product copy must not imply live integrations
without ledger Real.

Rows: mercury-media, cast-smarthub, home-assistant, text-expansion,
cursor-connector, daily-digest-notifications, settings-s1-s2-tabs (settings
placeholder death residual). pet-companion and computer-use-loop are separate
rows in the main ledger.

**Tests:** Structural — ledger verify requires revive_trigger + evidence path;
shell/nav tests cover primary routes. Integration unit tests may exist per
subsystem but do not earn Real without host evidence.
