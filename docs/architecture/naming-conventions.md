# ADR: Service / Store / Actor naming conventions (legacy filename)

**Status:** Superseded — use the numbered record.

This bookmark path is retained for older links. The canonical ADR is **[001-naming-conventions.md](001-naming-conventions.md)** (`*Service`, `*Store`, `*Actor`, `*Client` boundaries).
