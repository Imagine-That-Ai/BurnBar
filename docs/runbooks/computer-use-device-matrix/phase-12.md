# Phase 12 Device Matrix - Phone As Controller

## Scope

Phone control lets iPhone/iPad watch a Mac session, approve/reject pending actions, downgrade trust mode, and send signed panic/input intents back to the Mac.

## Required Checks

| Check | Command / Evidence | Expected |
|---|---|---|
| iOS build | `xcodebuild -project OpenBurnBar.xcodeproj -scheme OpenBurnBarMobile -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' -configuration Debug -quiet build` | exit `0` |
| Receiver exists | `OpenBurnBarMobile/Services/ComputerUse/AgentWatchReceiver.swift` | ingests `control.*` frames |
| Control stream exists | `OpenBurnBarMobile/Services/ComputerUse/AgentWatchOverlayCoordinator.swift` | opens persistent iroh control stream |
| Approval responses | `AgentWatchReceiver.approve/reject` | emits `control.approval.response` |
| Panic path | `PhoneControlSender.send(.panic)` | emits signed `control.input.intent` |

## Result Log

| Date (UTC) | Device | OS target | Build | Install | Launch / test | Evidence |
|---|---|---|---|---|---|---|
| 2026-05-18T00:46:58Z | Mac receiver harness | macOS arm64 test host | PASS: `xcodebuild -project OpenBurnBar.xcodeproj -scheme OpenBurnBar -destination 'platform=macOS,arch=arm64' -configuration Debug -derivedDataPath /tmp/DerivedData-cu-phone-chaos-fresh -only-testing:OpenBurnBarTests/PhoneControlReceiverTests/testReplayChaosRejectsOneThousandDuplicateIntentEnvelopes -quiet test` exited `0` | n/a | PASS: 1 signed tap dispatched once; 1,000 duplicate envelopes rejected as `counterReplay`. This is receiver-level replay proof, not the live paired-device matrix gate. | `/tmp/cu-phone-replay-chaos-fresh.log` |
| 2026-05-18T02:05:06Z | Samsung Galaxy S24 `R3CXB0CNS0J` / `SM_S921U` -> Mac `OpenBurnBar-ComputerUseProof.app` | Android 16 physical device + macOS arm64 proof app, Debug | PASS: `cd android && ./gradlew :openburnbar-iroh-relay:testDebugUnitTest :app:assembleDebug --no-daemon` exited `0`; current Mac proof app was relaunched from `/Applications/OpenBurnBar-ComputerUseProof.app` after current-checkout build | PASS: `adb -s R3CXB0CNS0J install -r android/app/build/outputs/apk/debug/app-debug.apk` reported `Success`; Android debug App Check token `7dccf216-30f1-4ba0-8f68-014ca9571914` registered via Firebase App Check REST; trusted escrow device seeded for `android-a855ab6d80cf436cf090b7e0be5430f75ee7c36c10d815fb09a92d286a834b18` | PASS with execution denied by macOS TCC: Android verified signed `iroh_pairing`, opened live iroh dial, published controller key, classified `control.input`, and sent signed tap/scroll/panic. Mac proof classified `android-phone-e900b647460116c4d972ff90`, received tap/scroll/panic, dispatched tap + scroll into audited Mac input path, and denied both with `accessibility_revoked` because `AXIsProcessTrusted()` remained false despite the Settings toggle. | Android proof: `/tmp/cu-live-proof-android.jsonl`; Mac proof: `/tmp/cu-live-computer-use-proof.jsonl`; Firestore controller doc: `users/6YTomKTKdQdpvIJgmz6VTIrrQ4w1/iroh_pairing/relay-23aa015d-b6c5-434c-8eba-e33b8b8e4aaa/controllers/android-phone-e900b647460116c4d972ff90` |
| 2026-05-18T03:40:03Z | Samsung Galaxy S24 `R3CXB0CNS0J` / `SM_S921U` -> Mac `OpenBurnBar-ComputerUseProof.app` | Android 16 physical device + macOS arm64 proof app, Debug | PASS: current Mac proof app rebuilt from checkout with `/tmp/DerivedData-cu-phone-approval-fix`, copied to `/Applications/OpenBurnBar-ComputerUseProof.app`, and relaunched after the Mac restart/TCC reset; focused capability-gate regression passed with `swift test --package-path OpenBurnBarCore --filter ComputerUseCapabilityGateTests` | PASS: same physical Android device and trusted controller identity `android-a855ab6d80cf436cf090b7e0be5430f75ee7c36c10d815fb09a92d286a834b18`; E2E launched with `adb -s R3CXB0CNS0J shell am start -n com.openburnbar/.MainActivity --ez openburnbar.e2e.computerUse true --es openburnbar.e2e.firebaseUid 6YTomKTKdQdpvIJgmz6VTIrrQ4w1 --es openburnbar.e2e.computerUseConnectionId relay-23aa015d-b6c5-434c-8eba-e33b8b8e4aaa` | PASS with live HID execution: Mac logged `mac_accessibility_trusted`, classified the Android phone, received signed tap/scroll/panic, executed `mac_input_click` and `mac_input_scroll` with audit entries `0` and `1`, and received `mac_phone_panic_received`. The approval hang was fixed by treating verified `originatedFromPhone` intents as `approvedBy: .phone`, so the Mac no longer asks for a second local approval sheet for an already signed phone action. | Android proof: `/tmp/cu-live-proof-android-executed.jsonl`; Mac proof: `/tmp/cu-live-computer-use-proof-executed.jsonl`; Firestore controller doc: `users/6YTomKTKdQdpvIJgmz6VTIrrQ4w1/iroh_pairing/relay-23aa015d-b6c5-434c-8eba-e33b8b8e4aaa/controllers/android-phone-e900b647460116c4d972ff90` |
| 2026-05-18T03:58:22Z | Samsung Galaxy S24 `R3CXB0CNS0J` / `SM_S921U` -> Mac `OpenBurnBar-ComputerUseProof.app` | Android 16 physical device + macOS arm64 proof app, Debug | PASS: Android debug APK rebuilt from current checkout with the Computer Use E2E burst hook and installed on `R3CXB0CNS0J`; Mac proof app rebuilt from current checkout with `/tmp/DerivedData-cu-phone-latency`, copied to `/Applications/OpenBurnBar-ComputerUseProof.app`, and relaunched with `OPENBURNBAR_E2E_COMPUTER_USE_ACTION_CAP=125` | PASS: same physical Android device and trusted controller identity; E2E launched with `adb -s R3CXB0CNS0J shell am start -n com.openburnbar/.MainActivity --ez openburnbar.e2e.computerUse true --es openburnbar.e2e.firebaseUid 6YTomKTKdQdpvIJgmz6VTIrrQ4w1 --es openburnbar.e2e.computerUseConnectionId relay-23aa015d-b6c5-434c-8eba-e33b8b8e4aaa --ei openburnbar.e2e.computerUseIntentCount 100 --el openburnbar.e2e.computerUseIntentIntervalMillis 350 --ez openburnbar.e2e.computerUseSendPanic false` | PASS for the Android latency row: Android sent 100 signed tap/scroll intents; Mac received 100/100 and dispatched 100/100 with `status=executed`. Mac receive-to-dispatch latency was p50 152.5 ms, p95 162.0 ms, max 200 ms. Cross-device phone-to-Mac timestamps are clock-skewed, so the clock-safe metric is Mac receive-to-dispatch. A prior 80 ms burst received 100/100 but executed only 26 before freshness expiry, so 350 ms is the measured passing row. | Android proof: `/tmp/cu-live-latency-android.jsonl`; Mac proof: `/tmp/cu-live-latency-mac.jsonl`; summary: `/tmp/cu-live-latency-summary.json` |
| 2026-05-18T04:15:15Z | Samsung Galaxy S24 `R3CXB0CNS0J` / `SM_S921U` -> Mac `OpenBurnBar-ComputerUseProof.app` | Android 16 physical device + macOS arm64 proof app, Debug | PASS: Android debug APK rebuilt and installed after adding E2E replay/tamper extras; current Mac proof app rebuilt with `/tmp/DerivedData-cu-phone-chaos-live`, copied to `/Applications/OpenBurnBar-ComputerUseProof.app`, and relaunched with proof denial logging | PASS: same physical Android device and trusted controller identity; replay run launched with `--ei openburnbar.e2e.computerUseReplayCount 1000 --ei openburnbar.e2e.computerUseTamperCount 100`; focused tamper run launched with `--ei openburnbar.e2e.computerUseTamperCount 100 --el openburnbar.e2e.computerUseTamperTimestampStepMillis 100` | PASS for Android live chaos: mixed run sent 1 valid tap, 1,000 duplicate replay frames, and 100 tampered frames; Mac received 1,101 input frames, executed only the initial valid tap, denied 1,100 bad frames, and executed 0 bad frames. Denial reasons were 36 `counter_replay` and 1,064 `stale_timestamp` because synchronous proof logging let many queued replay/tamper frames age past freshness before validation. A second tamper-focused run proved 100/100 tampered frames denied as `signature_failure`, with 0 bad dispatches. | Android replay/mixed proof: `/tmp/cu-live-chaos-android.jsonl`; Mac replay/mixed proof: `/tmp/cu-live-chaos-mac.jsonl`; Android tamper proof: `/tmp/cu-live-tamper-step-android.jsonl`; Mac tamper proof: `/tmp/cu-live-tamper-step-mac.jsonl`; summary: `/tmp/cu-live-chaos-summary.json` |
| 2026-05-18T04:49:00Z | Samsung Galaxy S24 `R3CXB0CNS0J` / `SM_S921U` -> Mac `OpenBurnBar-ComputerUseProof.app` | Android 16 physical device + macOS arm64 proof app, Debug | PASS: Mac proof app rebuilt from current checkout with `/tmp/DerivedData-cu-approval-proof-compile`, copied to `/Applications/OpenBurnBar-ComputerUseProof.app`, and launched through LaunchServices with `OPENBURNBAR_E2E_COMPUTER_USE_APPROVAL_PROOF=1`; focused Mac coordinator approval regression passed; Android relay approval codec test + `:app:assembleDebug` passed | PASS: `adb -s R3CXB0CNS0J install -r android/app/build/outputs/apk/debug/app-debug.apk` reported `Success`; same physical Android device and trusted controller identity; E2E launched with `--ez openburnbar.e2e.computerUseApprovalProof true --ei openburnbar.e2e.computerUseIntentCount 1 --ez openburnbar.e2e.computerUseSendPanic false` | PASS for the Android live approval row: Mac emitted `control.approval.request` for `mac_input_scroll` approval `3C71AC9D-78BD-4B75-9E77-F8AFD4C172DD`; Android received it and sent matching `control.approval.response`; Mac completed the pending action with `status=executed`, audit entry `0`, and audit chain readback showed `approvedBy: phone`. The same run also sent one signed tap afterward and Mac executed it as audit entry `1`. | Android proof: `/tmp/cu-live-approval-android.jsonl`; Mac proof: `/tmp/cu-live-approval-mac.jsonl`; audit chain: `~/Library/Application Support/OpenBurnBar/computer-use-audit/9A4B5EAC-A55B-43FD-8506-CE5870477D8E/chain.jsonl` |
| 2026-05-17T23:49:27Z | iPad Air 11-inch (M4) `407C0B12-010B-5970-8E85-D0E43DA8F457` | iPadOS device, Debug | PASS: `xcodebuild -project OpenBurnBar.xcodeproj -scheme OpenBurnBarMobile -destination "id=407C0B12-010B-5970-8E85-D0E43DA8F457" -configuration Debug -derivedDataPath /tmp/DerivedData-cu-current-ipad -allowProvisioningUpdates -quiet build` exited `0` | PASS on retry: `xcrun devicectl device install app --device 407C0B12-010B-5970-8E85-D0E43DA8F457 /tmp/DerivedData-cu-current-ipad/Build/Products/Debug-iphoneos/OpenBurnBarMobile.app` exited `0` | BLOCKED: `xcrun devicectl device process launch --device 407C0B12-010B-5970-8E85-D0E43DA8F457 com.openburnbar.app` failed because SpringBoard reported the device was locked | `/tmp/cu-current-ipad-build.log`, `/tmp/cu-current-ipad-install-2.log`, `/tmp/cu-current-ipad-launch.log` |
| 2026-05-17T23:55:41Z | Samsung Galaxy S24 `R3CXB0CNS0J` / `SM_S921U` | Android device, Debug APK | PASS: `cd android && ./gradlew :app:testDebugUnitTest --tests '*QuotaStore*' --tests '*ComputerUse*' --no-daemon` exited `0`; `./gradlew :app:assembleDebug --no-daemon` exited `0` | PASS: `adb -s R3CXB0CNS0J install -r app/build/outputs/apk/debug/app-debug.apk` reported `Success` | PASS: `adb -s R3CXB0CNS0J shell am start -W -n com.openburnbar/.MainActivity` reported `Status: ok`; `dumpsys window` showed `mCurrentFocus=Window{... com.openburnbar/com.openburnbar.MainActivity}` and `logcat -b crash` had no post-launch crash entries | `/tmp/cu-android-launch-after-quota.png` |

## Manual Smoke

1. Pair phone with Mac via Hermes iroh relay.
2. Open You -> Computer Use on iPhone.
3. Start a Mac Computer Use session.
4. Confirm the phone sees the session/timeline.
5. Approve one pending action from phone.
6. Reject one pending action from phone.
7. Send panic halt from phone and verify Mac stops dispatching.

## Failure Rules

- Signature, stale timestamp, and replay failures must emit `control.denied`.
- Phone may downgrade trust mode; it must not silently upgrade trust.
- Rejected approvals must write audit entries.
