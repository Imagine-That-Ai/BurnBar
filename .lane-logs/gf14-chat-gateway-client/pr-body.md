## Review Map
- `AgentLens/Services/CLIBridge/OpenAICompatibleChatGatewayClient.swift`: remains the request-path owner; `runStream`, the model allowlist, and the AgentLens budget gate stay here.
- `OpenAICompatibleChatGatewayClient+ToolLoop.swift`: OpenAI tool-loop request/response handling and tool-result wrapping.
- `OpenAICompatibleChatGatewayClient+Errors.swift`: non-streaming fallback and HTTP/auth error shaping.
- `OpenAICompatibleChatGatewayClient+Messages.swift`: OpenAI-compatible message construction, secret scrubbing, and retention/header helpers.
- `OpenAICompatibleModelProbe.swift`: `/v1/models` and `/health` probe helpers and probe result type.
- `AgentToolBroker*.swift`: broker core plus workspace, payload, and restricted-shell sandbox-policy extensions.
- `OpenBurnBar.xcodeproj/project.pbxproj`: regenerated by XcodeGen to include the new Swift files.

## Invariants Preserved
- Pure move split only: no behavior or signature changes intended.
- Budget request-path logic was not edited; `BudgetBlockedError` and the pre-request `BudgetEnforcement.shared.evaluate` gate remain in `runStream`.
- SQL/migration surfaces untouched.
- All split files are <=800 lines.
- No new lint/type suppressions.

## Losslessness Evidence
- Reconstructed the original monolithic file from the split files in original declaration order and diffed it against `git show HEAD:AgentLens/Services/CLIBridge/OpenAICompatibleChatGatewayClient.swift`.
- Normalization only re-applies the minimal `private` modifiers that must become module-internal for cross-file extensions.
- Result: `normalized diff: clean` in `.lane-logs/gf14-chat-gateway-client/losslessness-normalized-diff.log`.

## Visibility Relaxations
Required only because moved declarations now cross Swift file-private boundaries:
- `AgentToolBroker`: `readWorkspaceFile`, `listWorkspaceFiles`, `writeWorkspaceFile`, `exportDesktopFile`, `requiredString`, `denied`, `errorPayload`, `jsonPayload`, `jsonObject(fromArguments:)`, `burnBarJSONValue(fromArguments:)`, `canonicalFileURL`.
- `OpenAICompatibleChatGatewayClient`: both `errorDetail` overloads.

## Validation Matrix
- PASS: `xcodegen generate --spec project.yml` (`.lane-logs/gf14-chat-gateway-client/xcodegen-final.log`).
- PASS: `xcodebuild build -project OpenBurnBar.xcodeproj -scheme OpenBurnBar -destination platform=macOS,arch=arm64 -clonedSourcePackagesDirPath ./.spm-cache-new -disableAutomaticPackageResolution` (`.lane-logs/gf14-chat-gateway-client/xcodebuild-build-4.log`).
- PASS: final-tree rebuild with the repo-documented package env: `FIREBASE_SOURCE_FIRESTORE=1 xcodebuild build -project OpenBurnBar.xcodeproj -scheme OpenBurnBar -destination platform=macOS,arch=arm64 -clonedSourcePackagesDirPath ./.spm-cache-new -disableAutomaticPackageResolution` (`.lane-logs/gf14-chat-gateway-client/xcodebuild-build-final-firestore-source.log`). Note: a bare final retry without this env fails before compilation during SwiftPM resolution because `docs/FIREBASE_IOS27_GRPC.md` requires source-built Firestore and excludes `grpc-binary` from `Package.resolved`.
- PASS: `OPENBURNBAR_APP_TEST_FILTERS=$'OpenBurnBarTests/CLIBridgeTests\nOpenBurnBarTests/AgentSecurityPolicyTests' ./scripts/test-openburnbar-app.sh` (`135` selected tests, `0` failures).
- PASS: `bash scripts/debt/check-swift-file-size-budget.sh`.
- PASS: `bash scripts/ci/check-no-suppressions.sh`.
- PASS: `git diff --check` and `git diff --cached --check`.
- PASS: repo-unique new basenames; each new basename appears exactly once after staging.

## Risks
- Low runtime risk: declaration moves plus the listed module-internal relaxations.
- Review attention should focus on accidental ordering/access changes around broker helpers and the `runStream` budget gate.

## Rollback
Revert commit `67a234c042` to restore the original monolithic file and remove the Xcode project entries for the split files.
