The remediation is well test-backed. The dataExport test explicitly asserts `repoFullName`, `projectDisplayName`, `sourcePath`, `title`, `inferredTaskTitle` are DROPPED while `repoMatchToken` and sealed envelopes survive. The dedup-hash test asserts the stored row has no `sourcePath`/`sourceSlug`/`contentHash` and that a legacy contentHash-only client is rejected with nothing persisted.

One last check on the dataExport allowlist: `contentHash` is at line 247 of `OPAQUE_EXPORT_COLUMNS`. For the `pensieve` domain it's a non-issue (the field no longer exists on `cloud_search_knowledge`). But the same `sealAwareSerializeDoc` runs for the `session_logs` domain (`cloud_search_documents`/`cloud_search_chunks`), where `contentHash` IS written by encryptedSearch.ts. If that contentHash is a keyless SHA-256 of plaintext, allowlisting it lets the export re-emit a guess-confirmation oracle. That's a pre-existing session_logs concern outside this named scope, but since the allowlist lives in dataExport.ts and the line is load-bearing, I'll flag it as an out-of-scope observation rather than a verdict for the three named surfaces. Let me do a final confirmation that no cleartext title/name field is on the allowlist and that the allowlist contains no obviously-plaintext key.

The allowlist (lines 227-248): id, uid, vectorId, embedding, embeddingModelVersion, slugHmac, dedupHash, dedupHashVersion, sourceKind, byteCount, chunkIndex, schemaVersion, repoMatchToken, docID, projectKeyHash, bodyHash, storagePath, tokenHashes, semanticHashes, contentHash.

Two entries warrant scrutiny as potential non-opaque leaks: **`storagePath`** (a Cloud Storage object path — for session_logs these are content-addressed hash paths, opaque; acceptable) and **`contentHash`** (keyless in the session_logs path). Neither affects the three named pensieve surfaces. I have enough to render verdicts.

## VERDICTS

- [CLOSED][high] dataExport.collectInlineJson seal-aware default-deny | evidence: functions/src/callables/dataExport.ts:282-304, 323-341, 227-263 | end_to_end/zero_access domains route through `sealAwareSerializeDoc`, a DEFAULT-DENY allowlist that emits only structurally-detected AES-256-GCM sealed envelopes (`isSealedEnvelope` requires algorithm+nonce+ciphertext+tag or sealedBoxBase64), opaque crypto columns, and Timestamps/numbers/bools; every other top-level string (titles/paths/names/slugs) is DROPPED into `redactedFields[]`. server_readable stays verbatim by design. Test-backed: dataExport.test.ts:142 asserts repoFullName/projectDisplayName/sourcePath/title/inferredTaskTitle dropped, repoMatchToken+envelopes survive. No verbatim plaintext dump for E2E/ZA domains.

- [CLOSED][high] knowledge_repos repoMatchToken + sealedRepoFullName, non-name-derived repoId, webhook token match | evidence: functions/src/callables/knowledgeSync.ts:170-188 (cleartext `repoFullName` used only to compute `repoMatchToken`, NEVER persisted; doc stores repoMatchToken+sealedRepoFullName), :172 (`repoId = safeCloudDocumentID(repoMatchToken)` — token-derived, not name-derived), :117-121 (webhook recomputes `repoMatchTokenFor(full_name)` and matches `where("repoMatchToken","==",…)`); firestore.rules:1899-1904 defense-in-depth rejects client writes containing `repoFullName`/`sourcePath`; privacyBackfill.ts:119-121,220 sweeps legacy cleartext `repoFullName` once sealed name exists. A Firestore-dump adversary sees only an opaque HMAC token + sealed envelope.

- [CLOSED][high] knowledgeMemory.ts rejects raw embeddings + requires vault-keyed dedupHash (no keyless contentHash oracle) | evidence: functions/src/callables/knowledgeMemory.ts:142-151,261 (`requireCloakedVector(raw.cloakedVector)` — raw `embedding` absent → rejected; no `?? raw.embedding` fallback), :193-201 (`resolveDedupHash` = `requireHexDigest(raw.dedupHash)`, no `?? contentHash` fallback, always v1), :295-296 (idempotent skip reads `dedupHash` only, contentHash oracle never read); stored row carries slugHmac/dedupHash, no contentHash/sourcePath/sourceSlug. Test-backed: knowledgeMemoryDedupHash.test.ts:210-227 legacy contentHash-only client REJECTED with nothing persisted; record has no contentHash/sourcePath/sourceSlug.

- [CLOSED][high] knowledgeSearch.ts + hosted-mcp knowledge.ts dropped cleartext sourceSlug/contentHash | evidence: knowledgeSearch.ts:86-93 (filters by vault-keyed `slugHmac` only; no `.where("sourceSlug")`), :115-119 (hits return ciphertext/sealedMetadata/slugHmac/dedupHash — no sourceSlug/contentHash); services/hosted-mcp/src/knowledge.ts:171,176,201-202 (slugHmac filter, no sourceSlug), :248-253 (readKnowledgeDocument returns dedupHash/slugHmac, no contentHash/sourceSlug). Both return only opaque/sealed fields.

- [CLOSED][high] device emitters (PensieveKnowledgeChunker / KnowledgeSyncService / memoryHook.ts) emit dedupHash/slugHmac, drop contentHash/sourcePath | evidence: PensieveKnowledgeChunker.swift:185,196,209-226 (vault-keyed slugHmac+dedupHash; real source_path/sourceSlug sealed inside metadata blob only; no cleartext columns); KnowledgeSyncService.swift:282-302 (`encode` sends sourceSlug+slugHmac+per-vector vectorId/cloakedVector/sealedCiphertext/sealedMetadata/dedupHash — no contentHash/sourcePath); memoryHook.ts:95-98,212,229-239 (vaultKeyedHmac dedupHash/slugHmac; source_path only inside sealed metadata). Note: device sends `sourceSlug` cleartext as the manifest doc-id key (same accepted-leakage class as `knowledge_sync_manifests/{sourceSlug}` ids) — not the named-contract repo identity, so not a residual.

- [CLOSED][medium] Firestore rules reject plaintext on named collections | evidence: firestore.rules:1872-1874 (cloud_search_knowledge `write: if false` — admin-callable only, which enforces sealed/cloaked/keyed inputs), :1879-1881 (knowledge_sync_manifests `write: if false`), :1899-1904 (knowledge_repos create/update via ownerWritableNonSecret AND `!("repoFullName" …)` AND `!("sourcePath" …)`). No client path can inject plaintext into the per-vector rows.

Out-of-scope observations (NOT among the named pensieve surfaces; flagged for the chained pass): `contentHash` remains on the dataExport `OPAQUE_EXPORT_COLUMNS` allowlist (dataExport.ts:247) and is still written as a `requireHexDigest` column by encryptedSearch.ts:254 (cloud_search_chunks) and :353/:377 (project_memory_snapshots) — both in the `session_logs` E2E export domain. If that `contentHash` is a keyless SHA-256 of plaintext, allowlisting it re-emits a guess-confirmation oracle in the session_logs export. The named Pensieve `cloud_search_knowledge` rows no longer carry `contentHash`, so this does not affect the pensieve domain, but it is a residual oracle in the session_logs surface worth closing (seal-key the hash or drop it from both the writer and the export allowlist). `storagePath` is also allowlisted (content-addressed hash path; benign). The unused in-memory `knowledgeVector.ts` store still types `sourceSlug`/`contentHash`, but it is test/emulator-only and superseded by the Firestore path in knowledge.ts — dead, not a live write path.