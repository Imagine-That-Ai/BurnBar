Confirmed `mission_groups` is sealed via `sealGroupPayload` (and the rules enforce `contentSealed == true` + `!hasAny([title,prompt,targetProject,synthesisSummary])`). So `mission_groups` is **CLOSED** — that's the well-remediated sibling, which makes the *unsealed* siblings (`approval_policies`, `rollback_requests`, snapshots) clearly an oversight in the same Hermes Square cluster.

I have a complete picture. Compiling verdicts.

## Summary of findings

The core remediation (the surfaces explicitly named in the scan/scrub/backfill scripts) is solid and verified. But the adversarial broad sweep found a **cluster of unsealed Hermes Square collections** the remediation missed — siblings of the correctly-sealed `mission_groups`, sharing the same private `targetProject`/file-path/scope text, written in plaintext, permitted as plaintext by the rules, and covered by none of the three privacy scripts.

## VERDICTS

- [CLOSED][info] usage_spend (sealedProjectName + projectKeyHash) | evidence: AgentLens/Services/CloudSync/UsageSyncService.swift:151-157,120-138 | Project name sealed; only opaque keyed `projectKeyHash` + intentional server-readable spend metadata. Registry honestly labels server_readable with opaque hash.
- [CLOSED][high] budgetRules (sealedProjectName/sealedLabel) | evidence: AgentLens/Services/CloudBudgetService.swift:166-179 | name+label sealed, opaque hash only.
- [CLOSED][high] conversations / session_logs facets+chunks | evidence: AgentLens/Services/CloudSync/SessionLogSyncService.swift:245,248-249,305,370-371,539-572 | manifest hardcodes `inferredTaskTitle:"Encrypted session"`; only sealedTitle/sealedSnippet/sealedBodyPreview + keyed token/semantic hashes + non-identifying counters. No raw project/path/title/snippet.
- [CLOSED][high] project_memory_snapshots (opaque docID + sealedSnapshot) | evidence: AgentLens/.../SessionLogSyncService.swift:595-617 + functions/src/callables/encryptedSearch.ts:347-414 | opaque vault-keyed docID; plaintext slug/displayName neither accepted nor persisted (schemaVersion 2); legacyDocID used only as a `.delete()` target, never stored.
- [CLOSED][high] knowledge_repos (repoMatchToken + sealedRepoFullName) | evidence: functions/src/callables/knowledgeSync.ts:159-188 | cleartext repoFullName received transiently for HMAC/webhook routing, NEVER persisted; registry carries honest webhook caveat.
- [CLOSED][high] pensieve/knowledge chunks (cloaked vector + sealed text/metadata) | evidence: functions/src/callables/knowledgeMemory.ts:249-318 | only cloakedVector, sealedCiphertext, sealedMetadata, keyed slugHmac+dedupHash; no cleartext sourcePath/text/slug.
- [CLOSED][high] chat_threads / cli_sessions / mobile_assistant_chats (sealedPayload) | evidence: AgentLens/.../ChatThreadSyncService.swift:103-122; OpenBurnBarCore/.../CLIAgentSessionRecord.swift:432-455; OpenBurnBarMobile/Services/MobileChatHistoryStore.swift:390-437 | content sealed, top-level title/preview/messages/customTitle deleted/absent; rules allowlist via keys().hasOnly.
- [CLOSED][high] text_snippets (sealedTitle/Trigger/Body/Scope) | evidence: AgentLens/Services/CloudSync/TextExpansionSyncService.swift:118-138 | all text sealed, only opaque keyed triggerHash.
- [CLOSED][high] cli_agent_mission_requests / mission events (sealed) | evidence: scan assertions at scan-chat-cloud-plaintext.mjs:210-230,503-540; OpenBurnBarMobile/Services/CLIAgentMissionDispatcher.swift | private fields removed top-level, sealedPayload + sealed events.
- [CLOSED][high] mission_groups (sealGroupPayload) | evidence: OpenBurnBarMobile/Services/CLIAgentMissionDispatcher.swift:348-360 + firestore.rules:1577-1580 | content sealed; rules enforce contentSealed==true and `!hasAny(["title","prompt","targetProject","synthesisSummary"])`.
- [CLOSED][high] agent_notification_replies (sealedReplyPayload, generic preview) | evidence: scan-chat-cloud-plaintext.mjs:542-556; functions/src/agentNotifications.ts GENERIC_PREVIEW | no replyText/preview/content-hash in notifications.
- [CLOSED][med] Hermes/Pi relay requests+chunks (schemaVersion>=2 ciphertext-only) | evidence: firestore.rules relayRequestWrite/relayChunkWrite (asserted scan:254-306) | legacy plaintext schema branch removed; payloadCiphertext required.
- [CLOSED-by-design][med] hermes_gateway_messages/events/attachments | evidence: functions/src/callables/hermesGateway.ts:493,1176,632; registry connected_devices=server_readable | Plaintext text/senderDisplayName/fileName written server-side (E2E deferred to chained goal). HONEST LABEL CORRECTED: registry tier server_readable, serverSees names the readable gateway text; deviceOnly no longer claims all relay payloads sealed. Rules deny client writes (allow write: if false). This is the agreed "closed = honest labeling" state.
- [CLOSED][info] scan/scrub/backfill coverage | evidence: scan-chat-cloud-plaintext.mjs:578-666 (hasOnly semantic check across project_memory/gateway/knowledge/media), scrub-chat-cloud-plaintext.mjs:21-135, privacyBackfill.ts:72-122 | All three cover project_memory + gateway + knowledge_repos + media manifest rules; scan adds the semantic `keys().hasOnly([` allowlist assertion; scrub+backfill gate legacy plaintext deletion on a sealed copy existing.

- [RESIDUAL][high] approval_policies — server-readable plaintext targetProject + fileGlob + displayLabel | evidence: OpenBurnBarMobile/Services/ApprovalPolicyStore.swift:230,238,240 (writes plaintext) + firestore.rules:1605-1618 (allowlists them with NO sealing/hasAny rejection) | `targetProject` (≤512) is the SAME private project path/name sealed in usage/budget/mission_groups; `fileGlob` (≤256) reveals working-dir file paths; `displayLabel` is user-authored. Not in scan, scrub, or privacyBackfill. A Firestore-dump adversary reads project + file-path text plainly. (Android writer mirrors this: android/.../data/missions/ApprovalPolicyStore.kt.)
- [RESIDUAL][high] cli_sessions/{id}/snapshots — server-readable plaintext touchedFiles + macSnapshotPath + actionLabel | evidence: firestore.rules:1649-1658 (allowlist permits touchedFiles list ≤64, macSnapshotPath, actionLabel ≤512, all plaintext, no sealing) | `touchedFiles` are literal user working-directory file paths and `macSnapshotPath` is a local Mac filesystem path — directly private. Not in any privacy script or registry domain.
- [RESIDUAL][med] rollback_requests — server-readable plaintext scopeJSON + errorMessage | evidence: OpenBurnBarMobile/Services/RollbackService.swift:143 (writes scopeJSON plaintext) + firestore.rules:1635-1638 (scopeJSON ≤4096, errorMessage ≤2048, plaintext) | scopeJSON encodes the rollback scope (session/file details); errorMessage can carry path/diagnostic text. Not in scan/scrub/backfill.
- [RESIDUAL][med] agent_identities — no field allowlist, plaintext persona/identity text | evidence: firestore.rules:1668-1672 (only `keys().size() <= 24`, no hasOnly, no sealing) | persona overrides / agent identity text can be written server-readable with arbitrary field names; denylist-free block. Not in any privacy script.
- [RESIDUAL][low] subscription_topics — no field allowlist, possible plaintext topic titles/prompts | evidence: firestore.rules:1676-1684 (generic `keys().size() <= 20`, validates only enum cadence/deliveryMode, no field allowlist or sealing) | topic title/template text could be stored plaintext server-readable; not enumerated/sealed.
- [PARTIAL][low] knowledge dedup legacy v0 oracle | evidence: functions/src/callables/knowledgeMemory.ts:295-313, dedupHashVersion | v1 path uses vault-keyed HMAC (closed); but a not-yet-updated client may still send a legacy v0 cleartext SHA-256 contentHash as dedupHash, giving a transient content-confirmation oracle. Tolerated migration state, not a new readable-text write — flagged for the chained reseal/upgrade to fully retire v0.

Root pattern of the misses: the entire **Hermes Square** collection cluster (`approval_policies`, `rollback_requests`, `cli_sessions/snapshots`, `agent_identities`, `subscription_topics`) sits beside the correctly-sealed `mission_groups`, carries the same `targetProject`/file-path/scope private text, but was left plaintext + server-readable, is in **no data-domain registry entry** (so users aren't even told the server reads it), and is covered by **none** of scan/scrub/privacyBackfill. The `approval_policies.targetProject` and `snapshots.touchedFiles`/`macSnapshotPath` are the highest-severity — they leak the exact project/path text the remediation sealed everywhere else.