Confirmed: the gateway carries `senderDisplayName` (line 135) and `displayName` plaintext server-side — exactly the "sender names" the registry now honestly lists in `serverSees`. The honesty labeling is accurate and complete.

I have everything I need. Let me compile the verdicts. The chat_threads dual-gate (hasOnly via `validChatThreadKeys` + denylist via `chatThreadHasPlaintextContent`) was already confirmed at lines 598-639.

Summary of my adversarial findings:
- **session_logs manifest** (307-369): `hasOnly` allowlist + `!hasAny` denylist + sealed-text validation. Only loose key is `inferredTaskTitle` (≤64 char free string), but writer hardcodes the constant `"Encrypted session"` and seals the real title. CLOSED (with a bounded 64-char label noted).
- **session_logs chunk** (401-442): `hasOnly` + denylist + sealed snippet + opaque hashes. CLOSED.
- **chat_threads** (608-639): `hasOnly` + plaintext-content denylist + `sealedPayload`. CLOSED.
- **media_session_events** (2349-2387): `hasOnly` + filename/body/ciphertext rejection + bucketed enums. CLOSED.
- **media_attachment_manifests** (2731-2773): `hasOnly` accepts `sealedFilename`; rejects plaintext `filename` once sealed present — BUT permits a plaintext-only `filename` (≤256 char) when `sealedFilename` absent (dormant migration fallback, no live writer). PARTIAL.
- **knowledge_repos** (1899-1904): rejects `repoFullName` + `sourcePath`; server-only writer stores opaque match token + sealed name. CLOSED.
- **Gateway honesty**: registry `connected_devices` now `server_readable`, names gateway message text/sender names/attachment file names in `serverSees`, scopes sealed claim to E2E relay frames, discloses committed migration; website trust copy matches; 14/14 honesty tests pass; gateway docs genuinely carry plaintext (`text`, `senderDisplayName`, `fileName`) via Admin SDK. CLOSED (honest labeling).

Could not construct any unlisted-plaintext-key write that passes — `hasOnly()` rejects every non-allowlisted key on all five surfaces.

## VERDICTS
- [CLOSED][high] session_logs manifest (rules hasOnly allowlist) | evidence: firestore.rules:307-369 (validSessionLogManifestKeys hasOnly + !hasAny denylist body/text/title/snippet/terms/projectName/workingDirectory + validCloudSealedText on sealedTitle/sealedBodyPreview) | note: unlisted plaintext key (e.g. `messageBody`) is rejected by hasOnly; only free-text key `inferredTaskTitle` is bounded ≤64ch and writer hardcodes constant `"Encrypted session"` (SessionLogSyncService.swift:245) while sealing the real title (line 235). No content can ride along.
- [CLOSED][high] session_logs chunk (rules hasOnly allowlist) | evidence: firestore.rules:401-442 (validSessionLogChunkKeys hasOnly + denylist + validCloudSealedText sealedSnippet + validCloudSearchHashes token/semantic) | note: snippet sealed AES-256-GCM; search hashes are keyed trapdoors (CloudVaultCrypto.searchIndexTokenHashes); no unlisted-key bypass.
- [CLOSED][high] chat_threads (rules hasOnly + plaintext denylist) | evidence: firestore.rules:608-639 (validChatThreadKeys hasOnly + chatThreadHasPlaintextContent rejecting messages/title/preview + validSealedPayloadForUser) | note: backed-up content requires contentSealed + sealedPayload vault envelope; plaintext content keys denied by both gates.
- [CLOSED][high] media_session_events (rules hasOnly allowlist) | evidence: firestore.rules:2349-2387 (validMediaSessionEventKeys hasOnly + !filename + !body + !ciphertext + bucketed enums + peerDeviceIdHash) | note: only bucketed enums/opaque hashes; no free-text field; unlisted key rejected.
- [PARTIAL][med] media_attachment_manifests (filename seal w/ migration fallback) | evidence: firestore.rules:2731-2773 (hasOnly incl sealedFilename+filename; rejects plaintext filename once sealedFilename present, lines 2761-2764) + functions/src/types/legacy.ts:2705-2713 (filename @deprecated, sealedFilename hardened) | note: sealed shape accepted and plaintext rejected when sealed present — matches the stated Fork F=SEAL contract — BUT a plaintext-only `filename` (≤256ch) write is still permitted when sealedFilename is absent; the rule does NOT force sealedFilename. Dormant migration path: no live functions/native writer emits plaintext filename to this doc. Server can still read a legacy plaintext filename. To fully close: require sealedFilename (drop the plaintext-only branch) once migration completes.
- [CLOSED][high] knowledge_repos (rejects plaintext repo identity) | evidence: firestore.rules:1899-1904 (!repoFullName && !sourcePath on client writes; connectKnowledgeRepo Admin SDK stores opaque repoMatchToken + sealedRepoFullName) | note: cleartext repo name observed only transiently server-side for webhook routing, never stored; registry pensieve honesty caveat present.
- [CLOSED][high] Gateway honesty labeling (registry + website + tests) | evidence: packages/data-domains/registry.json:114-118 (connected_devices encryptionTier server_readable; serverSees names "hosted chat gateway message text/sender names/attachment file names"; deviceOnly scopes sealed to "end-to-end relay frame contents"; summary discloses committed E2E migration) + website/src/data/trust.generated.ts:219-230 + registry.test.mjs:223-250 (HONEST CLAIMS connected_devices test) — all 14 data-domains tests pass | note: not sealed, correctly NOT claimed sealed; gateway docs genuinely carry plaintext (functions/src/hermesGateway.ts:150 text, :135 senderDisplayName, :160 fileName) written by Admin SDK under allow-write:false rules (callables/hermesGateway.ts:493). Matches the deferred-E2E pass requirement: dishonest "sealed" label corrected to honest server_readable. No residual website/docs overclaim found.