The only top-level `customTitle` plaintext write is the dead fallback at line 205. RollbackService writes to `cli_sessions/{id}/snapshots` subcollection (governed by `validRollbackSnapshot`, no customTitle). No other plaintext writers.

I have completed a thorough adversarial verification. Let me compile the verdicts.

## Summary of verification

**iOS `CLIAgentMissionDispatcher.swift`** — `cancelMissionUpdate` (line 632) writes exactly `{status:"cancelled", contentSealed, sealedStateSchemaVersion, sealedStateVaultKeyID, sealedStatePayload, updatedAt}`; the cancel summary "Mission cancelled by user." is sealed into `sealedStatePayload` (no plaintext `liveSummary`). `mergeMissionGroupUpdate` (line 559) seals `synthesisSummary` into `sealedStatePayload`. `applySealedPrivatePayload` (line 996) strips all 10 plaintext private keys before every `setData`. Every `setData` (lines 176/368/408/541/625) writes only sealed output. The sealed envelope `sealedPayloadDictionary` (CloudVaultCrypto.swift:189) emits exactly the 5 keys `validCloudSealedPayload` requires.

**iOS `CLIAgentChatReader.swift`** — `updateSessionMetadata` (line 144) re-seals the whole record via `encodeSealed` (CLIAgentSessionRecord.swift:432, emits only allowlisted keys, no top-level customTitle/title/preview/messages) and `FieldValue.delete()`s the 4 legacy plaintext fields (line 205). No plaintext fallback (returns early on missing key, line 180-187).

**Android `ThreadInboxStore.kt`** — `parseCLISession` (line 302) has a sealed-read branch (line 312 → `parseSealedCLISession`, reads `payload.customTitle` from inside the vault). `resealSessionWithCustomTitle` (line 234) re-seals and deletes top-level `customTitle`. **BUT** `updateSessionMetadata` (line 190-220) has a plaintext fallback that writes top-level `customTitle` when reseal fails.

**`firestore.rules`** — `validMobileMissionCancel` (line 1393) `affectedKeys().hasOnly([status, contentSealed, sealedStatePayload, sealedStateSchemaVersion, sealedStateVaultKeyID, updatedAt])` exactly matches both iOS and Android cancel writes. `validMissionGroup` (line 1567) allowlist covers the merge write. `validCliSessionMirror` (line 1164) allowlist covers `encodeSealed`/Android-reseal output and rejects top-level `customTitle`.

## VERDICTS
- [CLOSED][high] iOS mission cancel (liveSummary sealed) | evidence: OpenBurnBarMobile/Services/CLIAgentMissionDispatcher.swift:632-646 vs firestore.rules:1393-1403 | cancelMissionUpdate writes exactly {status,contentSealed,sealedStateSchemaVersion,sealedStateVaultKeyID,sealedStatePayload,updatedAt}; "Mission cancelled by user." sealed into sealedStatePayload (no plaintext liveSummary); diff.affectedKeys hasOnly matches validMobileMissionCancel exactly; umbrella validCliAgentMissionRequest also passes on merged doc (line 1279-1282).
- [CLOSED][high] iOS mergeMissionGroup (synthesisSummary sealed) | evidence: CLIAgentMissionDispatcher.swift:559-576 vs firestore.rules:1567-1597 | synthesisSummary sealed into sealedStatePayload; rules denylist !hasAny(["synthesisSummary"]) (line 1577) + allowlist includes sealedState* keys; merge:true preserves create-time sealedPayload so validMissionGroup passes.
- [CLOSED][high] iOS dispatch/create + child fan-out + group create | evidence: CLIAgentMissionDispatcher.swift:996-1014, 833, 856 | applySealedPrivatePayload strips title/prompt/targetProject/liveSummary/synthesisSummary/etc. before every setData; no raw build()/initialQueuedEvent output reaches Firestore (all via buildSealed/sealGroupPayload/initialQueuedEventSealed).
- [CLOSED][high] iOS updateSessionMetadata re-seal (no top-level customTitle) | evidence: OpenBurnBarMobile/Services/CLIAgentChatReader.swift:169-209; CLIAgentSessionRecord.swift:432-478 | re-seals whole record via encodeSealed (allowlisted keys only), FieldValue.delete()s customTitle/title/preview/messages; no plaintext fallback (early return on missing key, line 180-187); satisfies validCliSessionMirror (firestore.rules:1164-1199).
- [CLOSED][med] Android parseCLISession sealed-read branch | evidence: android/.../square/ThreadInboxStore.kt:312-314, 355-404 | sealed branch reads customTitle/title/messages from decrypted sealedPayload (payload.customTitle line 400); legacy plaintext branch retained only as read-side fallback.
- [CLOSED][med] Android mission cancel parity | evidence: android/.../assistants/CLIAgentMissionDispatcher.kt:403-420, 93-119 | sealedMissionStateUpdate yields {status,updatedAt,contentSealed,sealedStateSchemaVersion,sealedStateVaultKeyID,sealedStatePayload} = validMobileMissionCancel allowlist; liveSummary sealed; reader uses statePrivate?.liveSummary fallback (line 870).
- [PARTIAL][med] Android updateSessionMetadata reseal-failure fallback | evidence: android/.../square/ThreadInboxStore.kt:190-220 (esp. 203-218) | PRIVACY CLOSED (no leak): when reseal returns false the fallback writes top-level plaintext customTitle, but validCliSessionMirror (firestore.rules:1164-1199) lacks customTitle in its hasOnly allowlist AND mandates contentSealed==true + validSealedPayloadForUser, so the server REJECTS the write — plaintext never lands. CORRECTNESS RESIDUAL: on its only trigger paths (vault key unavailable on a sealed doc, or genuine legacy-plaintext doc) the rename is silently dropped at the rules layer (no error surfaced to user). iOS has no such fallback (returns early). Recommend removing the plaintext fallback (lines 203-218 for the customTitle case) and surfacing a "key unavailable" error to match iOS, or routing the legacy-doc case through a seal-on-first-write.