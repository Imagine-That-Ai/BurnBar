The one remaining hit (privacy.astro:322) is a NEGATION ("never receives readable message text") — that's correct, not a leak. All my owned surfaces now state the gateway is sealed end-to-end. 

All verification passes: registry test (16/16), scanner (pass), dataExport tests (16/16), functions tsc (clean), drift coverage ([]), gen files in sync, Android in sync. Let me write the changelog.

## CHANGELOG

**`packages/data-domains/registry.json`** (B-honesty owned)
- `connected_devices`: tier stays `server_readable` (mixed domain), but flipped the gateway honesty — `summary` NOTE now says "end-to-end relay frames AND the hosted chat gateway are both sealed and never readable by the server … routes ciphertext per-link … never reads message text, sender names, or attachment file names. The per-agent subscription graph is cloaked behind opaque keyed doc ids." `serverSees` removed the three "hosted chat gateway …" entries, added `"opaque relay key material (public keys)"`. `deviceOnly` changed to `"end-to-end relay + gateway frame contents (message text, sender names, attachment file names — all sealed on-device)"`. (Crypto/codename jargon kept OUT of the registry so the generated public website copy passes the copy policy — detail lives in PROVIDERS.md.)
- `media`: filename → deviceOnly. `summary` NOTE added (manifest records only opaque hash/mime/size/peer-id-hash/direction/timestamps; `sealedFilename` is sealed on-device). `serverSees` now lists only opaque facets; `deviceOnly` adds `"attachment file names (sealed on-device)"`.
- `excludedCollections.subscription_topics`: extended reason to disclose the cloaked graph (opaque vault-keyed HMAC doc id + `sealedAgentURI`/`sealedTopicID`); kept the asserted `sealed`/`vault key`/`never` tokens.

**`packages/data-domains/gen/{domains.ts,DataDomains.swift,DataDomains.kt}` + `website/src/data/trust.generated.ts`** — regenerated via `node packages/data-domains/codegen.mjs` (byte-in-sync with registry; verified). Android in-tree `android/.../data/domains/DataDomains.kt` synced via `./gradlew :app:syncGeneratedSources` (BUILD SUCCESSFUL, verified diff-clean). Public website copy confirmed free of codenames/crypto jargon (`Hermes`/`iroh`/`Mercury`/`Pi agent`/`AES-256`/`P-256`/`ECDH`/`HermesRelayCrypto` all absent).

**`packages/data-domains/registry.test.mjs`** (cross-stream coupling — the registry's own test, co-owned with registry codegen) — flipped `HONEST CLAIMS: connected_devices …` from asserting the readable-gateway claim to asserting the sealed contract: serverSees must NOT name gateway message text/sender name/file name, must declare `opaque … key material`, deviceOnly must state gateway frame contents are `sealed`, summary must say `gateway … sealed` and must NOT say `server can read`. Without this flip the registry edit would have broken CI (it previously REQUIRED the readable claim). Test suite: 16/16 pass.

**`functions/src/callables/dataExport.ts`** (B-honesty owned)
- `isSealedEnvelope`: added detection of the Hermes gateway relay envelope — `relayEncryption === "p256-hkdf-sha256-aesgcm"` + string `payloadCiphertext` + string `wrappedKey` → opaque passthrough.
- `OPAQUE_EXPORT_COLUMNS`: added `relayEnvelope`, `relayEncryption`, `relayKeyVersion`, `sealedFilename`, `sealedAgentURI`, `sealedTopicID`, and the gateway/media routing-only opaque columns `sequence`, `kind`, `blobHash`, `mime`, `size`, `peerDeviceIdHash`, `direction`.
- New `SEAL_AWARE_CONTENT_COLLECTIONS = {hermes_gateway_events, hermes_gateway_messages, hermes_gateway_attachments}` and wired into `collectInlineJson`: these collections now ride the default-deny `sealAwareSerializeDoc` even though their parent `connected_devices` domain is `server_readable` — so plaintext `text`/`senderDisplayName`/`fileName` can never round-trip the export. (This is the tier reclassification the CONTRACT mandates, implemented without diverging from the registry's per-domain tier — which keeps the dataExport.test.ts drift gate green.)
- Updated the file-header docstring to document the mixed-domain seal-aware reclassification + the `relayEnvelope` passthrough.

**`functions/src/__tests__/dataExport.test.ts`** (tests I own for dataExport) — added: `isSealedEnvelope` detects the gateway `relayEnvelope` (and rejects partial/wrong-algorithm shapes); `sealAwareSerializeDoc` emits a sealed gateway event as `relayEnvelope`+routing metadata while dropping plaintext `text`/`senderDisplayName`/`threadId`/`destinationId`; emits a sealed media manifest as `sealedFilename`+opaque columns while dropping plaintext `filename`; emits sealed `sealedAgentURI`/`sealedTopicID` while dropping cleartext `agentURI`/`topicID`. Suite: 15/15 pass (16/16 with fail-closed suite). `tsc --noEmit` clean.

**`scripts/privacy/scan-chat-cloud-plaintext.mjs`** (B-honesty owned)
- Registry honesty (`assertRegistryPrivacyHonesty`): flipped `connected_devices` — now FAILS if serverSees claims the server reads gateway message text/sender name/file name; REQUIRES `opaque … key material` in serverSees, a `gateway … sealed` deviceOnly entry, and no `server can read` in the summary. Added a `media` block: FAILS if serverSees claims it reads file names; REQUIRES a sealed file-name deviceOnly entry.
- Gateway block comment updated to "server-only writer, NOW sealed end-to-end"; kept the three `assertRulesBlockDeniesClientWrite` (still valid — gateway stays `allow write: if false`).
- Added a "Sealed-content export coverage" section pinning my `dataExport.ts`: asserts `isSealedEnvelope` detects the `p256-hkdf-sha256-aesgcm` relay envelope; the three gateway content collections are in `SEAL_AWARE_CONTENT_COLLECTIONS`; `sealedFilename`/`sealedAgentURI`/`sealedTopicID`/`relayEnvelope` are in `OPAQUE_EXPORT_COLUMNS`; and the bare plaintext keys `text`/`senderDisplayName`/`fileName`/`filename` are NOT allowlisted. Scanner: **passes** (exit 0).

**`docs/PENSIEVE.md`** (B-honesty owned) — Security & threat model: rewrote the v0 dedup line to the flag-day reality — the cleartext-SHA-256 oracle is GONE end-to-end: search floors `dedupHashVersion == 1` + the bumped `bge-small-en-v1.5-vault-dedup-v1` tag, commit idempotent-skip never matches a v0 row, and `purgeLegacyKnowledgeVectors` deletes v0 + retired-tag rows, so the oracle survives in no served or queryable row.

**`docs/PROVIDERS.md`** (B-honesty owned) — retitled the relay/gateway block "both sealed end-to-end (honest label)"; rewrote the gateway bullet to "Shipped — the gateway is sealed end-to-end": describes the P-256 pairing (agent pubkey via `handleRuntimeStatus`, phone pubkey at `approveHermesGatewayDeviceGrant`), the per-direction sealing of events `{text,senderDisplayName,threadId}` / messages `{text}` / attachment manifest `{fileName,byteCount,contentType}`, and that the server stores only `relayEnvelope` ciphertext + routing metadata and never reads message text/sender names/file names.

**`website/src/pages/privacy.astro`** (B-honesty owned) — Zone C Cloud Run relay bullet and the Hermes Remote Relay opt-in card both rewritten: the hosted chat gateway is now stated as sealed end-to-end, routing ciphertext only, never receiving readable message text/sender names/file names. (Domain-inventory section auto-updates from the regenerated `trust.generated.ts`.)

### Verification run (all green)
- `node --test packages/data-domains/registry.test.mjs` → 16/16
- `node scripts/privacy/scan-chat-cloud-plaintext.mjs` → pass (exit 0) — **no expected cross-stream failures**; the gateway-writer rule assertions are satisfied by the existing `allow write: if false` blocks, and the sealed-export contract is pinned against my own dataExport.ts.
- `npx vitest run dataExport.test.ts dataExportFailClosed.test.ts` → 16/16
- `npx tsc --noEmit` (functions) → clean
- driftcheck `findDrift` → uncovered: `[]`
- Android `DataDomains.kt` synced via gradle (BUILD SUCCESSFUL), diff-clean; all gen files byte-in-sync with registry.

### Deviations / cross-stream notes
- **Tier reclassification approach**: rather than splitting `connected_devices` into a new `end_to_end` registry domain (which would ripple through the per-domain drift gates in `dataExport.test.ts` and `registry.json`/codegen), I implemented the gateway content-collection reclassification as a per-collection `SEAL_AWARE_CONTENT_COLLECTIONS` override inside `dataExport.ts`. This achieves the CONTRACT's "events/messages/attachments → end_to_end serialization; clients/destinations/typing/state stay server_readable" outcome (sealed envelopes only, plaintext dropped) without diverging the registry tier — keeping every drift gate green. Flagged here for the orchestrator in case a dedicated `end_to_end` gateway domain is preferred later.
- **`registry.test.mjs` ownership**: not listed under any stream in CONTRACT §STREAM OWNERSHIP, but it is the registry's own honesty test and directly contradicted the mandated registry flip. I co-owned it with the registry edit (the complete move — flipping the registry without flipping its test would knowingly red CI). No other B-honesty file depends on it.
- **`scripts/privacy/scrub-chat-cloud-plaintext.mjs`** shows as modified in `git status` but was edited by the migration tail stream, not me (B-honesty owns `scan-`, not `scrub-`). No conflict.
- **No blockers.** The honesty/registry/docs/website/dataExport slice is self-contained and does not depend on B-fn-gw / B-ios-gw / B-media / B-sub landing first to pass its own tests. The sealed claims I now publish become fully true once those streams ship the actual sealing (the gateway producer seal in iOS/agent, the media `sealedFilename` writer, and the subscription cloak) — which is the intended ship-together with PR2 per the CONTRACT.