

████ result 0 ████
## VERDICTS
- [BLOCKER][BROKEN] Sealed gateway reply shows "Hermes replied without text." in chat | OpenBurnBarMobile/Services/HermesService.swift:1498 | reads legacy `reply.text` (nil for sealed) instead of opened `reply.displayText`; every E2E reply renders empty in the primary conversation surface.
- [HIGH][BROKEN] Undecryptable/wrong-device reply masked as benign empty reply in chat | OpenBurnBarMobile/Services/HermesService.swift:1499 vs HermesSettingsView.swift:549 | chat path has no sealed-for-another-device state; a key/device mismatch looks like an empty Hermes answer with no recourse (the graceful copy exists only in the Settings hero).
- [HIGH][RISK] Multi-device E2E is single-device; recovery copy ambiguous, no re-pair affordance | OpenBurnBarMobile/Services/HermesGatewayRelayKeypair.swift:44, FunctionsRepository.swift:813, HermesSettingsView.swift:550 | per-device keychain key + single published pubkey means only the last-paired device can open replies; "Open BurnBar on the device that approved this gateway" is ambiguous (phone vs Mac) and re-pairing device B silently steals the channel from A.
- [MEDIUM][RISK] User-facing copy leaks protocol jargon ("relay key", "man-in-the-middle", "E2EE") | OpenBurnBarMobile/Views/Hermes/HermesSettingsView.swift:1067,1074,1886 + store:2192,2196 | violates copy policy (no transport/protocol jargon); the only recourse text is also the jargon-heavy text, so it needs a plain-language rewrite, not removal.
- [MEDIUM][RISK] No visible E2E lock indicator on the messaging surface | OpenBurnBarMobile/Views/Hermes/HermesTabView.swift:2756 | the only lock icon is the unrelated "API keys stored locally" footnote; E2E status appears only buried in Settings, so the headline security is invisible during normal chat.
- [LOW][RISK] Raw algorithm/key constants shown in Security section | OpenBurnBarMobile/Views/Hermes/HermesSettingsView.swift:1610,1620,1622 | renders raw base64 pubkey + literal `p256-hkdf-sha256-aesgcm` + key version to users — codec jargon on a user-visible screen.
- [LOW][WORKS] Send-side fail-closed gates + safe push preview | OpenBurnBarMobile/Views/Hermes/HermesSettingsView.swift:2395-2402, FunctionsRepository.swift:1459, functions/src/agentNotifications.ts:293 | send blocks on canSeal/key-change and surfaces an error bubble; seal core independently re-verifies the pin; server uses GENERIC_PREVIEW so no ciphertext/plaintext leaks to the lock screen. Tried to bypass via stale pre-flight notice — authoritative seal gate holds.

████ result 1 ████
## VERDICTS
- [BLOCKER][RISK] Agent TOFU pin-jacking from untrusted relay when persisted pin is absent | /Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:809 (`if pinned is None:`) via :895 (`_absorb_relay_state` on the `/destinations` response) and :714 (env-only seed) | With `BURNBAR_RELAY_E2E=1` surviving but `BURNBAR_RELAY_PEER_PUBLIC_KEY` lost (env write is best-effort, :819-820), the first untrusted `/destinations`/`/state`/`/events` response pins an attacker `relayPublicKey`; all agent→phone replies/attachments are then sealed to the attacker — full MITM. Untested (every pin test pre-seeds a pin: test_burnbar_plugin.py:400/663).
- [HIGH][RISK] Untrusted server can flip a never-E2E agent into E2E, then pin-jack | adapter.py:879-882 (`relayCapable is True or e2eEnabled is True` → `self._relay_e2e_enabled = True`) | Server toggles E2E on for an unpaired agent and, combined with the TOFU window above, lands an attacker key — moving a plaintext link to an attacker-keyed "encrypted" one.
- [MEDIUM][RISK] Agent pin durability is asymmetric to the phone | adapter.py:818-820 (best-effort `save_env_value`, swallowed on failure) vs HermesGatewayRelayKeypair.swift:247 (Keychain `ThisDeviceOnly`, fail-closed) | A persist failure silently re-arms TOFU on the agent; the phone refuses-to-seal on its equivalent failure. Persist-failure should be fail-closed on the agent too.
- [LOW][RISK] AAD routing ids freely mutable by the untrusted server | adapter.py:873-878, consumed at :671 | Server-fed `uid`/`clientId` mismatch breaks event opens (DoS), and widens attacker-writable identity state; not a confidentiality break.
- [WORKS] Server relay-key immutability across runtime/rotate/revoke/oversight | functions/src/callables/hermesGateway.ts:736-744, 756-775, 1226/1259, 1387/1441/1608/1644 | Pinned key is never overwritten; re-pair mints a new random clientId; no callable writes the relay-key fields after pinning. Tried token rotation, delete+recreate, /runtime swap — all blocked.
- [WORKS] Phone verifyOrPin refuses changed agent key before sealing; Keychain failure fails closed | OpenBurnBarMobile/Services/FunctionsRepository.swift:1459-1464; HermesGatewayRelayKeypair.swift:142-143, 177-178, 224-231 | Ciphertext built only after `allowsSeal`; mismatch/unreadable/undecodable/empty all refuse; pin clear is operator-only, not relay-triggerable.

████ result 2 ████
## VERDICTS

- [BLOCKER][BROKEN] Gateway key-wrap AAD diverges across languages; no cross-language test catches it | OpenBurnBarMobile/Services/FunctionsRepository.swift:1485 vs /Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:672 | iOS wraps the per-event key authenticating `gatewayEventKeyAAD` (`"gatewayEventKey"`, FunctionsRepository.swift:1485) while Python's `_open_envelope` unwraps under the SAME aad it uses for the payload, `_gateway_event_aad` (`"gatewayEvent"`, adapter.py:671-672). The labels differ → AES-GCM tag mismatch → `open_event` catches `Exception` and drops every real iOS→agent sealed event. Symmetric break agent→phone: Python `seal_message` wraps under `gatewayMessage` (adapter.py:535) but iOS unwraps under `gatewayMessageKeyAAD`=`"gatewayMessageKey"` (FunctionsRepository.swift:620). Both directions of the E2E channel fail on real traffic. Invisible because every test round-trips one language against itself (Python test uses one AAD for wrap+payload, test_burnbar_plugin.py:470-471; iOS test uses two, OpenBurnBarMobileTests.swift:189-194) and the wire vector contains zero gateway labels.

- [BLOCKER][BROKEN] No cross-language interop vector for ANY gateway AAD label | /Users/albertonunez/.hermes/hermes-agent/tests/gateway/fixtures/HermesRelayWireVector.json | The vendored Swift→Python vector covers only `request`/`key`/`chunk` AADs (the pre-existing realtime-relay path); it has no `gatewayEvent`, `gatewayEventKey`, `gatewayMessage`, `gatewayMessageKey`, `gatewayAttachment*`, or `gatewayModelSwitch` material (`has any gateway label: False`). The entire NEW feature's wire format is asserted only by same-language round-trips, which is precisely why the wrap-AAD divergence above ships green. A real Swift-generated gateway vector opened by Python (and vice-versa) is the missing launch gate.

- [HIGH][BROKEN] model_switch AAD label mismatch iOS↔Python (second, independent divergence) | OpenBurnBarMobile/Services/FunctionsRepository.swift:1480 vs /Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:640,200 | iOS routes model_switch through `applyGatewayEventSeal` → seals/wraps under the EVENT label (`gatewayEvent`/`gatewayEventKey`) with `modelId` inside the event payload; iOS has no model-switch AAD at all. Python `open_model_switch` opens under a distinct `_gateway_model_switch_aad` (`"gatewayModelSwitch"`, adapter.py:200,640). Even after fixing the wrap-AAD bug above, a real iOS model_switch still fails the Python tag. The CONTRACT (line 29) said "model_switch keeps cleartext modelId" — the distinct sealed `gatewayModelSwitch` label was a Python-only invention during the fix and was never mirrored to iOS. Tests pass because each side opens its own ciphertext.

- [HIGH][INCOMPLETE] iOS has no gateway-attachment open path; Python's attachment-AAD test guards a feature with no phone consumer | OpenBurnBarCore/Sources/OpenBurnBarCore/SharedModels/HermesRelayCrypto.swift (14 gateway refs, none attachment) | iOS `HermesRelayCrypto` exposes only `gatewayEvent*`/`gatewayMessage*` AADs — no `gatewayAttachmentManifest/Body/Key`, no decode path in FunctionsRepository. The Python adapter seals agent→phone attachments under those labels (adapter.py:545-580) and `test_seal_attachment_round_trip` rigorously proves the swap-resistance — but the phone cannot open the result. The test is internally meaningful yet validates a wire the only counterparty can't read; sealed attachments are undecryptable on-device.

- [MEDIUM][INCOMPLETE] Replay test does not exercise the finding's actual scenario (sealed redelivery) and there is no restart-replay test | /Users/albertonunez/.hermes/hermes-agent/tests/gateway/test_burnbar_plugin.py:854 ; adapter.py:719 | Finding #6 was "relay can redeliver a valid SEALED event." `test_duplicate_event_id_is_dropped` uses a `_legacy_adapter` plaintext event, not a sealed one (the dedup at adapter.py:959 runs before sealing, so it's exercised, but the named attack isn't reproduced). More importantly `_seen_event_ids` is an in-process `OrderedDict` (adapter.py:719), never persisted; only the cursor survives restart. No test restarts the adapter and redelivers at the resumed cursor, so cross-restart replay defense is unproven.

- [MEDIUM][WORKS] Grace-window downgrade has no test because the grace window does not exist | functions/src/hermesGateway.ts:86 | The CONTRACT (line 34) specified a `relayCapable`-gated plaintext grace window before a cutoff; the implementation is `gatewayPlaintextWriteAllowed(_relayCapable, _now) => false` — both args unused, hard-false. Security-positive (no plaintext writes ever) and matches Codex's "hard-false = GOOD," but the documented downgrade path the audit asked about is absent, so its non-existence (not just its safety) should be asserted by a test to prevent a future re-introduction silently re-opening plaintext writes.

- [LOW][INCOMPLETE] iOS pin-store `unknownKeychainError` fail-closed branch is untested | OpenBurnBarMobile/Services/HermesGatewayRelayKeypair.swift:136,142 ; OpenBurnBarMobileTests/OpenBurnBarMobileTests.swift:329 | The pin store fails closed (`allowsSeal == false`) on a Keychain read error, but `freshPinStore()` uses the real Keychain and no test injects a failure, so the security-critical "can't verify the pin → refuse to seal" path never runs in CI. The `.mismatch` path is well-covered; only the I/O-failure fail-closed is a blind spot.

- [LOW][WORKS] No concurrent-pairing test for server TOFU pin | functions/src/callables/hermesGateway.ts:726-739 ; functions/src/__tests__/hermesGatewayKeyImmutability.test.ts | The immutability tests are sequential (seed pinned → attacker swap). There's no test for two near-simultaneous first-pairing `/runtime` writes racing the TOFU establish (read-modify-write without a transaction guard visible at the pin site). Given the in-memory double serializes calls, a real Firestore race could let a second key win; worth a concurrency test, though lower risk than the interop breaks above.

Note: the pinning, fail-closed-send, serialize-strip-siblings, sanitize-strict (key-version clamp 2/99 → undefined), sealed-write-rejects-plaintext, and iOS seal-refusal tests are all genuinely adversarial and I could not break them by reading the logic — those fixes are sound and well-tested. The failure mode the team missed is entirely at the **cross-language wire boundary**, which no test in any of the three suites crosses.

████ result 3 ████
## VERDICTS
- [BLOCKER][BROKEN] Key-wrap AAD mismatch breaks E2E in both directions | OpenBurnBarMobile/Services/FunctionsRepository.swift:1485,620 vs /Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:535,672 | iOS wraps/unwraps the per-event/message symmetric key under `gatewayEventKey`/`gatewayMessageKey` AADs; Python wraps/unwraps under `gatewayEvent`/`gatewayMessage` → AES-GCM `InvalidTag` on every unwrap → agent drops all phone events and phone renders every agent reply as "sealed for another device". E2E never decrypts a single message on the wire.
- [BLOCKER][BROKEN] Sealed model_switch double-mismatched AAD | OpenBurnBarMobile/Services/FunctionsRepository.swift:1374-1381,1480 vs adapter.py:208,640 | iOS seals model_switch under `gatewayEvent`/`gatewayEventKey`; Python `open_model_switch` opens under `gatewayModelSwitch` → unwrap+open both fail → every E2E model switch silently dropped at adapter.py:973.
- [HIGH][BROKEN] Agent→phone sealed attachments have no phone-side opener | adapter.py:555-571 vs OpenBurnBarCore/Sources (no `gatewayAttachment*` AAD, no `hermes_gateway_attachments` reader) | Python seals attachment body/manifest/key with `gatewayAttachment*` AADs that have zero Swift counterpart → phone can never open an agent-sent sealed file.
- [HIGH][RISK] Tests encode the bug as truth; no cross-language gateway-AAD fixture | tests/gateway/test_burnbar_plugin.py:452,467 + tests/gateway/test_relay_e2ee.py:46-53 | Python round-trips use the same wrong AAD for wrap and payload, so they pass; the only interop fixture (`HermesRelayWireVector.json`) covers the `request`/`key` relay flavour, never any `gateway*` envelope — so the wire break is invisible to CI. "62 passing" proves nothing about cross-impl correctness.
- [LOW][WORKS] PR1 contains zero crypto, zero junk | git diff origin/main...ajnunezg/burnbar-gateway-pr1 | Grep for all crypto markers returns empty; no api_server.py / __pycache__ / user_ascii_apple.txt. Correctly labeled.
- [LOW][WORKS] PR2 remote head is the security-fix commit; fixes are in the diff | ajnunezg/burnbar-gateway-e2ee = 87465d3ac | `_pin_peer_public_key`/`must_seal`/`_event_already_seen` present; the open PR ships the fixed (not vulnerable) version.
- [LOW][WORKS] No adapter/crypto drift across repos | fork adapter.py vs tools/hermes-platform-burnbar/adapter.py | Byte-identical (diff exit 0); __init__.py + plugin.yaml identical; companion commits edee4da0a/3988402dc/cf6debe30 contain the server immutability guard + iOS Keychain pin store as claimed.
- [MEDIUM][WORKS] Server agent-key immutability + v1 key-version clamp (read+write) | functions/src/callables/hermesGateway.ts:734-744, functions/src/hermesGateway.ts:473,514 | A bearer token cannot overwrite a pinned `agentRelayPublicKey` at /runtime (logged + dropped); both validators reject any keyVersion ≠ 1. Tried token-swap and forged-version paths; both correctly refused.

Net: the protocol-layer fixes (pinning, fail-closed, replay, persistence, clamps, strip-siblings) are genuinely solid and the PR plumbing/consistency is clean — but the underlying E2E channel is **dead on the wire** because Python and Swift never agreed on the key-wrap AAD labels, and the test suite was structured so this could never surface. This would not survive a staff security review: it ships an encryption feature that cannot decrypt anything between the two real endpoints.

████ result 4 ████
## VERDICTS
- [BLOCKER][BROKEN] iOS↔Python gateway wrap-AAD divergence | OpenBurnBarMobile/Services/FunctionsRepository.swift:620,625,1480,1485 vs plugins/platforms/burnbar/adapter.py:529-536,671-673 | iOS wraps/unwraps event+message keys under `gatewayEventKeyAAD`/`gatewayMessageKeyAAD` while Python uses single `gatewayEvent`/`gatewayMessage` for both seal+wrap (the CONTRACT.md:29-30 spec); every reply is `InvalidTag`→unreadable on the phone and every inbound event is dropped by the agent — the E2E channel is dead both directions in production while all unit tests pass.
- [HIGH][INCOMPLETE] Shared fixture does not pin the gateway wire | tests/gateway/fixtures/HermesRelayWireVector.json (only request/key/chunk) | The one "byte-exact" interop gate has zero `gatewayEvent`/`gatewayMessage`/`gatewayAttachment` vectors; gateway AADs are asserted only against same-language literals, so #1 shipped green and any future AAD rename/reorder in one of 3 impls re-breaks production undetected. Fix: add an iOS-sealed→Python-opened (and reverse) gateway wire vector.
- [HIGH][RISK] README "5-line reuse" reproduces the rotating-key and single-AAD footguns | plugins/platforms/burnbar/README.md:50-65 | The advertised snippet calls `AgentRelayIdentity.load_or_create()` without `persist=` (Codex #4: key rotates per restart, breaks prior sealed events — relay_e2ee.py:303-309) and uses one `message_aad` for seal+wrap (the convention incompatible with canonical Swift, #1); any new adapter copying it inherits both defects.
- [MEDIUM][WORKS] Gateway handler boundaries clean; "is-sealed" predicate duplicated | functions/src/hermesGateway.ts:654,681,708-712 | Strip-siblings/grace-window are well-centralized (serializeHermesGatewayEvent + gatewayPlaintextWriteAllowed), but "doc is sealed" is re-derived with slightly different predicates in ~4 spots; extract `isSealedGatewayDoc()` so a future field can't be dropped in one check but echoed in another.
- [LOW][RISK] Rotation deferral architecture is extendable but under-documented | callables/hermesGateway.ts:712 ; OpenBurnBarMobile/Services/HermesGatewayRelayKeypair.swift:118 ; plugins/platforms/burnbar/adapter.py:804 | Clean rotation seams exist (clearPin, keyVersion plumbing, _pin_peer_public_key) but the "reject-changed-key, rotation needs a signature over new-bound-to-old" invariant lives only in scattered comments across 3 enforcement points — a new engineer could "fix" the mismatch-rejection in one impl and reopen the MITM. Add one ROTATION note naming all enforcement points.
- [LOW][WORKS] Reusable core defaults leak BurnBar branding | gateway/crypto/relay_e2ee.py:63 ; gateway/crypto/__init__.py:1 | `RelayNamespace` makes the API genuinely generic, but the default `RELAY_PRIVATE_KEY_ENV="BURNBAR_RELAY_PRIVATE_KEY"` and BurnBar-only docstrings undercut the "any adapter" claim; overridable via `env_var=` so non-blocking.

████ result 5 ████
## VERDICTS

- **[BLOCKER][BROKEN] Event key-wrap AAD mismatch — every phone→agent E2E event is undecryptable** | `OpenBurnBarMobile/Services/FunctionsRepository.swift:1485` vs `/Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:672` | The iOS phone wraps the per-event symmetric key with `gatewayEventKeyAAD` → `OpenBurnBar-HermesRelay-v1|gatewayEventKey|uid|cid|eid` (FunctionsRepository.swift:1485), but `_open_envelope` unwraps it with `_gateway_event_aad` → `...|gatewayEvent|...` (adapter.py:671-672 reuse the payload AAD for both unwrap and open). Proven byte-unequal. `unwrap_symmetric_key` raises `InvalidTag`; the adapter logs "failed to open sealed event" and drops it. **Attack/failure: the entire inbound E2E channel is dead — the phone cannot deliver a single message to the agent.** The adapter has no `gatewayEventKey` helper at all (only attachments got a distinct `gatewayAttachmentKey`), violating CONTRACT §CRYPTO "keyAAD for wrap, requestAAD for payload."

- **[BLOCKER][BROKEN] Message key-wrap AAD mismatch — every agent→phone E2E reply is undecryptable** | `/Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:534-535` vs `OpenBurnBarMobile/Services/FunctionsRepository.swift:620` | `seal_message` wraps the key with `_gateway_message_aad` → `...|gatewayMessage|...` (adapter.py:535), but the phone unwraps with `gatewayMessageKeyAAD` → `...|gatewayMessageKey|...` (FunctionsRepository.swift:620). Proven byte-unequal → `resolvedText == nil` → "sealed for another device" (FunctionsRepository.swift:628-630). **Every agent reply renders blank on the phone.** E2E is broken in both directions.

- **[BLOCKER][BROKEN] Sealed model_switch payload AAD mismatch — model switching never applies over E2E** | `OpenBurnBarMobile/Services/FunctionsRepository.swift:1480` vs `/Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:640,200-208` | The phone seals the model_switch payload with `gatewayEventAAD` (`...|gatewayEvent|...`) and wraps with `gatewayEventKeyAAD` (it reuses the normal-event seal path, FunctionsRepository.swift:1374-1381→1480/1485), but the adapter's `open_model_switch` opens with `_gateway_model_switch_aad` (`...|gatewayModelSwitch|...`) for both unwrap and payload (adapter.py:640,662-672). Both slots mismatch → `InvalidTag` → "failed to open sealed model_switch" (adapter.py:973) → dropped. **The phone's "switch model" command is silently discarded on every E2E link.** (The fix #7 design is sound; the two implementations just chose different labels.)

- **[HIGH][BROKEN] Agent→phone sealed attachments have no phone-side opener and use adapter-invented AAD labels** | `/Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:567-572` vs `OpenBurnBarCore/Sources/OpenBurnBarCore/SharedModels/HermesRelayCrypto.swift` | The adapter seals the attachment manifest/body with `gatewayAttachmentManifest`/`gatewayAttachmentBody` and wraps the body key with `gatewayAttachmentKey`, but Swift `HermesRelayCrypto` defines no `gatewayAttachment*AAD` at all (only event/message), and no iOS code unwraps a gateway attachment body key. **Any file the agent sends is undecryptable by the phone.** Distinct-AAD fix #6 is internally consistent but unanchored to any peer.

- **[MEDIUM][RISK] Self-referential interop tests hide the AAD break** | `/Users/albertonunez/.hermes/hermes-agent/tests/gateway/test_burnbar_plugin.py:452-453,466-467` | The adapter tests unwrap event/message keys with the same `_gateway_event_aad`/`_gateway_message_aad` the adapter wrapped with, and the "all AADs distinct" set (lines 613-617) never includes an event/message *key* AAD — so the suite green-lights a wire-incompatible scheme. The locked `tests/gateway/fixtures/HermesRelayWireVector.json` only covers the relay-*request* flavour (`key`/`request`), never the gateway event/message key-wrap, so the interop gate is blind to all three blockers above. A real iOS-envelope → adapter-open fixture would have caught this.

- **[LOW][WORKS] Fail-closed (#2), standalone seal (#3), serialize-strip (#8), key-version clamp (#10), plaintext-write/downgrade gates** | `adapter.py:485-502,1364-1372`; `hermesGateway.ts:86-88,670-718,460-495,507-532`; `callables/hermesGateway.ts:258-263,1520-1540,1214` | I actively tried to break these: `must_seal` is independent of crypto availability and the only fallback is refuse (never plaintext); `_standalone_send` refuses when E2E-paired and the adapter can't build; `serializeHermesGatewayEvent` unconditionally drops text/senderDisplayName/threadId for any sealed-or-schema≥2 doc (admin/backfilled/corrupt included); `gatewayPlaintextWriteAllowed()` is hard-`false`, so spoofing `relayCapable=false`/stripping the peer key yields a denial of service, not a plaintext downgrade — on both write and read. The model_switch cleartext `modelId` echoed by the server (hermesGateway.ts:713) is ignored by the adapter's sealed path (adapter.py:976 reads only the opened envelope), so it is not an injection vector. DataExport routes gateway docs through a default-deny seal-aware serializer (`dataExport.ts:225-228,354-360`). These fixes are correct — but they all sit behind a relay link that, per the three blockers, never successfully opens a single sealed frame.

████ result 6 ████
## VERDICTS
- [BLOCKER][BROKEN] Key-wrap AAD divergence (event/message/model_switch) | OpenBurnBarMobile/Services/FunctionsRepository.swift:1485,620 vs /Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:535,671 | iOS wraps under `gatewayEventKey`/opens under `gatewayMessageKey`; Python uses `gatewayEvent`/`gatewayMessage` for the wrap too → InvalidTag on every real inbound event and every outbound reply → E2E messaging is 100% non-functional in production; no test round-trips a phone-sealed event/message through the Python adapter so the suite stays green.
- [HIGH][BROKEN] Replay dedup bypass via stripped top-level `id` | /Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:845,958,670 | Relay blanks `raw.id`; `_event_already_seen` skips empty ids while AAD resolves from `envelope.eventId` → captured sealed command re-triggers on every poll.
- [HIGH][RISK] No persistent replay store + no freshness window; cursor is relay-controlled | /Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:719,950,697,854 | In-RAM LRU(4096) is the only defense; emptied on restart and the malicious relay also controls `nextCursor`, so post-restart it replays all captured events; LRU flood (4096 distinct old events) re-opens the window without a restart.
- [HIGH][RISK] Agent relay private key not 0600 in containers | /Users/albertonunez/.hermes/hermes-agent/hermes_cli/config.py:562,5193-5205 | `_secure_file` no-ops under `_is_container()`/managed, and `save_env_value` restores the prior (possibly 0644) mode → `BURNBAR_RELAY_PRIVATE_KEY` readable by co-tenants in the common Docker deploy; fix #4's 0600 claim is false there.
- [MEDIUM][INCOMPLETE] Persist failure silently keeps ephemeral key → restart rotates, wedges pinned link | /Users/albertonunez/.hermes/hermes-agent/gateway/crypto/relay_e2ee.py:330-336 ; adapter.py:823 | Unwritable `.env` → swallowed debug log, new key on next restart, phone refuses the changed pinned key, link dead with no user-surfaced error; under E2E this should fail loudly, not be best-effort.
- [LOW][WORKS] Attachment manifest/body/key distinct AAD (#6) | /Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:555,568,571 | Distinct labels on seal+open, body key bound to attachmentId; cross-slot swap fails the tag (verified, tried to break).
- [LOW][WORKS] Crypto primitives byte-exact vs Swift | /Users/albertonunez/.hermes/hermes-agent/gateway/crypto/relay_e2ee.py:355,423,222 ; tests/gateway/test_relay_e2ee.py:117-150 | Fresh 12B nonces, validated curve points, raw-scalar import, salt-empty≡32-zero proven by opening Swift ciphertext; no deviation.

████ result 7 ████
## VERDICTS
- [HIGH][BROKEN] `unsubscribe` leaves a ghost cloud subscription when the vault key is transiently unavailable | OpenBurnBarMobile/Views/Hermes/Square/AgentBrandZoneView.swift:1139 (+ Android AgentSubscriptionTopicStore.kt:237) | `if let vaultKey { delete(opaqueDocID) }` skips the opaque-doc delete on nil key but still removes the local row and clears `lastError`; the sealed doc resurrects the subscription on the next snapshot/refresh — user keeps receiving opted-out digests with a success UI.
- [HIGH][RISK] Re-ingest doubles stored chunks against the tier cap until the daily purge, blocking the migration it requires | functions/src/callables/knowledgeMemory.ts:300,356 | Old v0 doc id (SHA-256) ≠ new v1 doc id (vault-HMAC), so both coexist; the cap aggregate counts the whole collection with no version filter, so a near-cap Pro user's first post-flag-day re-ingest trips `failed-precondition` and cannot re-ingest until `purgeLegacyKnowledgeVectorsScheduled` runs (≤24h, longer past the 500-user page).
- [MEDIUM][RISK] Phone reinstall orphans the old client; agent keeps sealing replies to the lost phone key | functions/src/callables/hermesGateway.ts:1259 | Re-pair mints a new `clientId` (graceful for the phone) but never revokes the old client; the agent's old token keeps replying into the old clientId, sealing to the now-deleted phone pubkey — permanently undecryptable messages, no reaper for superseded clients.
- [MEDIUM][RISK] Replay cache is process-local; a redelivered event after adapter restart re-executes | plugins/platforms/burnbar/adapter.py:719,855 | `_seen_event_ids` is in-memory and bounded; only the persisted cursor survives restart, so a compromised relay re-serving an old `eventId` at `cursor ≥ current` after a restart (or after `MAX_SEEN_EVENT_IDS` eviction) re-triggers the agent action.
- [MEDIUM][RISK] Cross-platform `consentGivenAt` type mismatch corrupts subscription ordering | OpenBurnBarMobile/Views/Hermes/Square/AgentBrandZoneView.swift:1252 vs android AgentSubscriptionTopicStore.kt:429 | iOS writes Timestamp, Android writes Long; Firestore `order(by:consentGivenAt)` ranks by type, so on a mixed-device account all Android topics sort before all iOS topics regardless of real consent time.
- [MEDIUM][RISK] Pensieve cap check is TOCTOU under concurrent commits | functions/src/callables/knowledgeMemory.ts:300,390 | Two concurrent `commitKnowledgeBatch` both read the pre-write aggregate, both pass the cap gate, both write non-transactionally — caps are exceedable by the sum of concurrent batches (self-DoS only).
- [LOW][WORKS] Server + phone + adapter key pinning (TOFU, immutable post-pair, fail-closed) | functions/src/callables/hermesGateway.ts:725; OpenBurnBarMobile/Services/HermesGatewayRelayKeypair.swift:161; plugins/platforms/burnbar/adapter.py:794 | Actively attempted bearer-token swap, post-pair rotation, empty-key seal — all rejected/logged and fail closed.
- [LOW][WORKS] Dedup idempotent-skip + v0 retirement floor + model-version parity | OpenBurnBarCore/.../PensieveKnowledgeChunker.swift:230; functions/src/callables/knowledgeMemory.ts:323; knowledgeSearch.ts:102 | Content-derived doc id makes unchanged re-ingest a true no-op; v0 rows are floored out of recall and purge predicates can't touch v1 rows.
- [LOW][WORKS] Rollback legacy `inFlight` decode across every read path (iOS + Android) | OpenBurnBarCore/.../RollbackContracts.swift:134; OpenBurnBarMobile/Services/RollbackService.swift:183; android RollbackService.kt:86,174 | Sole dict read goes through `init?(wireValue:)`; encode emits snake_case; Android `whereIn` covers both wire forms; no `rawValue`-only path drops the row.
- [LOW][WORKS] `serializeHermesGatewayEvent` strip-siblings + sealed model_switch | functions/src/hermesGateway.ts:681,712 | `isSealedDoc` (envelope OR schema≥2) unconditionally drops text/threadId/senderDisplayName; model_switch requires a relayEnvelope on relay-capable targets and the adapter refuses unsealed control events.

████ result 8 ████
## VERDICTS
- [BLOCKER][BROKEN] Legacy gateway plaintext never auto-scrubbed | functions/src/callables/privacyBackfill.ts:169 + functions/src/callables/hermesGateway.ts:1338 | Old `hermes_gateway_*` docs carry plaintext text/senderDisplayName/fileName; backfill gates deletion on `requires:"relayEnvelope"` which write-once server docs never gain, no TTL/delete-on-delivery, and the only scrubber (`scrub-chat-cloud-plaintext.mjs`) is unscheduled/manual — so old plaintext is stored AND served (serializeHermesGatewayEvent schema-1 fallback) server-readable forever until an operator runs `--all-users --apply`.
- [HIGH][RISK] Replay cache reset-on-restart + flood-eviction, undocumented | /Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:719,848 | In-memory per-process 4096-bound set: an agent restart empties it (pin is persisted, this isn't) and an attacker can flood 4096 fresh sealed events to evict a captured id, then a relay replays the old sealed event (e.g. a message/model_switch) and it re-executes — not flagged as a known limitation anywhere.
- [MEDIUM][INCOMPLETE] Pensieve recall lost on deploy for non-daemon members; no model-bump re-ingest | AgentLens/Services/CloudSync/KnowledgeSyncService.swift:168 (no callers) | Search floors `dedupHashVersion==1` + new model tag immediately, but only the Mac daemon's 15-min full-walk re-ingests under the new tag; iOS/Android/chat_memory sources have no re-ingest trigger, so those members' recall silently returns nothing until sources are manually re-fed.
- [MEDIUM][RISK] Hosted-MCP knowledge search lacks the dedup-v0 floor | services/hosted-mcp/src/knowledge.ts:174 | The in-product callable hard-floors `dedupHashVersion==1`, but the hosted-MCP path filters only an OPTIONAL `embeddingModelVersion` and never floors the version — a non-shim OAuth caller omitting the tag queries v0 rows (ciphertext is opaque and dedupHash isn't returned, so no direct oracle, but the retirement is inconsistently enforced across the two search surfaces).
- [LOW][RISK] iOS rollback observer drops legacy "inFlight" rows | OpenBurnBarMobile/Services/RollbackService.swift:86 | iOS `whereField("status", in:["pending","in_flight"])` excludes legacy camelCase `"inFlight"` while Android `RollbackService.kt:174` includes it; a pre-migration in-flight rollback row is visible on Android but invisible to the iOS live observer and never normalized — the "legacy inFlight decodes everywhere" claim is false on iOS.
- [LOW][WORKS] purgeLegacyKnowledgeVectors registration/scheduling/predicate-safety | functions/src/callables/knowledgeMemory.ts:551,567 + functions/src/index.ts:96 | Owner callable + daily onSchedule both registered; exact retired-string equality can't over-delete freshly re-ingested `-vault-dedup-v1` rows; single-field purge queries need no composite index. Tried to find an over-delete or missing-registration and failed.
- [LOW][WORKS] media-filename + subscription-cloak tails complete end-to-end | OpenBurnBarMobile/Features/Mercury/Stores/MediaAttachmentManifestStore.swift:55 + android/.../AgentSubscriptionTopicStore.kt:426 | Live writers seal + strip cleartext + delete legacy ids; rules hard-drop plaintext branches; Android byte-for-byte HMAC doc-id parity with iOS. Tried to find a half-wired writer/missing rule and failed.
- [NIT][WORKS] dataExport doesn't leak legacy gateway plaintext on export | functions/src/callables/dataExport.ts:225,344 | Gateway content collections are forced through the default-deny seal-aware serializer even under the server_readable `connected_devices` domain, so a legacy plaintext field is dropped into `redactedFields[]` rather than exported — but it confirms the data physically remains in Firestore (export drops, doesn't delete).

████ result 9 ████
## VERDICTS

- **[BLOCKER][BROKEN] CONFIRMED — Agent→phone message key-wrap AAD mismatch; every reply blank** | `adapter.py:535` vs `FunctionsRepository.swift:620` | Adapter `seal_message` wraps the key with `_gateway_message_aad`=`...|gatewayMessage|...` (532=payload, 535=wrap, same label); iOS unwraps with `gatewayMessageKeyAAD`=`...|gatewayMessageKey|...` (620). Different HKDF info + GCM AAD → `unwrapSymmetricKey` throws → `resolvedText=nil` → "sealed for another device" (607). Reproduced byte-unequal. Every agent reply renders blank.

- **[BLOCKER][BROKEN] CONFIRMED — Phone→agent event key-wrap AAD mismatch; no inbound message decryptable** | `FunctionsRepository.swift:1485` vs `adapter.py:672` | iOS `sealGatewayEventPayload` wraps with `gatewayEventKeyAAD`=`...|gatewayEventKey|...` (1485); adapter `_open_envelope` unwraps the same wrappedKey with `_gateway_event_aad`=`...|gatewayEvent|...` (672, the single `aad` reused for unwrap+open). InvalidTag → frame dropped. The entire inbound E2E channel is dead; the phone cannot deliver one message to the agent.

- **[BLOCKER][BROKEN] CONFIRMED — model_switch DOUBLE AAD mismatch; model switching silently discarded** | `FunctionsRepository.swift:1374-1381,1480,1485` vs `adapter.py:640,672-673` | iOS routes model_switch through `sealGatewayEventPayload` (1374-1381) → seals payload with `gatewayEventAAD`=`gatewayEvent` (1480) and wraps with `gatewayEventKeyAAD`=`gatewayEventKey` (1485). Adapter `open_model_switch` opens with `_gateway_model_switch_aad`=`gatewayModelSwitch` for BOTH slots (640→672-673). Both payload AND key mismatch → InvalidTag → "failed to open sealed model_switch" → dropped (adapter.py:977). The phone's switch-model command is silently discarded on every E2E link. (Worse than the message/event cases, which mismatch only the key slot — here both slots differ.)

- **[HIGH][BROKEN] CONFIRMED — Agent→phone sealed attachments have no iOS opener** | `adapter.py:567-571` (manifest `gatewayAttachmentManifest`, body `gatewayAttachmentBody`, key `gatewayAttachmentKey`) vs iOS | `grep gatewayAttachment` across `OpenBurnBarMobile/` + `OpenBurnBarCore/` = **0 hits**; Swift `HermesRelayCrypto` defines only event/message AADs (lines 111-128), no `gatewayAttachment*AAD`; no iOS code unwraps a gateway attachment body key (FunctionsRepository only handles message/event envelopes, 612-625). Every file the agent sends is undecryptable by the phone. The distinct-AAD design is internally consistent but anchored to no peer.

- **[MEDIUM][RISK] CONFIRMED — Self-referential interop tests green-light the wire-incompatible scheme** | `test_burnbar_plugin.py:451-453,467-470,612-618` and `fixtures/HermesRelayWireVector.json` | The "phone" in the adapter tests unwraps the message key with `_burnbar._gateway_message_aad` (452) and seals/wraps the event with `_burnbar._gateway_event_aad` for both slots (469-470) — the adapter's own labels, never the real Swift `gatewayMessageKey`/`gatewayEventKey`. The "all distinct" set (612-618) omits any event/message *key* AAD because those helpers don't exist. The fixture only carries the relay-request `key`/`request`/`chunk` parts (no gateway event/message key-wrap). So no test, fixture, or interop gate exercises the actual iOS→adapter key-unwrap path — all three blockers pass CI. A real iOS-envelope→adapter-open fixture catches every one.

- **[LOW][WORKS] CONFIRMED — Fail-closed, standalone-seal, serialize-strip, key-version clamp, plaintext-write gates, peer-key pinning** | `adapter.py:485-502` (`can_seal`/`must_seal`), `1369-1394` (`_standalone_send` refuses, never plaintext on paired link), `662-679` (pin AFTER successful open, never overwrite pinned key) | I tried to break these and could not: `must_seal` is independent of crypto availability so the only fallback is refuse; the pin is adopted only after `unwrap`+`open` succeed (672-673 precede 678), so a forged inbound `senderPublicKey` cannot poison TOFU without first producing a valid frame under the pinned key. These are correct — but they all sit behind a relay link that, per the three blockers, never opens a single sealed frame, so "fail-closed" here means "the paired link refuses plaintext AND can't decrypt" = total messaging outage, not a leak.

- **[INFO] REFUTED-as-overstated nuance on the report's framing** | The report calls the AAD construction "Proven byte-unequal" without showing the construction is otherwise identical; I verified it IS identical (swift `aad()` 209-211 ≡ python `_gateway_aad` 180-194), so the mismatch is isolated to the parts list — strengthening, not weakening, the blockers. No correction to severity; the report's conclusions stand.

**Net:** The 10 protocol fixes and 3 privacy tails are correct *in isolation on each side*, but the gateway event/message/model_switch flavour was given distinct payload-vs-key AAD labels on iOS and a single shared label on the adapter. The feature cannot move a single sealed byte end-to-end. Ship-blocking. Minimal fix: add `gatewayEventKey`/`gatewayMessageKey`/`gatewayModelSwitchKey` helpers in adapter.py and pass them to `wrap_symmetric_key`/`unwrap_symmetric_key` (separate from the payload AAD), add Swift `gatewayAttachment*AAD` + an iOS attachment-body opener, and add a real iOS-vector→adapter-open interop fixture.

████ result 10 ████
## VERDICTS
- [BLOCKER][CONFIRMED-REVISED] First-pairing TOFU MITM is real but present on BOTH endpoints, not agent-only | adapter.py:809/870-872/895 + FunctionsRepository.swift:270 + HermesGatewayRelayKeypair.swift:171-176 | Both sides TOFU-pin their peer's relay key from a server-controlled source with no out-of-band key verification (only the user-code *session* is authenticated, hermesGateway.ts:469/516, no fingerprint/SAS anywhere); a malicious relay in the pairing path substitutes both keys on first contact and reads both directions. The report's BLOCKER stands, severity unchanged, but its agent-only/env-durability framing is corrected to a fundamental dual-endpoint TOFU gap.
- [HIGH][CONFIRMED] Agent runtime TOFU seed comes from untrusted `/destinations`/`/events`/`/state` when no pin is present | adapter.py:870-872 (source="state") reachable from :895/:947/:1076, seed-less precondition via :714/:721 | When `_relay_e2e_enabled=True` and `_peer_public_key=None`, the first untrusted relay doc pins an attacker `relayPublicKey`; all agent→phone replies/attachments then seal to the attacker (seal path :378/:325, gated only by `can_seal` :485-490). This is the agent half of the BLOCKER; confirmed independently.
- [HIGH][CONFIRMED-ADDED] Malicious `device/poll` creates the seed-less precondition with no env corruption | adapter.py:1545-1548 | `relayCapable is True` (server-controlled) persists `BURNBAR_RELAY_E2E=1` without writing `BURNBAR_RELAY_PEER_PUBLIC_KEY`; next `connect()` TOFU-pins the attacker key. Cleaner than the report's best-effort-env route — the env write becomes irrelevant. (Honest server blocks this at :1214/:555-568; malicious server is in-scope.)
- [MEDIUM][CONFIRMED] Agent pin persistence is best-effort/swallowed | adapter.py:818-820 | A persist failure silently re-arms TOFU on the next restart; the phone's equivalent is durable Keychain. Confirmed as cited, but note this only *widens* the BLOCKER's window — the first-pairing MITM does not need it.
- [MEDIUM][REVISED-DOWNGRADED] Untrusted server flips a never-E2E agent into E2E | adapter.py:879-882 | Confirmed reachable, but downgraded from the report's [HIGH][RISK]: a never-E2E agent already emits server-readable plaintext, so flipping+pin-jacking adds no confidentiality loss — the harm is a forged "encrypted" UI state (honesty/integrity), not a fresh leak.
- [LOW][CONFIRMED] AAD routing ids (`uid`/`clientId`) freely server-mutable | adapter.py:873-878 consumed at :671 | Wrong ids break event opens (DoS); not a confidentiality break. As reported.
- [LOW][REFUTED] Event-path `senderPublicKey` as a free MITM seed | adapter.py:676-680 | REFUTED as an *unauthenticated* seed: adoption occurs only after a successful `unwrap_symmetric_key` with the agent's real private key (:672); test_burnbar_plugin.py:671-697 confirms an attacker `senderPublicKey` cannot rotate the pin. The report correctly excluded this, but I flag it explicitly since the fix changelog lists events as a pin source.
- [WORKS][CONFIRMED] Server relay-key immutability across runtime/rotate/revoke/oversight/re-pair | hermesGateway.ts:732-743, 764-776, 1226/1259, 1387/1441/1576/1611 | Pinned key never overwritten; re-pair mints a fresh random clientId; no callable writes relay-key fields post-pin. Tried /runtime swap, token rotation, delete+recreate — all blocked.
- [WORKS][CONFIRMED] Phone verifyOrPin refuses a changed agent key before sealing; Keychain failure fails closed | FunctionsRepository.swift:1459-1464; HermesGatewayRelayKeypair.swift:142-143, 166, 177-178, 224-227 | Ciphertext built only after `allowsSeal`; mismatch/unreadable/undecodable/empty all refuse; pin clear is operator-only. Holds for *post-pairing* swaps; does NOT cover the first-pairing seed (see BLOCKER).

████ result 11 ████
## VERDICTS
- [BLOCKER][BROKEN] Sealed gateway reply renders "Hermes replied without text." in chat — **CONFIRMED** | OpenBurnBarMobile/Services/HermesService.swift:1498 | Reproduced: `finishBurnBarGatewayTurn` reads legacy `reply.text` (nil for schema≥2 sealed docs per FunctionsRepository.swift:540-543) instead of the opened `reply.displayText`; both `applyPendingGatewayReplyIfNeeded` callers (HermesTabView.swift:579,2289) pass the decoded `latestReply`, so 100% of E2E replies show the empty-text fallback on the primary surface. git blame proves the E2E commits nilled `text` without migrating this consumer.
- [HIGH][BROKEN] Chat thread masks undecryptable / wrong-device reply as benign empty reply — **CONFIRMED** | OpenBurnBarMobile/Views/Hermes/HermesTabView.swift:579 / HermesService.swift:1499 vs HermesSettingsView.swift:549-551 | Reproduced: HermesTabView has zero sealed-state handling (no `isSealed`/`displayText`/graceful copy); `newestThreadReply` selects a sealed reply even when unopened (FunctionsRepository.swift:698), so a `resolvedText==nil` reply still becomes `latestReply` and renders the generic fallback. The graceful "sealed for another device" copy exists only in the Settings hero (`gatewayReplyBodyText`), never in the conversation.
- [HIGH→MEDIUM][RISK] Multi-device E2E is single-device; ambiguous recovery copy, no device↔client binding — **REVISED** | OpenBurnBarMobile/Views/Hermes/HermesSettingsView.swift:2134,2263 ; functions/src/callables/hermesGateway.ts:1226,1259 | Lockout is real (unfiltered message read + `selectedClient` auto-default to another device's client → reply sealed to the other device's key), but the report's "re-pair silently steals/ping-pongs the channel by flipping the pubkey" is refuted: each pairing mints a new clientId with `merge:false` and the agent key is server-pinned immutable (`:734-742`). Downgraded to MEDIUM because no channel-steal occurs; the gap is missing device identity + ambiguous copy ("the device that approved this gateway").
- [MEDIUM][RISK] User-facing copy leaks protocol jargon ("relay key", "man-in-the-middle", "E2EE") — **CONFIRMED** | OpenBurnBarMobile/Views/Hermes/HermesSettingsView.swift:1067,1074,1886,2192,2196 | Reproduced verbatim: ":1067 …relay key changed … a possible man-in-the-middle. … Re-pair Hermes Gateway"; ":1074/:2192 …Cloud gateway messages are E2EE/end-to-end encrypted and require relay keys"; ":1886 Relay key changed — re-pair / Update/re-pair for E2EE"; ":2196 gatewayRelayKeyChangedMessage … man-in-the-middle". Violates the benefit-first / no-protocol-jargon copy policy; this is also the only recourse surface so it needs rewriting, not deleting.
- [MEDIUM][RISK] No persistent E2E indicator on the gateway messaging surface — **CONFIRMED** | OpenBurnBarMobile/Views/Hermes/HermesTabView.swift:2756 | Reproduced: the only `lock.shield.fill` is the connection sheet's "API keys stay securely stored locally" footnote (unrelated to message E2E); the chat thread and compose bar carry no encryption affordance. "E2EE ready" appears only in a buried per-client subtitle (HermesSettingsView.swift:1886) and the Settings readiness notice (:1081). For an E2E-headline feature the security is invisible during normal use.
- [LOW][RISK] Raw protocol constants in the Security section — **CONFIRMED** | OpenBurnBarMobile/Views/Hermes/HermesSettingsView.swift:1610,1620,1623 | Reproduced: renders "Relay Public Key" (raw base64), "Encryption" = literal `relayEncryption` (`p256-hkdf-sha256-aesgcm`), and "Key Version". This is the realtime-relay connection section (`mode == .relayLink`), not the gateway, and is diagnostic/advanced — but still user-visible jargon inconsistent with the policy. Low.
- [LOW][WORKS] Send-side fail-closed gates are correct and surfaced in chat — **CONFIRMED** | OpenBurnBarMobile/Views/Hermes/HermesSettingsView.swift:2395,2399 ; OpenBurnBarMobile/Services/FunctionsRepository.swift:1459-1463 ; functions/src/agentNotifications.ts:21,293 | Reproduced and actively tried to break: `sendGatewayMessage` pre-flight gates on `canSealToAgent` + `agentRelayKeyChanged`, and the authoritative seal core independently enforces `pinStore.verifyOrPin(...).allowsSeal`, throwing `gatewayRelayKeyChanged` BEFORE any ciphertext is built — so a stale UI notice can't leak an unverified-key send. The remote push hardcodes `GENERIC_PREVIEW` for all thread writes (no code reads sealed bodies into a preview) → no ciphertext/plaintext leak. WORKS.
- [MEDIUM][BROKEN] (ADDED) Undecryptable sealed reply is still selected as `latestReply`, compounding #2 — **ADDED** | OpenBurnBarMobile/Services/FunctionsRepository.swift:698 | `newestThreadReply` returns `message.isSealed` true regardless of whether `resolvedText` opened, so a tampered/wrong-AAD/wrong-device sealed doc becomes the rendered reply with `displayText==nil`. The Settings hero degrades gracefully (`gatewayReplyBodyText:549`), but the chat path (#1) turns it into the silent empty-reply (#2). Reinforces that the chat consumer, not just the resolver, must branch on `isSealed && displayText==nil`.

████ result 12 ████
## VERDICTS
- [BLOCKER][BROKEN] Key-wrap AAD mismatch breaks E2E in both directions — **CONFIRMED** | OpenBurnBarMobile/Services/FunctionsRepository.swift:1485,620 vs /Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:535,672 | I reproduced all four Swift builders (HermesRelayCrypto.swift:112-128), both iOS call-sites, Python's single-AAD wrap/open, and `relay_e2ee.wrap_symmetric_key` passing `aad` verbatim (no `Key` suffix). HKDF-info + GCM-AAD both diverge → hard `InvalidTag`; phone→agent events silently dropped at adapter.py:992, agent→phone replies → `resolvedText=nil` at FunctionsRepository.swift:629. E2E decrypts nothing on the wire.
- [BLOCKER][BROKEN] Sealed model_switch triple-mismatched AAD — **CONFIRMED** | FunctionsRepository.swift:1374,1480,1485 vs adapter.py:640,648 | iOS seals model_switch via the *event* sealer (`gatewayEvent`/`gatewayEventKey`); Python `open_model_switch` opens under `gatewayModelSwitch` → fails on label family and key-wrap split → dropped at adapter.py:973.
- [HIGH][BROKEN] Agent→phone sealed attachments have no phone-side opener — **CONFIRMED** | adapter.py:545-580 vs OpenBurnBarCore/Sources (no `gatewayAttachment*AAD`) | Grep confirms Swift has only the 4 gateway builders; the only mobile attachment-manifest consumers are the unrelated realtime-relay `HermesRealtimeRelayAttachmentManifest` (AttachmentBubble.swift:16). Python's `gatewayAttachment*` envelopes are unopenable.
- [HIGH][RISK] Tests encode the bug as truth; no cross-language gateway-AAD fixture — **CONFIRMED** | tests/gateway/test_burnbar_plugin.py:450-454,467-471 + fixtures/HermesRelayWireVector.json | Test comment literally says "SAME gateway AAD" for wrap+payload; fixture top-level keys are only request/key/chunk — no `gateway*`. The wire break is invisible to CI; "passing" proves nothing about interop.
- [LOW][WORKS] PR1 contains zero crypto/junk — **CONFIRMED (not independently re-grepped)** | git diff origin/main...burnbar-gateway-pr1 | pr1 head `f24554f9` confirmed on remote; I did not re-run the full crypto-marker grep but it is not load-bearing.
- [LOW][WORKS] PR2 head is the security-fix commit — **CONFIRMED** | ajnunezg/burnbar-gateway-e2ee = 87465d3ac (verified via git ls-remote) | `_pin_peer_public_key`/`open_model_switch`/replay-cache present in the shipped adapter.
- [LOW][WORKS] No adapter/crypto drift across repos — **CONFIRMED (partial)** | I read the fork adapter.py directly; did not byte-diff against tools/hermes-platform-burnbar/adapter.py, but the cited Python logic is the load-bearing artifact and it is what I audited.
- [MEDIUM][WORKS] Server v1 key-version clamp (read+write) + agent-key immutability — **CONFIRMED** | functions/src/callables/hermesGateway.ts:217-218,738; functions/src/hermesGateway.ts:479-482,520 | keyVersion forced to exactly 1 on both sides; post-pairing key swap rejected (`agent_relay_public_key_immutable`). Correct.
- [ADDED][REVISED severity context] The channel is dead in BOTH directions including reply *generation* — the report framed agent→phone as a render-placeholder; in fact Python-sealed replies (`gatewayMessage`/`gatewayMessage`) can never be opened by iOS (`gatewayMessageKey`/`gatewayMessage`), so no successful plaintext crosses in either direction.

Net: the report is correct and I independently reproduced its core attack. The protocol-hardening fixes (pinning, fail-closed, replay, clamps, immutability) are genuinely solid, but the underlying E2E channel is non-functional on the wire because Python and Swift never agreed on the key-wrap AAD label, and the test suite was structured so this could never surface. Ships an encryption feature that cannot decrypt anything between the two real endpoints. Would not survive a staff security review.

████ result 13 ████
## VERDICTS
- [BLOCKER][BROKEN] iOS↔Python gateway wrap-AAD divergence breaks E2E both directions | OpenBurnBarMobile/Services/FunctionsRepository.swift:1485,620 vs plugins/platforms/burnbar/adapter.py:535,671-673 | **CONFIRMED.** iOS wraps under `gatewayEventKey`/`gatewayMessageKey`; Python wraps+unwraps under `gatewayEvent`/`gatewayMessage`. Wrap-AAD feeds HKDF info + GCM tag (HermesRelayCrypto.swift:174,177; relay_e2ee.py:394) → guaranteed InvalidTag → every reply nil'd (HermesSettingsView.swift:2598→FunctionsRepository.swift:628), every inbound event dropped (adapter.py:992-994 path). Live, not dead code.
- [HIGH][INCOMPLETE] Shared fixture pins only the realtime relay, not the gateway wire | tests/gateway/fixtures/HermesRelayWireVector.json (requestAAD/keyAAD/chunkAAD only) | **CONFIRMED.** Zero gateway vectors; each platform asserts its own labels against its own literals (test_burnbar_plugin.py:593-605; OpenBurnBarMobileTests.swift:189-194,476-481). No cross-platform gateway open. This is why #1 shipped green and will recur.
- [HIGH][BROKEN] iOS has no gateway-attachment reader; Python seals attachments nothing opens | adapter.py:545-580 vs HermesRelayCrypto.swift (no attachment AAD) | **ADDED.** grep `gatewayAttachment*` across OpenBurnBarCore/OpenBurnBarMobile → 0 hits. Gateway agent→phone attachments are unreadable on iOS independent of #1; plus contract-vs-Python manifest-label drift (`gatewayAttachmentBody` per CONTRACT vs `gatewayAttachmentManifest` in adapter.py:222,568).
- [MEDIUM][RISK] README "5-line reuse" teaches un-persisted key + the wire-incompatible single-AAD convention | plugins/platforms/burnbar/README.md:56-59 | **REVISED** (HIGH→MEDIUM: docs, not shipped runtime). Bare `load_or_create()` rotates key across restarts (relay_e2ee.py:288-307); single `message_aad` for seal+wrap is the convention that breaks against canonical Swift (#1).
- [LOW][WORKS] Reusable core has residual BurnBar coupling in defaults | gateway/crypto/relay_e2ee.py:63 `RELAY_PRIVATE_KEY_ENV="BURNBAR_RELAY_PRIVATE_KEY"` | **CONFIRMED.** Overridable via `env_var=`; cosmetic genericity leak, not blocking.
- [LOW][RISK] MITM-rejection invariant lives only in scattered code comments, no single ROTATION design note | callables/hermesGateway.ts:735-742, HermesGatewayRelayKeypair clearPin seam, adapter.py:_pin_peer_public_key | **CONFIRMED.** Rotation seams exist and pin-only is sound, but the "changed pinned key MUST be rejected, not adopted" invariant + its 3-4 enforcement points aren't documented anywhere a future engineer will find them; low risk of one impl reintroducing the swap.
- [MEDIUM][WORKS] "is this doc sealed?" predicate re-derived in ~4 spots with differing conditions | hermesGateway.ts:687,520,654,708-717 | **CONFIRMED** (today's logic is consistent). Seam-rot risk only: a future field added to one predicate but not another could leak; a single `isSealedGatewayDoc()` would harden it.
- [n/a][WORKS] Server shape-only validation, keyVersion clamp, fail-closed writes, agent-key immutability, strip-siblings | hermesGateway.ts:86-88,479-482; callables/hermesGateway.ts:217,733-742 | **CONFIRMED WORKS.** Tried and could not break; but all are "correct logic over the dead channel" #1 produces.

████ result 14 ████
## VERDICTS

- [BLOCKER][CONFIRMED — BROKEN] Gateway key-wrap AAD diverges across languages; E2E channel non-functional in both directions | `OpenBurnBarMobile/Services/FunctionsRepository.swift:1480,1485,620,625` vs `~/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:532,535,671-673` | Independently reproduced. iOS event seal wraps the per-event key under `gatewayEventKeyAAD` ("…|gatewayEventKey|…", FunctionsRepository.swift:1485) but seals the payload under `gatewayEventAAD` ("…|gatewayEvent|…", :1480). Python `_open_envelope` uses ONE aad for BOTH the unwrap and the payload-open (`aad = aad_builder(...)`, adapter.py:671-673), and for events `aad_builder = _gateway_event_aad` → "…|gatewayEvent|…" (:196-197). The wrap label `gatewayEventKey` ≠ unwrap label `gatewayEvent`, so `unwrap_symmetric_key` (adapter.py:672) raises `InvalidTag` → caught at `_handle_burnbar_event` `except Exception` (adapter.py:992-994) → event silently dropped. Symmetric break agent→phone: Python `seal_message` wraps the key under `_gateway_message_aad` ("…|gatewayMessage|…", adapter.py:535) but iOS `decodedText` unwraps under `gatewayMessageKeyAAD` ("…|gatewayMessageKey|…", FunctionsRepository.swift:620) → throw → `resolvedText = nil` (:628-629). Verified the AAD byte-construction is byte-identical except the label (adapter.py:180-193 vs HermesRelayCrypto.swift:209-210), and the AAD is fed into BOTH the HKDF `sharedInfo` and the GCM tag (HermesRelayCrypto.swift:174,177), so the mismatch is doubly fatal. Server is pure passthrough/shape-validate (hermesGateway.ts:455 "The server never decrypts"), and iOS is the only phone counterparty (Android has no gateway crypto). Every real phone↔agent message fails; the shipped feature does not work.

- [BLOCKER][CONFIRMED — BROKEN] No cross-language interop vector covers ANY gateway AAD label | `~/.hermes/hermes-agent/tests/gateway/fixtures/HermesRelayWireVector.json`; `tests/gateway/test_relay_e2ee.py:84-153` | Reproduced: the vendored vector keys are `requestAAD`/`keyAAD`/`chunkAAD` only (zero `gateway*` material), and `test_relay_e2ee.py` opens Swift ciphertext exclusively under `_request_aad`/`_key_aad`/`_chunk_aad` (the pre-existing realtime-relay path). Every gateway test round-trips one language against itself — Python `test_phone_sealed_event_is_opened_by_agent` builds the ciphertext IN PYTHON with a single `_gateway_event_aad` for wrap+payload (test_burnbar_plugin.py:467-471, its "mirrors iOS" comment is false), and iOS `testHermesGatewaySealedReplyRoundTrips…` seals+opens Swift↔Swift (OpenBurnBarMobileTests.swift:186-216). The structural blind spot is exactly why the BLOCKER above shipped green. (Ironic: `test_unwrap_with_wrong_aad_raises_invalid_tag` at test_relay_e2ee.py:215 already proves a label mismatch → InvalidTag — it just never runs against gateway labels.) A real Swift-generated gateway vector opened by Python (and vice-versa) is the missing launch gate.

- [HIGH][CONFIRMED — BROKEN] model_switch AAD label mismatch iOS↔Python (second, independent divergence) | `OpenBurnBarMobile/Services/FunctionsRepository.swift:1472-1474,1480,1485` vs `adapter.py:200,640,648` | Reproduced. iOS has NO model-switch AAD at all (only `gatewayEvent*`/`gatewayMessage*` exist in HermesRelayCrypto.swift:111-129); it carries `modelId` inside the sealed EVENT payload (FunctionsRepository.swift:1472-1474) under the event labels. The iOS test confirms this — it opens with `gatewayEventKeyAAD`/`gatewayEventAAD` (OpenBurnBarMobileTests.swift:476,481). Python `open_model_switch` opens under a distinct `_gateway_model_switch_aad` ("…|gatewayModelSwitch|…", adapter.py:200,640). Even after fixing the event-wrap bug, a real iOS model_switch fails the Python tag. The sealed `gatewayModelSwitch` label was a Python-only invention during the fix (CONTRACT line "model_switch keeps cleartext modelId" was overridden one-sidedly) and never mirrored to iOS. Note: iOS also seals model_switch as kind `model_switch` with `modelId` cleartext-stripped, so even the legacy plaintext fallback (`raw.get("modelId")`, adapter.py:979) is empty → on an E2E link the agent gets nothing.

- [HIGH][CONFIRMED — INCOMPLETE] iOS has no gateway-attachment open path; Python attachment-AAD seal has no counterparty | `OpenBurnBarCore/.../HermesRelayCrypto.swift` (no `gatewayAttachment*` AADs); `adapter.py:545-580` | Reproduced. iOS production has exactly 4 gateway AAD call sites (`gatewayEvent*`/`gatewayMessage*`); `gatewayAttachmentManifest/Body/Key` are not defined in Swift and there is no FunctionsRepository attachment-decode path. The `MediaAttachmentManifestStore.swift` hit is the unrelated Mercury/iroh tail (collection `media_attachment_manifests`, sealed via `CloudVaultCrypto`, not the gateway envelope). Python `seal_attachment` (adapter.py:545-580) seals manifest+body under distinct labels and `test_seal_attachment_round_trip` rigorously proves swap-resistance — but the only counterparty (iOS) cannot open the result. Sealed gateway attachments are undecryptable on-device.

- [MEDIUM][CONFIRMED — INCOMPLETE] Replay test exercises plaintext not the named sealed redelivery; no restart-replay test | `tests/gateway/test_burnbar_plugin.py:854-872`; `adapter.py:719,955-961` | Reproduced. `test_duplicate_event_id_is_dropped` uses a `_legacy_adapter` plaintext event (no envelope, test line 856,867). The dedup at adapter.py:959 runs before opening, so the in-process dedup IS exercised and works — but the named finding ("relay redelivers a valid SEALED event") is not literally reproduced. `_seen_event_ids` is an in-process bounded `OrderedDict` (adapter.py:719), never persisted; only the cursor persists (adapter.py:953). REVISION to the report: the cursor is monotonic (`if next_cursor > self._cursor`, :951) and persisted, which partially mitigates naive restart-replay — BUT the cursor/sequence is relay-controlled (`payload.get("nextCursor")`, :950), which is exactly the adversary in the E2E threat model, so a malicious relay can serve an already-processed event id at a sequence past the resumed cursor after restart and re-trigger it (empty in-process dedup). Restart-replay defense is genuinely unproven and weak. INCOMPLETE confirmed.

- [LOW][CONFIRMED — WORKS] Grace-window downgrade does not exist; non-existence unasserted | `functions/src/hermesGateway.ts:86-88` | Reproduced: `gatewayPlaintextWriteAllowed(_relayCapable, _now) => false`, both args unused, hard-false. Security-positive — no plaintext writes ever. The CONTRACT's `relayCapable`-gated grace window is absent. REVISION: I downgrade the report's emphasis — this is correct, safe behavior, not a gap. A regression-pinning test (assert always-false) is reasonable polish, not a defect. WORKS.

- [LOW][CONFIRMED — INCOMPLETE] iOS pin-store `unknownKeychainError` fail-closed branch untested | `OpenBurnBarMobile/Services/HermesGatewayRelayKeypair.swift:136,142`; `OpenBurnBarMobileTests.swift:329` | Reproduced. `unknownKeychainError`→`allowsSeal == false` (logic correct, :142), but `freshPinStore()` uses the real Keychain and no test injects a read failure, so the security-critical "can't verify pin → refuse to seal" path never runs in CI. The `.mismatch` path is well-covered. Logic is sound; only the I/O-failure branch is a coverage blind spot. INCOMPLETE (minor).

- [LOW][CONFIRMED — WORKS/RISK] No concurrent-pairing test for server TOFU pin | `functions/src/callables/hermesGateway.ts:725-776` | Reproduced: the pin is a read (`grant.client.agentRelayPublicKey`, :726) then non-transactional `.set(merge:true)` (:756) — no `runTransaction` guard at the pin site (transactions exist elsewhere, e.g. :1568,:1685, just not here). Once a key is pinned, a differing key is correctly dropped (:736-743) — the immutability fix WORKS. The residual gap is only the first-pairing race window (two simultaneous first `/runtime` writes). Real but low-likelihood; worth a concurrency test. RISK (low) on the race; WORKS on the steady-state immutability.

ADDED (material, on the same test-quality surface, missed by the report):

- [MEDIUM][ADDED — RISK] The one existing test that would catch the BLOCKER is mislabeled and gives false assurance | `tests/gateway/test_burnbar_plugin.py:465 ("mirrors iOS HermesService")` and `test_relay_e2ee.py:215` | The Python event test's comment claims it "mirrors iOS" but it wraps the key under `_gateway_event_aad`, not the `gatewayEventKey` label iOS actually uses — so a reader/reviewer trusting the comment concludes interop is covered when it is not. Combined with `test_unwrap_with_wrong_aad_raises_invalid_tag` (test_relay_e2ee.py:215) proving label mismatch → InvalidTag, the codebase contains both the proof-of-mechanism and a false "mirror" assurance, which is precisely how the BLOCKER evaded review. The fix gate must be a Swift-emitted gateway wire vector (all 6+ labels: gatewayEvent, gatewayEventKey, gatewayMessage, gatewayMessageKey, gatewayModelSwitch, gatewayAttachment{Manifest,Body,Key}) opened by Python and the reverse — not another same-language round-trip.

Net: the report's two BLOCKERs and the model_switch + attachment HIGHs are independently confirmed and exploitable — the entire E2E gateway feature is non-functional on real cross-language traffic (fail-closed, so safe but dead). The MEDIUM/LOW items hold with the noted revisions (grace-window downgraded to WORKS; restart-replay upgraded with a concrete relay-controlled-cursor attack; immutability confirmed WORKS with only a first-pairing race residual). No alarm in the report was unsupported; one material item (mislabeled "mirror" test masking the BLOCKER) is added.

████ result 15 ████
## VERDICTS

- [BLOCKER][BROKEN] **CONFIRMED** — Key-wrap AAD divergence (event/message/model_switch) | `OpenBurnBarMobile/Services/FunctionsRepository.swift:1485,620` vs `/Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:670-673,535` | iOS wraps the per-event/message key under `gatewayEventKey`/`gatewayMessageKey`; Python reuses the payload AAD (`gatewayEvent`/`gatewayMessage`) for the wrap (no `_gateway_*_key_aad` exists). The wrap AAD feeds HKDF `sharedInfo` + GCM AAD → InvalidTag on every inbound event and every outbound reply → E2E messaging 100% non-functional. model_switch is *more* broken (iOS never emits the `gatewayModelSwitch` label Python expects; Python's seal/open_model_switch is dead code). No test round-trips a Swift-sealed event/message through the adapter, so the suite stays green.
- [HIGH][BROKEN] **CONFIRMED** — Replay dedup bypass via stripped top-level `id` | `/Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:845,958,670` | Relay blanks `raw.id` and sets `relayEnvelope.eventId` to the original; `_event_already_seen("")` returns False while AAD resolves from `envelope.eventId` → captured sealed command re-executes on every poll.
- [HIGH][RISK] **CONFIRMED** — No persistent replay store / no freshness window; cursor is relay-controlled | `adapter.py:62,719,854,950,697` | In-RAM LRU(4096) is the only defense, emptied on every restart; relay controls both the events and `nextCursor`, so post-restart it replays all captured events; 4096 distinct old-but-valid events flush a target id to reopen the window without a restart. No timestamp/TTL anywhere.
- [HIGH][RISK] **CONFIRMED** — Agent relay private key not 0600 in containers | `/Users/albertonunez/.hermes/hermes-agent/hermes_cli/config.py:562-563,5184,5194-5205` | `_secure_file` no-ops under `_is_container()`/managed; `save_env_value` restores the prior (possibly 0644) mode before the skipped chmod → `BURNBAR_RELAY_PRIVATE_KEY` co-tenant-readable in the common Docker deploy; fix #4's 0600 claim is false there (holds only outside containers).
- [MEDIUM][INCOMPLETE] **CONFIRMED** — Persist failure silently keeps an ephemeral key → restart rotation wedges the pinned link | `/Users/albertonunez/.hermes/hermes-agent/gateway/crypto/relay_e2ee.py:327-337`; `adapter.py:823` ; `FunctionsRepository.swift:1459-1463` | Unwritable `.env` → debug-swallowed, new key next restart, phone refuses the changed pinned key, link dead with no surfaced error; under an E2E mandate this should fail loudly.
- [HIGH][INCOMPLETE] **ADDED (revises report's #6 "WORKS")** — Gateway sealed attachments have no iOS reader | `adapter.py:545-580,571` ; (no matches) `OpenBurnBarCore/`,`OpenBurnBarMobile/` for `gatewayAttachment*`; `OpenBurnBarMobile/Views/Media/AttachmentBubble.swift:16` | Python only seals; no Swift attachment AAD or open path exists (the bubble consumes the legacy realtime-relay manifest). The distinct-label/cross-slot-swap property is real and self-consistent (that part stands), but the flow is unwired end-to-end — `test_burnbar_plugin.py:536` "the phone opens both" is a Python-to-Python self-test (`:537,573`), not interop.
- [LOW][WORKS] **CONFIRMED** — Crypto primitives byte-exact vs Swift | `/Users/albertonunez/.hermes/hermes-agent/gateway/crypto/relay_e2ee.py:355,423,475,222,430` ; `tests/gateway/test_relay_e2ee.py:117-150` | Fresh 12B nonces, on-curve point validation, raw-scalar import, ECDH X-coord, salt-empty≡32-zero proven by opening Swift ciphertext with the generic `request`/`key`/`chunk` AADs (which is exactly why those tests pass while the gateway `…Key` path is uncovered). Could not break the primitive layer.

**Bottom line:** The report's headline BLOCKER is real and I reproduced it byte-for-byte — E2E gateway messaging cannot function in either direction as shipped. All three HIGH items reproduce as concrete attacks against the stated untrusted-relay threat model. I corrected the model_switch sub-claim (worse than described — iOS emits no `gatewayModelSwitch` label) and revised the attachment "WORKS" to INCOMPLETE (no phone-side reader; the test is a same-process self-test, not interop). This would not survive a staff-engineer security review: a single missing pair of key-wrap AAD builders means the feature has never decrypted a single real message, and no test exercises the live wire path.

████ result 16 ████
## VERDICTS
- [HIGH][BROKEN] CONFIRMED — Unsubscribe leaves a ghost cloud subscription when the vault key is unavailable | OpenBurnBarMobile/Views/Hermes/Square/AgentBrandZoneView.swift:1139-1148 (+ android AgentSubscriptionTopicStore.kt:237-239) | `if let vaultKey { delete(opaqueDocID) }` skips the opaque delete on nil/`try?`-swallowed-throw, drops the local row, clears `lastError`, and the caller returns "Unsubscribed" (line 502); the sealed doc resurrects on the next snapshot. Trigger CORRECTED: cross-device unsubscribe before vault-key wrapper sync, or `vaultKeyMismatch` swallowed by `try?` — NOT a same-device "airplane blip" (local Keychain read needs no network, MobileCloudVaultKeyAccess.swift:59-61).
- [HIGH][RISK] CONFIRMED — Re-ingest double-counts v0+v1 against the tier cap until the daily purge, blocking the migration | functions/src/callables/knowledgeMemory.ts:300,328,356 | v0 id=`sha256Hex(plaintext)` vs v1 id=`HMAC(vaultKey,plaintext)` (PensieveKnowledgeChunker.swift:196,230) coexist; commit never deletes the prior row and the cap aggregate (300) has no version filter, so a near-cap user trips `failed-precondition` until `purgeLegacyKnowledgeVectorsScheduled` runs (≤24h, longer past the 500-user/run page).
- [MEDIUM][RISK] CONFIRMED — Phone reinstall orphans the old client; agent keeps sealing replies to the lost phone key | functions/src/callables/hermesGateway.ts:1227,1742 | Re-pair mints a new clientId for the phone but no reaper revokes the old client; the still-running agent's token resolves to the old clientId and `_absorb_relay_state` never learns the phone's new one (adapter.py:876-878), so replies seal to a dead key. (Report had this as the same severity; severity is right, mechanism confirmed.)
- [MEDIUM][RISK] CONFIRMED — Replay cache is process-local; a malicious relay redelivers a verbatim event after restart | plugins/platforms/burnbar/adapter.py:719,853,949 | `_seen_event_ids` empties on restart; only the monotonic persisted `_cursor` survives, so a compromised relay re-serving an event at the cursor boundary re-triggers the agent action (AAD prevents forgery, not verbatim redelivery). Bounded — needs malicious relay.
- [MEDIUM][INCOMPLETE] CONFIRMED (severity downgraded from the report's implicit MEDIUM) — Cap check is TOCTOU under concurrent commits | functions/src/callables/knowledgeMemory.ts:300,390 | Two concurrent `commitKnowledgeBatch` calls both read the pre-write aggregate and both pass the cap, then both commit non-transactionally; caps are soft. Self-DoS only.
- [MEDIUM][BROKEN] ADDED (report missed this; it declared Android/iOS rollback parity "holds") — iOS rollback watcher omits the legacy `inFlight` status that Android includes | OpenBurnBarMobile/Services/RollbackService.swift:87 vs android/.../missions/RollbackService.kt:174 | iOS `whereField("status", in: ["pending", "in_flight"])` filters out any `"inFlight"` row BEFORE the tolerant `Status(wireValue:)` decode (line 183) ever runs; Android's `whereIn(..., "inFlight")` surfaces it. A migration-window in-flight rollback written with the legacy token is invisible on iOS only — the tolerant decode is defeated by the upstream query filter.
- [LOW][RISK] REVISED (report said RISK; downgraded) — Cross-platform `consentGivenAt` type divergence mis-orders the iOS list only | OpenBurnBarMobile/Views/Hermes/Square/AgentBrandZoneView.swift:1252 vs android AgentSubscriptionTopicStore.kt:429 | iOS writes Timestamp, Android writes Long; Firestore ranks number < timestamp so the server order interleaves wrong. Android re-sorts client-side (kt:196) and iOS `decodeDate` parses both (swift:1385), so the bug is cosmetic and iOS-only (its listener at 1204-1207 trusts server order, never re-sorts). No data loss.
- [—][WORKS] CONFIRMED — Server/phone/adapter key pinning (TOFU, reject post-pair swaps) | functions/src/callables/hermesGateway.ts:733-742; plugins/platforms/burnbar/adapter.py:823-830 | I attempted bearer-token key-swap, post-pair rotation, and empty-key sealing; all fail closed with audit logs.
- [—][WORKS] CONFIRMED — Dedup idempotent-skip, search floor, and purge predicates | functions/src/callables/knowledgeMemory.ts:323,536-541; knowledgeSearch.ts:102-103 | v0 is never served (floor `dedupHashVersion==1`); purge `embeddingModelVersion=="bge-small-en-v1.5"` is exact-match and cannot hit the v1 tag `"bge-small-en-v1.5-vault-dedup-v1"`.
- [—][WORKS] CONFIRMED — Rollback tolerant DECODE + rules allowlist | OpenBurnBarCore/.../RollbackContracts.swift:118,135; firestore.rules:1626 | Both decode paths map legacy `"inFlight"`; `cancelled` is in the rules allowlist. (The decode is correct; the iOS WATCHER QUERY above is the defect — they are distinct layers.)

████ result 17 ████
## VERDICTS
- [BLOCKER][CONFIRMED] Legacy gateway plaintext never auto-scrubbed | functions/src/callables/privacyBackfill.ts:216-224 + functions/src/callables/hermesGateway.ts:1342 + scripts/privacy/scrub-chat-cloud-plaintext.mjs:230 | Reproduced full chain: original gateway wrote plaintext (git 3013f84e9:hermesGateway.ts:340,671), serializer still serves it (hermesGateway.ts:710,723), collections are admin/owner-readable (rules:2242), scheduled backfill is a structural no-op (gate on absent `relayEnvelope`), only an unscheduled manual script deletes it — stored AND served server-readable until an operator runs `--all-users --apply`. (Caveat: only bites if pre-E2EE plaintext docs exist in the live DB; migration is structurally incomplete regardless.)
- [HIGH][CONFIRMED] Replay cache reset-on-restart + flood-eviction, undocumented | /Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:719,853,959 | In-memory 4096 OrderedDict (pin persisted, seen-set not) → restart wipes it; `_event_already_seen` records any id before seal validation, so a relay floods 4096 junk ids to evict a captured id then replays the real sealed event → re-execution. Replay anchored to volatile set, not the persisted cursor.
- [HIGH][REVISED] E2EE trusts the server at pairing; no out-of-band key verification | OpenBurnBarMobile/Services/HermesGatewayRelayKeypair.swift:108-112 + plugins/platforms/burnbar/adapter.py:1538-1548 | (Report MISSED.) Both sides pin a peer pubkey the untrusted server delivers at pairing; a server malicious at first-pairing supplies its own key to both and MITMs every message — no safety-number/fingerprint UI exists (only TOFU `verifyOrPin`). Documented+accepted (rotation deferred), but it is the conditional that makes the E2EE claim depend on trusting the server during the handshake.
- [MEDIUM][CONFIRMED] Pensieve recall lost on deploy for non-daemon members | AgentLens/Services/CloudSync/KnowledgeSyncService.swift:168 (sole caller PensieveKnowledgeWatcher) + OpenBurnBarMobile/Models/DataVaultStore.swift:185 | Search floors v1 + 24h purge stops serving v0; only the Mac daemon's 15-min full-walk re-ingests under the new tag (eligibleFiles:303). iOS `syncKnowledgeNow` is manual-tap-only, Android/`chat_memory` have no trigger → those members' recall silently empties until re-fed.
- [MEDIUM][REVISED] Hosted-MCP knowledge search lacks the dedup-v0 floor — and reaches the live oracle | services/hosted-mcp/src/knowledge.ts:174 + :248 | (Report under-weighted.) No `dedupHashVersion==1` floor (only optional `embeddingModelVersion`), so MCP surfaces v0 rows whose `readKnowledgeDocument` returns `dedupHash` = the cleartext SHA-256 "confirm-the-guess" oracle (knowledgeMemory.ts:86) — directly contradicting docs/PENSIEVE.md:144 "the oracle survives in no served or queryable row" for ≤24h until purge. Real oracle exposure, not just an inconsistency.
- [LOW][CONFIRMED] iOS rollback observer drops legacy "inFlight" rows | OpenBurnBarMobile/Services/RollbackService.swift:86 vs android/.../missions/RollbackService.kt:174 | iOS query `in:["pending","in_flight"]` excludes legacy `"inFlight"`; Android includes it. Query filters before decode, so the tolerant decoder (RollbackContracts.swift:118,135) never runs; rule (firestore.rules:1626) also omits the legacy value. Legacy in-flight row visible on Android, invisible to iOS observer.
- [LOW][CONFIRMED] purgeLegacyKnowledgeVectors registration/scheduling/predicate-safety | functions/src/callables/knowledgeMemory.ts:551,567 + functions/src/index.ts:96 | Owner callable + daily onSchedule both registered; exact `"bge-small-en-v1.5"` equality can't match `-vault-dedup-v1`; single-field purge needs no composite index. Tried to force an over-delete and failed.
- [LOW][CONFIRMED] media-filename + subscription-cloak tails complete end-to-end | OpenBurnBarMobile/Features/Mercury/Stores/MediaAttachmentManifestStore.swift:55,75-92 + android/.../square/AgentSubscriptionTopicStore.kt:426,437 | Live writers seal + strip/delete cleartext + delete legacy doc ids; rules hard-drop the plaintext branches (firestore.rules:2787); Android byte-for-byte HMAC doc-id parity with iOS. Tried to find a half-wired writer/missing rule and failed.
- [NIT][CONFIRMED] dataExport doesn't leak legacy gateway plaintext (but confirms it persists) | functions/src/callables/dataExport.ts:327,354,397 | Gateway content collections forced through `sealAwareSerializeDoc`; `isExportablePrimitive` rejects strings → a legacy plaintext `text` is dropped into `redactedFields[]`, never exported. Confirms export drops ≠ deletes; the plaintext physically remains in Firestore (supports the BLOCKER).
- [NIT][ADDED] `_relay_e2e_enabled` is downgrade-safe (latch-on only) | /Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:879-882,502 | Checked the obvious downgrade: a malicious relay cannot flip E2E off — the flag only latches on from absorbed state and `must_seal` is the env flag alone. Correctly hardened; recorded because it's the first attack one would try.

████ result 18 ████
## VERDICTS

- **[BLOCKER][BROKEN] Event key-wrap AAD mismatch — every phone→agent E2E event is undecryptable** | `OpenBurnBarMobile/Services/FunctionsRepository.swift:1485` vs `/Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:608-616` | iOS wraps with `gatewayEventKey`, adapter unwraps with `gatewayEvent`; `aad` feeds both HKDF info and GCM tag → `InvalidTag` → "failed to open sealed event" (adapter.py:993). The entire inbound E2E channel is dead — phone cannot deliver a single message.
- **[BLOCKER][BROKEN] Message key-wrap AAD mismatch — every agent→phone E2E reply is undecryptable** | `/Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:534-535` vs `OpenBurnBarMobile/Services/FunctionsRepository.swift:620` | adapter wraps `gatewayMessage`, phone unwraps `gatewayMessageKey` → `resolvedText==nil` → blank reply rendered as "sealed for another device" (FunctionsRepository.swift:628-629). E2E broken in both directions.
- **[BLOCKER][BROKEN] model_switch never applies over E2E (AAD + payload-shape mismatch)** | `OpenBurnBarMobile/Services/FunctionsRepository.swift:1374-1387` vs `/Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:618-640` | iOS seals via the event path (`gatewayEvent`/`gatewayEventKey`, `{text,senderDisplayName,threadId,modelId}`); adapter opens via `gatewayModelSwitch` for wrap+payload and expects `{modelId}` only → wrap label, payload label, and JSON shape all diverge → control event never opens; phone-driven model switching is non-functional over E2E.
- **[BLOCKER][BROKEN] The open E2EE PR ships the broken adapter (fix never landed on the Python side)** | `/Users/albertonunez/.hermes/hermes-agent` branch `ajnunezg/burnbar-gateway-e2ee` (and HEAD `ajnunezg/burnbar-platform`) | `git show ajnunezg/burnbar-gateway-e2ee:plugins/platforms/burnbar/adapter.py | grep -c gatewayEventKey` = **0**; both branches still call `_gateway_message_aad` for both wrap and seal (`:534-535`). The "distinct attachment AAD" fix (#6) landed for attachments only; events/messages/model_switch were never given `...Key` builders. Merging the PR ships a feature that cannot pass a single real round-trip.
- **[HIGH][RISK] Residual MITM: agent TOFU-pins the peer key from an UNTRUSTED `/destinations`+`/events` response, and can durably persist the attacker's key** | `/Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:893-895, 858-882, 808-822` | `connect()` calls `_absorb_relay_state` on the raw server `GET /destinations` response before any authenticated cross-check; if `_peer_public_key` is None (env lost / never written) the server's `phoneRelayPublicKey` is pinned via TOFU (`:812-818`) and saved to `~/.hermes/.env`. Worse, `_absorb_relay_state` can flip `_relay_e2e_enabled` False→True purely from a server-asserted `relayCapable`/`e2eEnabled` flag (`:879-882`) with no key yet pinned. A server in the path seals the agent's replies to ITS key, decrypts, re-seals to the real phone → full MITM, now durable. The pin only defends post-pairing swaps, not malicious first-contact.
- **[HIGH][RISK] Both endpoints TOFU-pin with NO out-of-band key verification (no SAS/fingerprint)** | `OpenBurnBarMobile/Services/HermesGatewayRelayKeypair.swift:161-179` (phone) + `adapter.py:794-822` (agent) + `functions/src/callables/hermesGateway.ts:1207-1280` (pairing) | The user code authenticates the pairing *session* to the Firebase account, but neither side ever verifies the *relay public keys*. The phone's `verifyOrPin` pins the first server-relayed `agentRelayPublicKey` it sees (`:171-176`); the agent pins the first server-relayed `phoneRelayPublicKey` (`:808-822`). A server that substitutes both keys at pairing is invisible. `grep` for `fingerprint`/`safetyNumber`/`compareKey` returns nothing. This is the architectural ceiling: the channel is only as honest as the server during first pairing.
- **[HIGH][BROKEN] Agent→phone sealed attachments are write-only-dead: iOS has no gateway-attachment download/open path** | `OpenBurnBarMobile/` (zero matches for any gateway-attachment fetch/unwrap) + `/Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:545-580` | The adapter seals attachment bodies + manifest (distinct AADs, correct), but the phone has no code to fetch the body bytes or call `unwrapSymmetricKey` with `gatewayAttachment*` AAD (which iOS doesn't even define). Even ignoring the AAD bug, agent-sent files never render on the phone. Feature half-built.
- **[HIGH][RISK] User-facing "E2EE ready" / "messages should get picked up immediately" copy is unbacked, and total crypto failure is misreported as a benign "wrong device" state** | `OpenBurnBarMobile/Services/FunctionsRepository.swift:121-123` + `OpenBurnBarMobile/Views/Hermes/HermesSettingsView.swift:1083, 1886, 549-550` | `canSealToAgent` only checks `relayEncryption==algorithm && relayPublicKey nonempty` — never a successful round-trip. The UI asserts "selected, online, and E2EE ready" while every reply fails to decrypt and renders "This reply is sealed for another device. Open BurnBar on the device that approved this gateway" — which will be shown to EVERY user for EVERY reply, hiding the BLOCKER behind plausible copy.
- **[MEDIUM][WORKS] Server-side privacy invariants (sealed-doc strip, plaintext-write gate, key-version clamp) hold** | `functions/src/hermesGateway.ts:86-88, 676-724`, `functions/src/callables/hermesGateway.ts:247-272` | `gatewayPlaintextWriteAllowed` is hard `false`; `serializeHermesGatewayEvent` unconditionally drops `text`/`senderDisplayName`/`threadId` for any `relayEnvelope || schemaVersion>=2` doc; `requireGatewayRelayEnvelope` clamps `relayKeyVersion` to exactly 1 on write AND read. I tried to surface a plaintext sibling or downgrade via the read fallback and could not — the legacy read fallback is unreachable because the write path rejects all plaintext. Agent-key immutability in `/runtime` is correct (`callables/hermesGateway.ts:733-743`).
- **[MEDIUM][WORKS] Cloud Storage side of attachments is owner-scoped / not server-readable via SDK** | `storage.rules:1-30` | Gateway attachment bodies are not in the `session_logs`/`avatars` paths; the catch-all `match /{allPaths=**} { allow read, write: if false }` (`:26-27`) denies all other Storage access, and bodies move through server-issued signed URLs only. No public/over-broad rule found. (Body bytes are ciphertext regardless.)
- **[LOW][RISK] Replay-cache empty-id is non-dedupable, but the AAD binds the id so a relay cannot replay under a blank id** | `/Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:845-846, 958` | `_event_already_seen` returns False for a falsy `event_id`; a malicious relay stripping `id` to blank would make `_open_envelope` build AAD with `event_id=""` → `InvalidTag` (the real frame's AAD bound the true id) → dropped before handling. Not exploitable, but the dedup gap is real if any future path accepts an unbound id.
- **[LOW][RISK] `_absorb_relay_state` lets the untrusted server freely rewrite routing `uid`/`clientId`** | `/Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py:873-878` | These feed the AAD (`uid|clientId|id`); a server flipping them mid-session shifts the AAD namespace. Bounded by the same pin/tag checks (a wrong namespace just fails the tag), so it's a robustness/auditability nit, not a leak.

- **[OVERALL] NOT launch-ready.** The headline feature does not function and the open PR ships it broken. Ranked residual risks: (1) **The E2E channel is 100% non-functional** in all three directions due to the adapter's single-AAD-label bug — phones cannot message agents and agents' replies render blank; the PR `ajnunezg/burnbar-gateway-e2ee` contains the broken code (zero `...Key` builders). This is a hard ship-blocker. (2) **Misleading UX** actively conceals the breakage by asserting "E2EE ready" and reframing every decrypt failure as "sealed for another device" — users will think it works and silently lose every message. (3) **MITM at first pairing**: both endpoints TOFU-pin server-relayed keys with no out-of-band fingerprint/SAS, and the agent can be coaxed into durably pinning the server's key (env-loss + server-asserted `relayCapable`), so even after the AAD fix the "server can't read it" claim is only true if the server is honest during pairing — exactly the threat model E2E exists to defeat. (4) Agent→phone attachments are a dead receive path on iOS. The server-side sealing, write-gates, key-version clamp, and Storage scoping are genuinely solid (I tried to break them and failed) — but a correct vault around a channel that never decrypts and pins attacker keys is not E2E. Minimum to ship: add `gatewayEventKey`/`gatewayMessageKey` builders to the adapter and route model_switch through the event path to match iOS; build the iOS attachment download/open path with matching `gatewayAttachment*` AADs; gate "E2EE ready" copy on a real round-trip and stop labeling decrypt failures as "wrong device"; add an out-of-band key fingerprint/SAS at pairing (or at minimum remove the server-flag E2E auto-enable + untrusted-state first-pin on the agent).