All five owned files are touched as planned. Let me do a final review of the AAD wrapper additions in HermesRelayCrypto to confirm the part labels exactly match what the Python adapter and TS server will use. Per CONTRACT line 29-30: event AAD parts `["gatewayEvent", uid, clientId, eventId]`, message AAD `["gatewayMessage", uid, clientId, messageId]`. My wrappers use exactly `["gatewayEvent", uid, clientId, eventId]` and `["gatewayMessage", uid, clientId, messageId]`. For the key-wrap AAD I used `["gatewayEventKey", ...]` / `["gatewayMessageKey", ...]`.

The CONTRACT specifies the payload AAD parts but for the key-wrap it references "keyAAD for wrap" generally. The existing realtime pattern uses `requestAAD` (payload) and `keyAAD` (wrap) with distinct first segments (`"request"` vs `"key"`). I mirrored that with `gatewayEvent`/`gatewayEventKey`. This is a detail the Python `relay_e2ee.py` and TS must match. The CONTRACT's `request_aad/key_aad/chunk_aad` Python API and "namespace param" suggests the key AAD is derived by a separate label. Let me flag this as a cross-stream interop point to confirm with F-crypto/F-adapter: the key-wrap AAD label must be `gatewayEventKey`/`gatewayMessageKey` to match. I'll note it explicitly in the changelog.

All implementation is complete and self-reviewed.

## CHANGELOG

**OpenBurnBarCore/Sources/OpenBurnBarCore/SharedModels/HermesRelayCrypto.swift** — Added four `public static` AAD wrappers on the `HermesRelayCrypto` enum (no crypto changed; they delegate to the existing private `aad([...])`, preserving the single `"OpenBurnBar-HermesRelay-v1|"` prefix + `keyWrapSharedInfo`):
- `gatewayEventAAD(uid:clientId:eventId:)` → parts `["gatewayEvent", uid, clientId, eventId]` (payload seal, phone→agent events)
- `gatewayEventKeyAAD(uid:clientId:eventId:)` → parts `["gatewayEventKey", uid, clientId, eventId]` (key wrap to agent)
- `gatewayMessageAAD(uid:clientId:messageId:)` → parts `["gatewayMessage", uid, clientId, messageId]` (payload open, agent→phone messages)
- `gatewayMessageKeyAAD(uid:clientId:messageId:)` → parts `["gatewayMessageKey", uid, clientId, messageId]` (key unwrap with phone key)

**OpenBurnBarMobile/Services/HermesGatewayRelayKeypair.swift** (NEW) — Keychain-backed persistent P-256 relay keypair for the phone, modeled on `iOSDeviceKeypair` (`kSecAttrAccessibleWhenUnlockedThisDeviceOnly`, service tag `com.openburnbar.mobile.hermes-gateway-relay`). New symbols: `struct HermesGatewayRelayKeypair { let privateKey: HermesRelayPrivateKey; var relayPublicKeyBase64: String (x963, 65B 0x04‖X‖Y); let keyVersion = HermesRelayCrypto.keyVersion; var relayEncryption: String; static func loadOrCreate() -> HermesGatewayRelayKeypair }` and `enum HermesGatewayRelayKeypairError`. No crypto implemented here — only key persistence; falls back to an in-memory key if Keychain persistence fails so a single send/open still proceeds.

**OpenBurnBarMobile/Services/FunctionsRepository.swift**:
- Added `@preconcurrency import FirebaseAuth` (uid for AAD).
- `HermesGatewayClientRecord`: added `relayPublicKey: String?`, `relayKeyVersion: Int?`, `relayEncryption: String?` (the **agent's** published key) across stored props, `CodingKeys`, `init(from:)`, memberwise init, and `init?(documentID:data:)`. Added computed `var canSealToAgent: Bool` (mirrors `HermesService` `canSealToHost`: `relayEncryption == HermesRelayCrypto.algorithm && relayPublicKey?.isEmpty == false`).
- `HermesGatewayMessageRecord`: added `payloadCiphertext`/`wrappedKey`/`relayEncryption`/`relayKeyVersion` decode + in-memory `var resolvedText: String?`; new `var isSealed`, `var displayText` (opened plaintext or legacy `text`), and `func decodedText(using keypair: HermesGatewayRelayKeypair, uid:) -> HermesGatewayMessageRecord` (unwraps key with phone relay key via `gatewayMessageKeyAAD`, opens via `gatewayMessageAAD`; LEGACY plaintext passes through; un-openable sealed doc returns `resolvedText == nil`).
- `HermesGatewayMessageResolver.newestThreadReply`: now selects `message.isSealed || message.displayText?.isEmpty == false || !attachmentIds.isEmpty` (was `text?.isEmpty == false || ...`) so sealed replies surface.
- Protocol `HermesGatewayRepository`: `approveHermesGatewayDeviceGrant` gained `relayPublicKey:relayKeyVersion:relayEncryption:`; `enqueueHermesGatewayEvent`/`enqueueHermesGatewayModelSwitch` gained `targetClient: HermesGatewayClientRecord?`. Convenience overloads updated: 2-arg `approve` now loads `HermesGatewayRelayKeypair.loadOrCreate()` and publishes the phone pubkey.
- Impl: `approveHermesGatewayDeviceGrant` sends `relayPublicKey`/`relayKeyVersion`/`relayEncryption` (non-empty). New private `applyGatewayEventSeal(into:text:senderDisplayName:threadId:modelId:targetClient:)` + `trimmedClientID`: when `canSealToAgent`, generates a per-event key, seals JSON `{text, senderDisplayName, threadId[, modelId]}` (AAD `gatewayEventAAD`), wraps the key to the agent pubkey (AAD `gatewayEventKeyAAD`), and writes top-level `eventId`/`payloadCiphertext`/`wrappedKey`/`relayEncryption`/`relayKeyVersion` while **dropping** plaintext `text`/`senderDisplayName`; model_switch keeps cleartext top-level `modelId`. LEGACY fallback (no agent pubkey / no uid) sends plaintext `text`/`senderDisplayName`. Client-generated `eventId` (`evt_<uuid>`) binds the AAD.

**OpenBurnBarMobile/Views/Hermes/HermesSettingsView.swift**:
- `sendGatewayMessage`/`switchGatewayModel`/`sendTest` now pass `targetClient: targetClient`.
- `handleMessagesSnapshot` opens each record via `record.decodedText(using: HermesGatewayRelayKeypair.loadOrCreate(), uid: listenedUID)` before resolving/rendering (runs synchronously in the existing `@MainActor` handler).
- `presentReplyNotification` uses `reply.displayText` (was `reply.text`).
- Both render paths (`gatewayReplyHero` body + `gatewayDeliveryStatusCard`) use new `gatewayReplyBodyText(_:fallback:)` which shows opened text, a graceful "sealed for another device" message for un-openable sealed replies, or the legacy fallback.

**OpenBurnBarMobileTests/OpenBurnBarMobileTests.swift** (tests I own):
- Updated `MockHermesGatewayRepository` to the new signatures; added `ApprovalGrant` capture (`approvalGrants`) and `sealedToAgent` on `EnqueuedEvent`/`EnqueuedModelSwitch`.
- Added tests: `testHermesGatewayMessageRecordParsesSealedReplyAndExposesNoPlaintext`, `testHermesGatewaySealedReplyRoundTripsThroughPhoneRelayKey` (seal-to-phone → `decodedText` recovers plaintext), `testHermesGatewaySealedReplyForAnotherDeviceStaysSealed` (wrong recipient → `resolvedText == nil`, `isSealed == true`), `testHermesGatewayClientRecordReadsAgentRelayPubkeyAndCanSeal`, `testHermesGatewayClientWithoutAgentPubkeyCannotSeal`, `testHermesGatewayRelayKeypairPublishesX963PubkeyAtPairing` (asserts published key decodes to 65 bytes, leading `0x04`, version + algorithm). Extended `testHermesGatewayMessageRecordParsesReplyPayload` with `isSealed`/`displayText` assertions.

**Deviations / decisions**:
- Threaded the agent pubkey via a new `targetClient: HermesGatewayClientRecord?` enqueue param (rather than raw pubkey strings) — minimal churn, carries `canSealToAgent`. The convenience overloads forward `targetClientId ?? targetClient?.id`.
- Per CONTRACT (authoritative over the slice brief), the sealed event payload is `{text, senderDisplayName, threadId}`; for model_switch I additionally include `modelId` inside the sealed payload **and** keep `modelId` cleartext top-level (CONTRACT: "model_switch keeps cleartext modelId"). The callable request still carries top-level `threadId` as server routing input; persisting it sealed-only is the server (B-fn-gw) stream's responsibility.
- Sealing is synchronous (CryptoKit), so `decodedText` is a non-async struct method called inline in the MainActor snapshot handler (the brief mentioned async; not needed).

**Cross-stream dependencies (flag for orchestrator / other streams)**:
1. **B-fn-gw (server):** `publicClientView`/`HermesGatewayClientDoc` must surface the **agent's** `relayPublicKey`/`relayKeyVersion`/`relayEncryption` on the client record returned to iOS — that is the key iOS reads via `HermesGatewayClientRecord.relayPublicKey` to seal events. The server must also accept this slice's new `enqueueHermesGatewayEvent` request fields (`eventId`, `payloadCiphertext`, `wrappedKey`, `relayEncryption`, `relayKeyVersion`), honor `eventId` as the doc id, reject plaintext `text`/`senderDisplayName` when sealed, and accept the phone's pubkey at `approveHermesGatewayDeviceGrant`.
2. **F-crypto / F-adapter:** the key-wrap AAD labels are `gatewayEventKey` / `gatewayMessageKey` (distinct from the payload labels `gatewayEvent` / `gatewayMessage`), mirroring the existing `request`/`key` split. The Python `relay_e2ee.py` key_aad/request_aad namespacing for the gateway must use these exact first segments or wrap/unwrap will fail with InvalidTag. Agent must seal `hermes_gateway_messages` `{text}` to the phone pubkey with `gatewayMessageAAD`/`gatewayMessageKeyAAD` for iOS `decodedText` to open it.

No blockers. Builds/tests not run per instructions (Swift-only stream; central verify follows).