Critical finding confirmed: the BurnBar-repo copy (`tools/hermes-platform-burnbar/adapter.py`, 891 lines) is significantly AHEAD of the fork plugin (702 lines working / 618 committed) — it has a whole human-in-the-loop oversight subsystem the fork doesn't. There is **no crypto/sealing in either copy yet** (no `seal`/`crypt`/`relay`/`payloadCiphertext` markers). This is the divergence PR1 must reconcile. I have the complete picture.

## DESIGN BRIEF — Slice: fork-platform-and-pr1

### A. Fork structure & repos (ground truth)
- Fork repo: `/Users/albertonunez/.hermes/hermes-agent`, remotes `origin`=NousResearch/hermes-agent, `ajnunezg`=Ajnunezg/hermes-agent. Working branch `ajnunezg/burnbar-platform` @ `7ac8ae023`.
- Worktrees: `/private/tmp/hermes-burnbar-pr-audit-20260601` is branch `ajnunezg/burnbar-platform-audit-20260601` @ `7402a68a6` — it is **the same BurnBar commit rebased one commit ahead onto upstream** (`7402a68a6 feat(platforms): add BurnBar gateway integration` on top of `b9646276f` upstream). It contains **no bespoke audit-notes file** — it's a clean rebase scratch checkout (only standard `RELEASE_*.md`/`SECURITY.md`/`.plans/*.md` ship there). `/private/tmp/hermes-origin-main-shutdown-check-20260601` is a detached upstream-main checkout. Neither holds findings to import.
- PR1 lands as a **plugin** (`plugins/platforms/burnbar/`) — the "Plugin Path (Recommended for Community/Third-Party)" in `gateway/platforms/ADDING_A_PLATFORM.md:5-58`, requiring **zero core edits**. The committed BurnBar commit already follows this correctly: it does NOT touch `gateway/config.py` Platform enum, `gateway/run.py`, `toolsets.py`, etc. (the entire "Built-in Path" §1-14 checklist is intentionally skipped — correct for a plugin). `Platform("burnbar")` resolves dynamically via the plugin scan (proven by `test_platform_enum_resolves_via_plugin_scan`).

### B. What the BurnBar platform addition currently IS (commit `7ac8ae023`, 7 files, +998)
- `plugins/platforms/burnbar/{__init__.py, adapter.py, plugin.yaml, README.md}` — full adapter: `BurnBarAdapter(BasePlatformAdapter)` with `connect/disconnect/_poll_loop/_handle_burnbar_event/send/_send_local_file/send_document/send_image_file/send_voice/send_video/send_typing/get_chat_info`; module-level `_standalone_send`, `_apply_yaml_config`, `_env_enablement`, `interactive_setup`, `register(ctx)`.
- `tests/gateway/test_burnbar_plugin.py` (162 lines, 9 tests via `load_plugin_adapter("burnbar")`).
- `tools/send_message_tool.py` (+25) + `tests/tools/test_send_message_tool.py` (+75) — teaches the generic media path to route to a plugin's `standalone_sender_fn` instead of dropping attachments.

### C. PR1 polish gaps to a clean upstream PR (concrete)

1. **CRITICAL — reconcile the three diverged adapter copies before submitting.** There are now three versions of the same file:
   - `7ac8ae023:plugins/platforms/burnbar/adapter.py` (committed, 618 lines)
   - fork **working tree** `plugins/platforms/burnbar/adapter.py` (702 lines, uncommitted `+runtime-status` diff)
   - **BurnBar-repo source-of-truth** `/Users/albertonunez/Documents/Windsurf/BurnBar/tools/hermes-platform-burnbar/adapter.py` (**891 lines — far ahead**, adds a human-in-the-loop **oversight subsystem**: `_refresh_oversight_mode`, `OVERSIGHT_REFRESH_SECONDS`, supervised/autonomous gating, `_resolve_oversight_gates`).
   Decide canonical direction (the BurnBar-repo 891-line copy is newest) and **fast-forward the fork plugin to match it**, then commit. PR1 must not ship a 200-line-stale adapter. Note: **neither copy has any sealing/crypto yet** (zero `seal|crypt|relay|payloadCiphertext|wrappedKey|ecdh|p256` markers) — confirming E2E sealing is genuinely PR2-new work, not partially present.

2. **Commit the two uncommitted working-tree changes or split them out.** `git status` shows uncommitted `M plugins/platforms/burnbar/adapter.py` (runtime-status publish) and `M gateway/platforms/api_server.py` (+160 lines: upstream `/v1/models` catalog proxy + `requested_model` plumbing + truncation finish-reason). **`api_server.py` is a core-file change unrelated to the BurnBar plugin** — it must NOT ride in PR1 (PR1 is plugin-only / zero-core-edit). Carve it into its own upstream PR or drop it from the BurnBar-platform branch. The runtime-status adapter diff belongs in PR1 (it's plugin-local).

3. **`plugin.yaml` conformance** (`plugins/platforms/burnbar/plugin.yaml`) is already correct: `kind: platform`, `requires_env`/`optional_env` rich dicts matching `register()`. Verify `name: burnbar-platform` vs `register(name="burnbar")` is intentional (yaml package name vs platform key — both other plugins use the same split; OK).

4. **`tests/gateway/test_burnbar_plugin.py` gaps to close for upstream:** add coverage the ADDING_A_PLATFORM §16 checklist names that's currently missing — (a) `send()`/`_post_message` happy path + error→`SendResult(success=False)` (mock httpx); (b) attachment `_init_attachment`/`_upload_attachment`/`_create_attachments` (mock signed-URL PUT); (c) cursor round-trip (`_read_cursor`/`_write_cursor`); (d) the new oversight + runtime-status helpers once copy #1 is reconciled; (e) standalone-send routing already partly in `tests/tools/test_send_message_tool.py` — keep. All tests must use `load_plugin_adapter("burnbar")` (the `tests/gateway/conftest.py` guard rejects ad-hoc `sys.path` adapter imports).

5. **Docs for upstream** (ADDING_A_PLATFORM §15 expects these for built-ins; for a plugin keep it light but add): a `website/docs/user-guide/messaging/burnbar.md` setup page is the idiomatic upstream artifact; the `README.md` in the plugin dir currently documents the **BurnBar-repo** smoke harness (`tools/hermes-platform-burnbar/smoke_local.py`) which **does not exist in the fork** — fix the README so an upstream reviewer can run it, or vendor a minimal smoke into the plugin dir.

6. **Lint/CI conformance (the fork's exact gates):**
   - **Ruff** (`pyproject.toml:255-274`): only rule `PLW1514` (unspecified-encoding) is blocking, `preview=true`. `plugins/**` is in `per-file-ignores` for PLW1514 (`:274`) — so the plugin is effectively exempt from the one blocking rule, but the **advisory diff job** (`.github/workflows/lint.yml`) still surfaces ruff+`ty` diagnostics vs base. Run `ruff check .` (blocking job `ruff-blocking`, `lint.yml:158-182`) and `ty` locally before PR.
   - **Tests** (`.github/workflows/tests.yml`): runs in **6 slices**, per-file in fresh subprocesses, **30s pytest-timeout per test** (`pyproject.toml:241-244`), `testpaths=["tests"]`. Async tests need `pytest-asyncio` (already dev-pinned `:86`); the existing test uses `@pytest.mark.asyncio` — keep that.
   - Strip the committed `__pycache__/*.pyc` under `plugins/platforms/burnbar/__pycache__/` from the PR (they're tracked-adjacent; ensure `.gitignore` excludes and they aren't staged).

### D. Where the reusable E2E crypto module should live (PR2 home — scope this now)

- **No `gateway/crypto/`, `gateway/_e2ee/`, or `gateway/platforms/_e2ee/` exists today.** Precedent placement: shared platform crypto lives **flat at `gateway/platforms/<name>_crypto.py`** (e.g. `gateway/platforms/wecom_crypto.py`, imported as `from gateway.platforms.wecom_crypto import WXBizMsgCrypt` — `wecom_callback.py:50`), and per-platform crypto lives **inside the platform package** (`gateway/platforms/qqbot/crypto.py`, imported `from .crypto import ...` — `qqbot/onboard.py:32`, re-exported in `qqbot/__init__.py:32`).
- **Recommendation: create a new shared package `gateway/crypto/` with `gateway/crypto/relay_e2ee.py`** (module-level, reusable by *any* platform adapter, mirroring the "shared, not platform-bound" intent). Rationale: the relay envelope (`p256-hkdf-sha256-aesgcm`) is **cross-platform infrastructure**, not BurnBar-specific, so it should NOT live under `plugins/platforms/burnbar/` (a plugin can't be a dependency of core or of other plugins) and should NOT be a flat `gateway/platforms/burnbar_crypto.py` (that name implies platform-coupling). A dedicated `gateway/crypto/` package signals reuse and gives PR2 room for the test module + fixtures.
  - Acceptable alternative if reviewers prefer minimal new dirs: flat **`gateway/platforms/relay_e2ee_crypto.py`** (directly analogous to `wecom_crypto.py`). Pick `gateway/crypto/relay_e2ee.py` for clarity; fall back to the flat name only if upstream resists a new top-level package.
- **Idiomatic module shape (follow qqbot/wecom precedent + the CONTRACT envelope):** pure functions + small dataclasses, `from __future__ import annotations`, deferred `from cryptography.hazmat...` imports (qqbot does this inside `decrypt_secret`), a typed error class hierarchy (wecom: `WeComCryptoError/SignatureError/DecryptError/EncryptError`). Public surface to implement for the `p256-hkdf-sha256-aesgcm` envelope from `recon-crypto-primitives.md §6`:
  - `seal_to_base64(plaintext: bytes, key: bytes, aad: bytes) -> str` (AES-256-GCM, 12B nonce, 16B tag, `.combined = nonce‖ct‖tag` base64)
  - `open_base64(b64: str, key: bytes, aad: bytes) -> bytes`
  - `wrap_symmetric_key(sym_key: bytes, recipient_pub_b64: str, aad: bytes) -> WrappedKey` (P-256 ECDH → HKDF-SHA256 salt=∅, `sharedInfo = "OpenBurnBar-HermesRelay-KeyWrap-v1|"+aad` → AES-GCM-wrap)
  - `unwrap_symmetric_key(wrapped, private_key, aad) -> bytes`
  - AAD builders matching Swift exactly: `request_aad/key_aad/chunk_aad` binding `uid|connectionID|requestID(|seq|kind)` (Swift `HermesRelayCrypto.swift:81-97`), constant `RELAY_ENCRYPTION = "p256-hkdf-sha256-aesgcm"`, `RELAY_KEY_VERSION = 1`. X9.63 uncompressed (65B, `0x04`) pubkeys.
- **Dependency note:** `cryptography==46.0.7` is importable in the fork (verified: `ec`, `HKDF`, `AESGCM` all present) and is already a transitive dep (`uv.lock:259,284`). PR2 should **add `cryptography` explicitly to a pyproject extra** (e.g. a new `e2ee = ["cryptography>=46"]` or fold into `messaging`) so the relay crypto isn't relying on a transitive pin — upstream-clean.
- **Test placement:** `tests/gateway/test_relay_e2ee.py` (sibling of `test_wecom_callback.py`), with a **byte-exact cross-language fixture** — a Swift-`HermesRelayCrypto`-sealed payload that Python `open_base64`/`unwrap_symmetric_key` must open (the precise analog of `apps/console/test/interop.test.ts`). This is the gate that proves Python joined the byte-exact envelope set (`recon-crypto-primitives.md` explicitly flags "Python is NOT yet in this set").

### E. PR split (your slice's recommendation)
- **PR1 = `plugins/platforms/burnbar/` (reconciled to the 891-line BurnBar-repo copy) + `tools/send_message_tool.py` media-routing + tests + docs.** Zero core edits. **Remove the `gateway/platforms/api_server.py` change from this branch** (separate concern, separate PR). This is purely the BurnBar platform addition; it carries **no crypto** and is shippable independently of E2E.
- **PR2 = new `gateway/crypto/relay_e2ee.py` + `tests/gateway/test_relay_e2ee.py` + pyproject `cryptography` extra + adapter wiring** (the adapter calls `seal_to_base64`/`wrap_symmetric_key` and writes `payloadCiphertext`/`wrappedKey`/`relayEncryption`/`relayKeyVersion`). PR2 depends on PR1 landing first (it edits the same `adapter.py`).

### F. Key file:line references
- Plugin contract: `gateway/platforms/ADDING_A_PLATFORM.md:5-58` (plugin path), `:342-358` (test checklist), `:330-339` (docs).
- Base API: `gateway/platforms/base.py` — `send` signature `:2001-2020`, `MessageEvent:1284`, `SendResult:1413`, `MessageType:1262`, `build_source:4250`, `handle_message:3466`, `_keep_typing:2711`.
- Registry surface: `gateway/platform_registry.py:94-159` (all `register_platform` kwargs incl. `standalone_sender_fn:159`, `allow_update_command:106`).
- Crypto precedents: `gateway/platforms/qqbot/crypto.py:1-46` (AES-GCM, deferred import, base64 `.combined`), `gateway/platforms/wecom_crypto.py:22-34` (error hierarchy).
- Lint/test config: `pyproject.toml:255-274` (ruff), `:235-244` (pytest, 30s timeout), `:86` (dev pins), `.github/workflows/lint.yml:158-182` (blocking ruff), `.github/workflows/tests.yml` (6-slice per-file).
- Test loader: `tests/gateway/_plugin_adapter_loader.py` (use `load_plugin_adapter("burnbar")`; conftest guard enforces it).
- Divergence to reconcile: fork `plugins/platforms/burnbar/adapter.py` (702 working / 618 committed) vs BurnBar-repo `tools/hermes-platform-burnbar/adapter.py` (891, +oversight). Stray core change to excise from PR1: `gateway/platforms/api_server.py` (uncommitted, +160).