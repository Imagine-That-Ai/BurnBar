Both owned files compile and all 24 tests pass. Final state delivered.

## CHANGELOG

### `/Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py` (rewritten: 702 → 1331 lines)
**PR1 reconcile (no crypto):** brought the fork plugin up to the canonical 891-line BurnBar-repo copy — ported the human-in-the-loop **oversight subsystem** (`_refresh_oversight_mode`, `OVERSIGHT_REFRESH_SECONDS=15.0`, `send_slash_confirm`, `_arm_approval`, `_resolve_pending_confirms`, `_resolve_slash_confirm`, `_post_confirm_followup`, `_oversight_mode`/`_oversight_checked_at`/`_pending_confirms`) and the **runtime-status** `_agent_version()` helper + `agentVersion` field. Preserved fork plugin registration/imports (`register`, `gateway.config`/`gateway.platforms.base`). Did NOT touch `gateway/platforms/api_server.py`.

**PR2 seal (E2E):** imports `from gateway.crypto import relay_e2ee` under a `RELAY_CRYPTO_AVAILABLE` guard (mirrors httpx guard). New symbols/fields:
- Module constants: `RELAY_ENCRYPTION` (`relay_e2ee.ALGORITHM`), `RELAY_KEY_VERSION`, `RELAY_PRIVATE_KEY_ENV="BURNBAR_RELAY_PRIVATE_KEY"`, `RELAY_E2E_ENV="BURNBAR_RELAY_E2E"`, `_RELAY_NAMESPACE`.
- Gateway AAD builders (locked wire prefix `"OpenBurnBar-HermesRelay-v1|"` via `RelayNamespace.aad`): `_gateway_aad`, `_gateway_event_aad`, `_gateway_message_aad`, `_gateway_attachment_body_aad`, `_gateway_attachment_key_aad`, plus `_public_key_base64` (tolerates `RelayPrivateKey` method vs `AgentRelayIdentity` property).
- `_RelaySealer` class (`can_seal`/`must_seal`, `seal_message`, `seal_attachment`, `open_event`) and `_RelayPlaintextRefused`. All AES/ECDH/HKDF delegated to `relay_e2ee` — no invented crypto.
- `BurnBarAdapter.__init__`: `_relay_identity`, `_peer_public_key`/`_peer_public_keys` (seeded from `BURNBAR_RELAY_PEER_PUBLIC_KEY`), `_relay_e2e_enabled`, `_relay_uid`/`_relay_client_id`, `_sealer`; helpers `_ensure_relay_identity`, `_relay_private_key`, `_relay_public_key_base64`, `_cache_peer_public_key`, `_absorb_relay_state`.
- `_post_message`/`_init_attachment`/`_upload_attachment`/`_create_attachments` gained an optional `sealer`/`relay_envelope`/`body_bytes` path — outgoing text → `relayEnvelope{payloadCiphertext,wrappedKey,relayEncryption,relayKeyVersion,messageId}` wrapped to phone pubkey (drops plaintext `text`); attachments seal bytes + manifest `{fileName,byteCount,contentType}` (drops plaintext `fileName`).
- `_handle_burnbar_event` opens sealed events (unwrap with agent key, open `payloadCiphertext`), caches phone pubkey from `senderPublicKey`, keeps `model_switch` cleartext, **drops** unsealed events when E2E-paired; **LEGACY plaintext fallback** retained when E2E not required.
- `interactive_setup` publishes `agentRelayPublicKey`/`relayKeyVersion`/`relayEncryption` at `device/start`, persists peer pubkey/uid/clientId + `BURNBAR_RELAY_E2E=1` from the grant.
- **Refuse-plaintext-when-paired:** `send`/`_send_local_file`/`_post_confirm_followup` return `SendResult(success=False, error="…upgrade BurnBar to exchange messages")` instead of leaking.

### `/Users/albertonunez/.hermes/hermes-agent/tests/gateway/test_burnbar_plugin.py` (162 → 24 tests, all green via `load_plugin_adapter("burnbar")`)
Kept all PR1 tests; added: `test_model_switch_event_synthesizes_model_command`, `test_cursor_round_trip`, `test_send_happy_path_posts_plaintext_when_legacy`, `test_send_error_returns_failure`, `test_send_local_file_inits_uploads_and_posts`, `test_refresh_oversight_mode_reads_state`, `test_autonomous_oversight_auto_approves`, `test_runtime_status_payload_shape`, and E2E: `test_agent_relay_public_key_is_x963_65_bytes`, `test_seal_send_then_phone_opens`, `test_phone_sealed_event_is_opened_by_agent`, `test_refuses_plaintext_event_when_e2e_paired`, `test_refuses_send_when_e2e_but_no_peer_key`, `test_seal_attachment_round_trip`, `test_gateway_aad_is_locked_wire_prefix`. Seal→open round-trips use the **real** `gateway.crypto.relay_e2ee` with a mocked phone keypair; `_RecordingClient` httpx double asserts no plaintext leaves. `@requires_relay` skips E2E tests if `cryptography` is absent.

### `/Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/README.md`
Added an **End-to-end encryption** section (algorithm, pairing publish, seal/open behavior, refuse-plaintext, legacy fallback), the 5-line reusable integration, the gateway AAD parts list, and a relay-env-var table. Replaced the broken `smoke_local.py` smoke section (that script lives in the BurnBar repo, not the fork) with the actual fork test entry (`pytest tests/gateway/test_burnbar_plugin.py`) and a pointer to `test_relay_e2ee.py`.

**Verification:** `python -m py_compile` clean on both files; `pytest tests/gateway/test_burnbar_plugin.py` → 24 passed; collection passes the conftest anti-pattern guard. Removed untracked `plugins/platforms/burnbar/__pycache__`.

### Deviations / cross-stream notes
- **AAD parts hyphenation:** the LOCKED CONTRACT (§GATEWAY WIRE) uses `"gatewayEvent"/"gatewayMessage"/"gatewayAttachmentBody"/"gatewayAttachmentKey"` (camelCase). I followed the CONTRACT verbatim; `recon-gw-ios-rules-honesty.md:34` shows `"gateway-event"` (hyphenated) — **the iOS B-ios-gw stream must use the camelCase CONTRACT form** for the phone↔agent seal/open to interoperate. Flagging for cross-stream alignment.
- **Cross-stream dependency (satisfied):** `gateway/crypto/relay_e2ee.py` (F-crypto) is landed and matches its `__init__.py` contract; my adapter binds to its real API. Note an F-crypto inconsistency I worked around: `AgentRelayIdentity.public_key_base64` is a **property** while `RelayPrivateKey.public_key_base64` is a **method** — handled via `_public_key_base64`. `unwrap_symmetric_key` needs `AgentRelayIdentity.private_key` (not the identity), handled via `_relay_private_key`. F-crypto may want to harmonize these for other consumers.
- **Per-CONTRACT scope:** `gateway/platforms/api_server.py` removal is the orchestrator's job (not edited). `pyproject.toml`/`uv.lock` `cryptography` extra is F-crypto-owned (not edited); `cryptography==46.0.7` is importable in the fork.