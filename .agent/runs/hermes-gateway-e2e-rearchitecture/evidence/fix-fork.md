All green: 62 total, 17 of which target the seven Codex findings. py_compile clean on all four owned files. Byte-exact interop fixture and locked-AAD tests untouched and passing.

## CHANGELOG

### `/Users/albertonunez/.hermes/hermes-agent/plugins/platforms/burnbar/adapter.py`
- **Peer-key pinning (Finding 1, P1).** Replaced the always-overwriting `_cache_peer_public_key` with `_pin_peer_public_key(...)`: pins the peer relay pubkey EXACTLY ONCE (trust-on-first-use, persisted to `~/.hermes/.env`), then treats it as IMMUTABLE. A different inbound key (from `senderPublicKey`/`relayPublicKey`/`phoneRelayPublicKey` in events, `/state`, `/events`, `/destinations`) is refused with a `SECURITY` warning and never adopted; for sealed events the open path adopts the sender key only via this pin-aware method (in `_open_envelope`), so a substituted `senderPublicKey` cannot rotate the pin. `_absorb_relay_state` now routes its peer key through the pin (uid/clientId still update freely as non-secret routing ids).
- **Fail-closed (Finding 2, P1).** `_RelaySealer.must_seal` is now `self._adapter._relay_e2e_enabled` alone — independent of identity/peer availability. `can_seal` now requires `_ensure_relay_identity() is not None`. `_post_message`/`_create_attachments` seal when `can_seal`, else refuse (`_RelayPlaintextRefused`) when `must_seal` — never plaintext. `open_event`/`open_model_switch` refuse an unsealed payload whenever E2E is enabled (no longer gated on `_relay_identity is not None`), and refuse if the agent's own private key can't load. Added `cannot_seal_reason(...)` for clear, distinct refusal copy (crypto-missing vs identity-load-fail vs legacy-peer).
- **Standalone bypass (Finding 3, P1).** `_standalone_send` now builds a `BurnBarAdapter(pconfig)` to obtain a pairing-aware `_RelaySealer` and passes it to `_create_attachments`/`_post_message`; on a paired link it seals or returns an explicit `refused` error — no plaintext path on a paired link.
- **Key persistence (Finding 4, P1).** `_ensure_relay_identity` and `interactive_setup` now pass `persist=save_env_value` (via new `_relay_key_persister()` / `hermes_cli.config.save_env_value`, which atomically writes `~/.hermes/.env` at 0600 through `_secure_file`). The agent relay key no longer rotates per restart.
- **Replay (Finding 5, P1).** Added `MAX_SEEN_EVENT_IDS=4096` + a bounded per-`(uid,clientId,eventId)` `OrderedDict` cache and `_event_already_seen(...)`; `_handle_burnbar_event` drops duplicate event ids BEFORE any handling/`handle_message`.
- **Attachment AAD (Finding 6, P2).** Added `_gateway_attachment_manifest_aad` (`gatewayAttachmentManifest`); `seal_attachment` now seals the manifest under that distinct label vs `gatewayAttachmentBody` for bytes vs `gatewayAttachmentKey` for the wrapped key — a cross-slot ciphertext swap now fails the GCM tag.
- **model_switch sealing (Finding 7, P2).** Added `_gateway_model_switch_aad` (`gatewayModelSwitch`), `_RelaySealer.open_model_switch` (rejects unsealed model_switch on E2E links, drops it as injectable control) and `seal_model_switch`; `_handle_burnbar_event` now opens model_switch via the sealed path on paired links (plaintext only when not paired). Refactored open into shared `_open_envelope(...)`.

### `/Users/albertonunez/.hermes/hermes-agent/gateway/crypto/relay_e2ee.py`
- **Key persistence hardening (Finding 4, P1).** `AgentRelayIdentity.load_or_create` now writes a minted key back into the live `environ` map (stable in-process reload even if disk persistence is unavailable) and swallows-and-logs a throwing `persist` callback instead of losing the minted key. No wire/byte-exact behavior changed.

### `/Users/albertonunez/.hermes/hermes-agent/tests/gateway/test_burnbar_plugin.py`
- Updated `test_seal_attachment_round_trip` to open the manifest under the new `gatewayAttachmentManifest` AAD and assert cross-slot swaps fail (`InvalidTag`) — closes Finding 6. Extended `test_gateway_aad_is_locked_wire_prefix` with manifest + model_switch labels and a 5-distinct-labels assertion.
- Added: `test_peer_key_pins_once_then_rejects_a_changed_key`, `test_absorb_relay_state_does_not_rotate_pinned_peer_key`, `test_event_with_substituted_sender_key_does_not_rotate_pin` (Finding 1); `test_fail_closed_refuses_send_when_identity_cannot_load`, `test_fail_closed_refuses_inbound_event_when_identity_cannot_load` (Finding 2); `test_standalone_send_seals_on_paired_link`, `test_standalone_send_refuses_when_paired_but_no_peer_key` (Finding 3); `test_relay_identity_persists_and_survives_reload` (Finding 4); `test_duplicate_event_id_is_dropped`, `test_seen_event_id_cache_is_bounded` (Finding 5); `test_unsealed_model_switch_rejected_on_e2e_link`, `test_sealed_model_switch_opens_on_e2e_link`, `test_seal_model_switch_round_trip` (Finding 7).

### `/Users/albertonunez/.hermes/hermes-agent/tests/gateway/test_relay_e2ee.py`
- Added `test_minted_key_is_written_back_into_environ_for_stable_reload` and `test_persist_failure_is_non_fatal_and_key_kept_in_process` (Finding 4 hardening).

**Validation:** `python -m py_compile` clean on all 4 owned files. `pytest tests/gateway/test_relay_e2ee.py tests/gateway/test_burnbar_plugin.py` → **62 passed**. Byte-exact interop (`HermesRelayWireVector.json`) + locked-AAD prefix tests pass unchanged. The 9 `aiohttp`-collection errors elsewhere in `tests/gateway/` are a pre-existing `.venv` gap (missing `aiohttp`) in unowned api_server/webhook modules, not introduced here.