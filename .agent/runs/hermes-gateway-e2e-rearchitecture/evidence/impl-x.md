Line 2475 is a different collection (`media_session_events`) that denies `filename` entirely — not my target, correct to leave. The media_attachment_manifests block (2816-2852) now requires `sealedFilename` and has no plaintext `filename` allowlist entry. Line 1650 has `cancelled`. All correct.

Let me verify line 1650's leading whitespace didn't get mangled (it uses tabs/spaces oddly in that region) — the edit preserved the original indentation since I matched exactly. Confirmed via the grep output showing the line intact.

All four of my owned changes are done and the test syntax check passes.

## CHANGELOG

### `/Users/albertonunez/Documents/Windsurf/BurnBar/firestore.rules`
- **media-filename FLAG-DAY** (`match /users/{userId}/media_attachment_manifests/{manifestId}`): DROPPED the plaintext-filename branch. Removed `"filename"` from the `validMediaAttachmentManifestKeys()` `hasOnly` allowlist; replaced the "sealed OR plaintext present" + legacy-size branches with a hard requirement `&& ("sealedFilename" in request.resource.data) && validCloudSealedText(request.resource.data.sealedFilename)`. Kept the `!("body"...)` / `!("ciphertext"...)` denials and the `direction`/`schemaVersion`/`id`/`blobHash`/`mime`/`size`/`peerDeviceIdHash` checks. Rewrote the block comment to document the clean flag-day (no legacy on-device plaintext writer ever existed).
- **rollback** (line 1650): added `"cancelled"` to the `validRollbackRequest()` status allowlist → now `["pending", "in_flight", "completed", "failed", "cancelled"]`. The legacy camelCase `"inFlight"` is deliberately NOT added (read-side decoders tolerate it; the wire surface stays snake_case clean).
- **gateway COMMENT blocks** (collections stay `write:if false`; enforcement is in callables/HTTP handlers, per CONTRACT §35): added sealed-contract documentation comments above `hermes_gateway_events` (phone→agent: `relayEnvelope{payloadCiphertext, wrappedKey, relayEncryption, relayKeyVersion}` + routing; text/senderDisplayName/threadId sealed, rejected by callable), `hermes_gateway_messages` (agent→phone: sealed `{text}`; bearer `handleMessageSend` enforces), and `hermes_gateway_attachments` (agent→phone: body sealed per-attachment key; manifest seals `{fileName, byteCount, contentType}`; plaintext fileName + storagePath fileName segment dropped). No rule allowlist added — write rules unchanged.

### `/Users/albertonunez/Documents/Windsurf/BurnBar/functions/scripts/test-firestore-rules.mjs`
- **T9 media flip** (`T9 media_attachment_manifests require sealedFilename and reject plaintext`): `sealed-1` (sealedFilename only) still `assertSucceeds`; `legacy-1` (plaintext `filename`) flipped to `assertFails`; **added** `neither-1` (no sealed, no plaintext) → `assertFails` (sealedFilename now mandatory); kept `both-1` (sealed+plaintext) and `extra-1` (smuggled key) `assertFails`. Renamed the test and updated comments.
- **T15 rollback extension** (`T15 rollback_requests require sealed scope/error and reject plaintext`): appended three cases — `active` with `status:"in_flight"` → `assertSucceeds`; `cancelled` with `status:"cancelled"` → `assertSucceeds`; `legacy-status` with camelCase `status:"inFlight"` → `assertFails` (legacy alias not in the wire allowlist). All new docs use only allowlisted keys (`id`, `sessionID`, `sealedScope`, `status`).
- `node --check functions/scripts/test-firestore-rules.mjs` → **passes** (`SYNTAX_OK`).

### Coordination / cross-stream notes
- The media WRITER (sealing iOS store + `sealedFilename` emission) is **B-media**; the rollback ENUM (`RollbackContracts.swift` snake_case rawValues + `cancelled` + tolerant `init(from:)`/`init?(wireValue:)`) is **B-rollback**. This slice owns only the rule + rule-tests and assumes those writers land in the same wave. The T9 `assertSucceeds` on `sealed-1` depends on B-media emitting exactly the new `hasOnly` key set; the T15 `cancelled` success depends on B-rollback/clients writing snake_case `cancelled`.
- No deviations or blockers. firestore.rules is not JS-checkable; the JS test file syntax-check passed (full emulator run is the central verify step).