// GENERATED by packages/data-domains/codegen.mjs — DO NOT EDIT. Source: registry.json

package com.openburnbar.data.domains

enum class EncryptionTier(val raw: String) {
    SERVER_READABLE("server_readable"),
    ZERO_ACCESS("zero_access"),
    END_TO_END("end_to_end");
    companion object { fun from(raw: String) = entries.first { it.raw == raw } }
}

data class DataDomain(
    val id: String,
    val title: String,
    val icon: String,
    val encryptionTier: EncryptionTier,
    val summary: String,
    val serverSees: List<String>,
    val deviceOnly: List<String>,
    val firestorePaths: List<String>,
    val storagePaths: List<String>,
    val countSource: String?,
    val byteSource: String?,
    val retention: String,
    val actions: List<String>,
    val entitlementGate: String?,
    val suspensionSurface: String?,
    val sealingScheme: String?,
)

object DataDomains {
    val all: List<DataDomain> = listOf(
        DataDomain(
            id = "usage_spend", title = "Usage & Spend", icon = "chart.bar.fill",
            encryptionTier = EncryptionTier.SERVER_READABLE, summary = "Per-session token counts, cost estimates, and provider/model/device telemetry. Project names and budget labels are sealed on-device; the server groups spend only by opaque per-project hashes.",
            serverSees = listOf("provider", "model", "device", "token counts", "cost estimates", "timestamps", "opaque project hashes"), deviceOnly = listOf("project names", "budget labels"),
            firestorePaths = listOf("usage", "usage_rollups", "usage_counter_days", "usage_counter_totals", "recent_usage", "quota_snapshots", "rollup_jobs", "projects"), storagePaths = listOf(),
            countSource = "usage", byteSource = null,
            retention = "rolling", actions = listOf("view", "export", "delete"),
            entitlementGate = null, suspensionSurface = null,
            sealingScheme = null,
        ),
        DataDomain(
            id = "conversations_chat", title = "Conversations & Chat", icon = "bubble.left.and.bubble.right.fill",
            encryptionTier = EncryptionTier.END_TO_END, summary = "Assistant chats, CLI agent transcripts, mobile mission prompts/results, saved text snippets, rollback scope/diagnostics, approval rules, agent personas, subscription graph edges, and conversation recall metadata are sealed on-device before Firestore receives them.",
            serverSees = listOf("provider/runtime identifiers", "message counts", "status/routing metadata", "timestamps", "device ids"), deviceOnly = listOf("chat titles", "chat previews", "message bodies", "CLI transcripts", "mission prompts/results", "saved text snippets", "project/file/command labels", "rollback scope paths", "rollback error diagnostics", "approval policy labels/globs/projects", "agent persona text", "subscription graph edges and display text"),
            firestorePaths = listOf("conversations", "chat_threads", "mobile_assistant_chats", "cli_sessions", "cli_agent_mission_requests", "text_snippets", "rollback_requests", "approval_policies", "agent_identities", "subscription_topics"), storagePaths = listOf(),
            countSource = "chat_threads", byteSource = null,
            retention = "until_deleted", actions = listOf("view", "export", "delete"),
            entitlementGate = "burnbar_pro", suspensionSurface = "burnbar_cloud",
            sealingScheme = null,
        ),
        DataDomain(
            id = "session_logs", title = "Searchable Session Logs", icon = "text.magnifyingglass",
            encryptionTier = EncryptionTier.END_TO_END, summary = "Full conversation bodies + the encrypted search index + project memory. Sealed on-device; the server holds only ciphertext, aggregate cockpit facets, and opaque search/integrity hashes. NOTE: the keyword search index is deterministic — repeated and co-occurring search terms produce stable keyed digests, so the server can learn which terms recur and appear together across your logs (the search structure), and the integrity hashes can confirm a guessed body or chunk; every title, snippet, body, and path stays sealed and unreadable.",
            serverSees = listOf("provider", "model", "cost", "token counts", "timing", "integrity hashes", "deterministic keyed search digests"), deviceOnly = listOf("project/path text", "title", "snippet", "body preview", "full transcript body"),
            firestorePaths = listOf("session_logs", "cloud_search_documents", "cloud_search_chunks", "cloud_search_postings", "cloud_search_index_state", "cloud_search_index_manifest", "project_memory_snapshots"), storagePaths = listOf("session_logs/{documentID}/bodies/{bodyHash}.json.aesgcm"),
            countSource = "cloud_search_documents", byteSource = "session_logs",
            retention = "until_deleted", actions = listOf("view", "export", "delete"),
            entitlementGate = "burnbar_pro", suspensionSurface = "burnbar_cloud",
            sealingScheme = null,
        ),
        DataDomain(
            id = "pensieve", title = "Pensieve Knowledge", icon = "brain.head.profile",
            encryptionTier = EncryptionTier.END_TO_END, summary = "Your private semantic memory: repo docs, notes, and chat-derived memories your agents recall. Cloaked vectors + sealed text; the server runs ANN over them without reading your content, though these structures still reveal some recurrence/co-occurrence patterns. NOTE: a connected repo stores only an opaque keyed match token plus a sealed repo name — the cleartext repo name is observed transiently server-side only to route an inbound GitHub push webhook, never stored.",
            serverSees = listOf("cloaked 384-dim vectors", "sourceKind", "opaque keyed slug/dedup hashes", "opaque repo match token", "chunk/byte counts", "timestamps"), deviceOnly = listOf("chunk text", "source paths", "repo names", "section/category metadata"),
            firestorePaths = listOf("cloud_search_knowledge", "knowledge_sync_manifests", "knowledge_repos"), storagePaths = listOf(),
            countSource = "knowledge_sync_manifests.chunkCount", byteSource = "knowledge_sync_manifests.byteCount",
            retention = "until_deleted", actions = listOf("view", "export", "delete", "configure", "sync"),
            entitlementGate = "burnbar_pro_max", suspensionSurface = "burnbar_cloud_pro",
            sealingScheme = "cloudvault-aesgcm-v2",
        ),
        DataDomain(
            id = "provider_accounts", title = "Provider Accounts", icon = "person.badge.key.fill",
            encryptionTier = EncryptionTier.SERVER_READABLE, summary = "Connected AI provider accounts. Labels + status only — the actual credentials live in Google Cloud Secret Manager, never in your data tree.",
            serverSees = listOf("provider id", "redacted label", "status", "refresh metadata"), deviceOnly = listOf("secret material (in Secret Manager, not the user tree)"),
            firestorePaths = listOf("provider_accounts", "provider_connections", "provider_account_device_links", "runtime_connection_preferences"), storagePaths = listOf(),
            countSource = "provider_accounts", byteSource = null,
            retention = "until_disconnected", actions = listOf("view", "revoke"),
            entitlementGate = null, suspensionSurface = null,
            sealingScheme = null,
        ),
        DataDomain(
            id = "connected_devices", title = "Connected Devices & Pairings", icon = "laptopcomputer.and.iphone",
            encryptionTier = EncryptionTier.SERVER_READABLE, summary = "Your paired Macs, phones, and relays (Hermes, Pi agent, iroh) and which can talk to your account. NOTE: paired relay and hosted-gateway frame contents are only claimed as sealed after paired E2EE is enabled and the runtime-readiness gate is complete; relay routing metadata, public keys, coarse tool labels, thread ids, attachment ids, and delivery state remain visible. The per-agent subscription graph is cloaked behind opaque keyed doc ids.",
            serverSees = listOf("device ids", "pairing metadata", "last seen", "relay routing", "opaque relay key material (public keys)"), deviceOnly = listOf("paired relay + gateway frame contents after E2EE/runtime readiness is complete (message text, sender names, attachment file names — sealed on-device in that mode)"),
            firestorePaths = listOf("devices", "hermes_connections", "hermes_pairings", "hermes_relay_requests", "hermes_session_cache", "hermes_gateway_clients", "hermes_gateway_destinations", "hermes_gateway_events", "hermes_gateway_messages", "hermes_gateway_typing", "hermes_gateway_state", "hermes_gateway_attachments", "hermes_gateway_approvals", "pi_agent_connections", "pi_agent_pairings", "pi_agent_relay_requests", "iroh_pairing", "iroh_pairing_keys", "runtime_connection_preferences"), storagePaths = listOf("hermes_gateway_attachments/**"),
            countSource = "devices", byteSource = null,
            retention = "until_revoked", actions = listOf("view", "revoke"),
            entitlementGate = null, suspensionSurface = null,
            sealingScheme = null,
        ),
        DataDomain(
            id = "external_mcp", title = "External Agent Access (MCP)", icon = "key.horizontal.fill",
            encryptionTier = EncryptionTier.SERVER_READABLE, summary = "Coding agents you've granted hosted-MCP access to, the scopes they hold, and their access audit trail.",
            serverSees = listOf("client id", "display name", "granted scopes", "grant mode", "access timestamps"), deviceOnly = listOf("decrypted search/recall results (decrypted only in the local shim)"),
            firestorePaths = listOf("remote_mcp_clients", "remote_mcp_grants", "remote_mcp_audit_events", "remote_mcp_rate_limits"), storagePaths = listOf(),
            countSource = "remote_mcp_clients", byteSource = null,
            retention = "until_revoked", actions = listOf("view", "revoke"),
            entitlementGate = "burnbar_pro", suspensionSurface = "remote_mcp",
            sealingScheme = null,
        ),
        DataDomain(
            id = "computer_use", title = "Agent Control & Escrow", icon = "cursorarrow.rays",
            encryptionTier = EncryptionTier.ZERO_ACCESS, summary = "Computer-use (Agent Control) sessions and their tamper-evident escrow audit trail.",
            serverSees = listOf("session metadata", "action counts", "quota usage"), deviceOnly = listOf("sealed escrow envelopes"),
            firestorePaths = listOf("computer_use_sessions", "computer_use_actions", "computer_use_quota_usage", "agent_grant_authorities", "agent_capability_grant_requests"), storagePaths = listOf(),
            countSource = "computer_use_sessions", byteSource = null,
            retention = "rolling", actions = listOf("view", "delete"),
            entitlementGate = "burnbar_pro_max", suspensionSurface = "hosted_agent_control",
            sealingScheme = null,
        ),
        DataDomain(
            id = "media", title = "Media", icon = "photo.on.rectangle.angled",
            encryptionTier = EncryptionTier.ZERO_ACCESS, summary = "Files, screen, and video relayed between your Mac and phones (Floo media). NOTE: an attachment manifest records only an opaque content hash, mime type, byte size, an opaque per-peer device-id hash, direction, and timestamps — the human-readable file name is sealed on-device (sealedFilename) and never readable by the server.",
            serverSees = listOf("session events", "quota usage", "attachment blob hash", "mime type", "byte size", "opaque peer device-id hash", "direction", "timestamps"), deviceOnly = listOf("media payload contents (relayed/sealed)", "attachment file names (sealed on-device)"),
            firestorePaths = listOf("media_session_events", "media_quota_usage", "media_attachment_manifests"), storagePaths = listOf(),
            countSource = "media_attachment_manifests", byteSource = "media_attachment_manifests",
            retention = "rolling", actions = listOf("view", "delete"),
            entitlementGate = "burnbar_pro_max", suspensionSurface = "floo_relay",
            sealingScheme = null,
        ),
        DataDomain(
            id = "entitlements_billing", title = "Plan & Billing", icon = "creditcard.fill",
            encryptionTier = EncryptionTier.SERVER_READABLE, summary = "Your subscription entitlements (Cloud Pro, Ultra) and their change history.",
            serverSees = listOf("entitlement ids", "product ids", "active state", "expiry", "purchase source"), deviceOnly = listOf(),
            firestorePaths = listOf("entitlements", "entitlement_events", "entitlement_bindings"), storagePaths = listOf(),
            countSource = "entitlements", byteSource = null,
            retention = "permanent", actions = listOf("view"),
            entitlementGate = null, suspensionSurface = null,
            sealingScheme = null,
        ),
        DataDomain(
            id = "device_trust_keys", title = "Device Trust & Vault Keys", icon = "lock.shield.fill",
            encryptionTier = EncryptionTier.END_TO_END, summary = "Which devices are trusted to decrypt your data, and the wrapped vault keys that make end-to-end encryption possible. The crux of the whole E2EE model.",
            serverSees = listOf("device trust state", "public key fingerprints", "wrapped (ciphertext) key blobs"), deviceOnly = listOf("the vault key itself (Keychain / 0600 file, never uploaded)"),
            firestorePaths = listOf("cloud_vault_state", "cloud_vault_key_wrappers", "escrow_devices", "escrow_public_keys", "signal_identity_public_keys", "escrow_grants", "escrow_envelopes", "escrow_audit_events", "account_recovery_methods"), storagePaths = listOf(),
            countSource = "escrow_devices", byteSource = null,
            retention = "until_revoked", actions = listOf("view", "approve", "revoke", "recover"),
            entitlementGate = null, suspensionSurface = null,
            sealingScheme = null,
        ),
        DataDomain(
            id = "audit_timeline", title = "Access Audit Timeline", icon = "list.bullet.rectangle.portrait.fill",
            encryptionTier = EncryptionTier.SERVER_READABLE, summary = "A unified, tamper-evident log of who/what accessed your data, when — across every device, agent, and grant.",
            serverSees = listOf("actor", "action", "domain", "timestamp", "hash-chain links", "generic notification routing ids"), deviceOnly = listOf(),
            firestorePaths = listOf("remote_mcp_audit_events", "hermes_audit_events", "pi_agent_audit_events", "iroh_audit_events", "escrow_audit_events", "entitlement_events", "budgetEvents", "agent_notification_events", "agent_notification_replies", "unified_audit_log", "audit_meta"), storagePaths = listOf(),
            countSource = "unified_audit_log", byteSource = null,
            retention = "append_only", actions = listOf("view", "verify", "export"),
            entitlementGate = null, suspensionSurface = null,
            sealingScheme = null,
        ),
    )
    fun domain(id: String): DataDomain? = all.firstOrNull { it.id == id }
}
