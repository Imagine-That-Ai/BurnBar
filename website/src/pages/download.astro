---
import BaseLayout from "@layouts/BaseLayout.astro";
import DownloadPlatformIcon from "@components/DownloadPlatformIcon.astro";
import { SITE } from "@data/site";

const macDownloadBaseUrl = SITE.macDownloadBaseUrl.replace(/\/$/, "");
const macUpdateBaseUrl = (SITE.macUpdateBaseUrl ?? SITE.macDownloadBaseUrl).replace(/\/$/, "");
const macDownloadHref = macDownloadBaseUrl
  ? `${macDownloadBaseUrl}/${SITE.macReleaseFile}`
  : `/downloads/${SITE.macReleaseFile}`;
const hasMacUpdateFeed = Boolean(macUpdateBaseUrl);
const macLatestHref = hasMacUpdateFeed ? `${macUpdateBaseUrl}/${SITE.macUpdateFeedFile}` : "";
const macAppcastHref = hasMacUpdateFeed ? `${macUpdateBaseUrl}/${SITE.macAppcastFile}` : "";

const linuxDownloadBaseUrl = SITE.linuxDownloadBaseUrl.replace(/\/$/, "");
const linuxAppImageHref = `${linuxDownloadBaseUrl}/${SITE.linuxReleaseFile}`;
const linuxDebHref = `${linuxDownloadBaseUrl}/${SITE.linuxDebFile}`;
const linuxRpmHref = `${linuxDownloadBaseUrl}/${SITE.linuxRpmFile}`;
const linuxPubKeyHref = `${linuxDownloadBaseUrl}/${SITE.linuxPubKeyFile}`;
---

<BaseLayout
  title="Download"
  description="Download OpenBurnBar. macOS by direct download, iPhone & iPad on the App Store, Linux ARM64 builds, Cursor Marketplace plugin, and the Cursor/VS Code extension source-only."
  ambientEffects={false}
>
  <section class="pagehead">
    <div class="container pagehead__inner">
      <span class="eyebrow eyebrow--ember">Download</span>
      <h1 class="pagehead__h" data-pretext-native>
        <span>Get</span>
        <span class="pagehead__product">OpenBurnBar</span>
      </h1>
      <p class="lead pagehead__lead">
        <strong>macOS</strong>, <strong>iOS</strong>, and <strong>Linux</strong> ship today — Android
        and the editor extension are buildable from source.
      </p>

      <!-- At-a-glance platform grid so Linux isn't buried mid-page -->
      <div class="platgrid" aria-label="Platform availability">
        <a class="platcard platcard--live" href="#macos">
          <DownloadPlatformIcon platform="macos" />
          <span class="sr-only">macOS</span>
          <span class="platcard__ver mono">v{SITE.macReleaseLatest}</span>
          <span class="platcard__status">Available now</span>
          <span class="platcard__cta">Download DMG →</span>
        </a>
        <a class="platcard platcard--live" href="#linux">
          <DownloadPlatformIcon platform="linux" />
          <span class="sr-only">Linux</span>
          <span class="platcard__ver mono">v{SITE.linuxReleaseLatest} · ARM64</span>
          <span class="platcard__status">Available now</span>
          <span class="platcard__cta">AppImage · deb · rpm →</span>
        </a>
        <a class="platcard platcard--live" href="#ios">
          <DownloadPlatformIcon platform="ios" />
          <span class="sr-only">iOS and iPadOS</span>
          <span class="platcard__ver mono">App Store</span>
          <span class="platcard__status">Available now</span>
          <span class="platcard__cta">Get the app →</span>
        </a>
        <a class="platcard platcard--pending" href="#editor">
          <DownloadPlatformIcon platform="editor" />
          <span class="sr-only">Cursor and VS Code</span>
          <span class="platcard__ver mono">source</span>
          <span class="platcard__status">Build locally</span>
          <span class="platcard__cta">Setup guide →</span>
        </a>
        <a class="platcard platcard--pending" href="#cursor-plugin">
          <DownloadPlatformIcon platform="cursor-plugin" />
          <span class="sr-only">Cursor Marketplace plugin</span>
          <span class="platcard__ver mono">Cursor</span>
          <span class="platcard__status">Marketplace pending</span>
          <span class="platcard__cta">Plugin setup →</span>
        </a>
        <a class="platcard platcard--pending" href="#android">
          <DownloadPlatformIcon platform="android" />
          <span class="sr-only">Android</span>
          <span class="platcard__ver mono">source</span>
          <span class="platcard__status">Play Store pending</span>
          <span class="platcard__cta">Details →</span>
        </a>
      </div>

      <nav class="jump" aria-label="Jump to platform">
        <a class="jump--live" href="#macos" aria-label="Jump to macOS" title="macOS">
          <DownloadPlatformIcon platform="macos" compact />
        </a>
        <a class="jump--live" href="#linux" aria-label="Jump to Linux" title="Linux">
          <DownloadPlatformIcon platform="linux" compact />
        </a>
        <a
          class="jump--live"
          href="#ios"
          aria-label="Jump to iPhone and iPad"
          title="iPhone and iPad"
        >
          <DownloadPlatformIcon platform="ios" compact />
        </a>
        <a href="#editor" aria-label="Jump to Cursor and VS Code" title="Cursor and VS Code">
          <DownloadPlatformIcon platform="editor" compact />
        </a>
        <a
          href="#cursor-plugin"
          aria-label="Jump to the Cursor Marketplace plugin"
          title="Cursor Marketplace plugin"
        >
          <DownloadPlatformIcon platform="cursor-plugin" compact />
        </a>
        <a href="#android" aria-label="Jump to Android" title="Android">
          <DownloadPlatformIcon platform="android" compact />
        </a>
      </nav>
    </div>
  </section>

  <!-- MACOS -->
  <section id="macos" class="dlsec">
    <div class="container dlsec__shell">
      <div class="dlsec__head">
        <div class="dlsec__intro">
          <span class="tag tag--exact tag--plain mono">Release · {SITE.macReleaseLatest}</span>
          <h2>macOS app</h2>
          <p class="lead">
            Native Swift app. Runs as a menu-bar utility. Requires {SITE.macMin} or later on an Apple
            Silicon Mac.
          </p>
        </div>
        <div class="dlsec__primarywrap">
          <a class="btn btn--ember dlsec__primary" href={macDownloadHref}>
            Download DMG · {SITE.macReleaseLatest}
            <span class="btn-arrow" aria-hidden="true">↓</span>
          </a>
          <a
            class="dlsec__verify mono"
            href={`${SITE.releasesUrl}/tag/v${SITE.macReleaseLatest}`}
            target="_blank"
            rel="noopener noreferrer">Verify this download — checksums &amp; SBOM ↗</a
          >
        </div>
      </div>

      <ol class="steps" data-animate-onview>
        <li class="step">
          <span class="step__num mono">1</span>
          <div class="step__card">
            <h3>Download &amp; open the DMG</h3>
            <p>
              The macOS DMG is served from OpenBurnBar's first-party download host. This link
              targets the signed, notarized, smoke-tested {SITE.macReleaseLatest} release whose checksum
              matches the immutable GitHub Release asset.
            </p>
            {
              hasMacUpdateFeed && (
                <p class="muted small">
                  Release feeds:
                  <a href={macLatestHref}>latest metadata</a>
                  {" · "}
                  <a href={macAppcastHref}>appcast</a>
                </p>
              )
            }
          </div>
        </li>
        <li class="step">
          <span class="step__num mono">2</span>
          <div class="step__card">
            <h3>Drag OpenBurnBar.app to <code>/Applications</code></h3>
            <p>That's the install. No installer, no admin password.</p>
          </div>
        </li>
        <li class="step">
          <span class="step__num mono">3</span>
          <div class="step__card">
            <h3>Launch &amp; find the menu bar</h3>
            <p>
              OpenBurnBar lives in the menu bar — no Dock icon. Click the flame to open the popover.
              If Gatekeeper hesitates the first time, right-click → Open.
            </p>
          </div>
        </li>
        <li class="step">
          <span class="step__num mono">4</span>
          <div class="step__card">
            <h3>Watch the burn</h3>
            <p>
              The app reads logs in <code>~/.claude/</code>, <code>~/.codex/</code>,
              <code>~/.factory/</code> and friends. No account, no API keys for local tracking. Quota
              providers (Cursor, Copilot, Warp, MiniMax, etc.) prompt you per provider when you choose
              to enable them.
            </p>
          </div>
        </li>
      </ol>

      <details class="alt">
        <summary>Build from source instead</summary>
        <div class="alt__body">
          <pre><code class="mono">git clone {SITE.github}.git
cd BurnBar
make install</code></pre>
          <p class="muted small">Requires Xcode 16+, Swift 5.10+, XcodeGen optional.</p>
        </div>
      </details>
    </div>
  </section>

  <!-- LINUX -->
  <section id="linux" class="dlsec dlsec--linux">
    <div class="container dlsec__shell">
      <div class="dlsec__head">
        <div class="dlsec__intro">
          <span class="tag tag--exact tag--plain mono">{SITE.linuxStatus}</span>
          <h2>Linux desktop app</h2>
          <p class="lead">
            Available now. Tauri + WebView shell over the native OpenBurnBar daemon. First public
            release ships <strong>{SITE.linuxArch}</strong> artifacts only — the AppImage is portable
            across ARM64 distros, with deb and rpm for Debian/Ubuntu and Fedora/RHEL on the same architecture.
            Every artifact is Ed25519-signed with a keyless Sigstore/cosign attestation.
          </p>
        </div>
      </div>

      <div class="linux-downloads" data-animate-onview>
        <a class="linux-dl linux-dl--primary" href={linuxAppImageHref}>
          <span class="linux-dl__kind mono">PRIMARY</span>
          <span class="linux-dl__title">AppImage</span>
          <span class="linux-dl__meta">Portable · {SITE.linuxArch}</span>
          <span class="linux-dl__action">Download · {SITE.linuxReleaseLatest} ↓</span>
        </a>
        <a class="linux-dl" href={linuxDebHref}>
          <span class="linux-dl__kind mono">DEB</span>
          <span class="linux-dl__title">.deb</span>
          <span class="linux-dl__meta">Debian · Ubuntu · arm64</span>
          <span class="linux-dl__action">Download ↓</span>
        </a>
        <a class="linux-dl" href={linuxRpmHref}>
          <span class="linux-dl__kind mono">RPM</span>
          <span class="linux-dl__title">.rpm</span>
          <span class="linux-dl__meta">Fedora · RHEL · aarch64</span>
          <span class="linux-dl__action">Download ↓</span>
        </a>
      </div>

      <p class="linux-note muted small">
        {SITE.linuxStatusDetail}. Hosted on the signed GitHub Release{" "}
        <a href={`${SITE.github}/releases/tag/linux-v${SITE.linuxReleaseLatest}`}
          >linux-v{SITE.linuxReleaseLatest}</a
        >. x86_64 builds are next.
      </p>

      <ol class="steps" data-animate-onview>
        <li class="step">
          <span class="step__num mono">1</span>
          <div class="step__card">
            <h3>Install</h3>
            <p>
              <strong>AppImage:</strong>{" "}
              <code
                >chmod +x OpenBurnBar_{SITE.linuxReleaseLatest}_aarch64.AppImage && ./OpenBurnBar_{
                  SITE.linuxReleaseLatest
                }_aarch64.AppImage</code
              >
            </p>
            <p>
              <strong>deb:</strong>{" "}
              <code>sudo apt install ./OpenBurnBar_{SITE.linuxReleaseLatest}_arm64.deb</code>
            </p>
            <p>
              <strong>rpm:</strong>{" "}
              <code>sudo dnf install ./OpenBurnBar-{SITE.linuxReleaseLatest}-1.aarch64.rpm</code>
            </p>
          </div>
        </li>
        <li class="step">
          <span class="step__num mono">2</span>
          <div class="step__card">
            <h3>Daemon &amp; socket</h3>
            <p>
              The app talks to the OpenBurnBar daemon over an AF_UNIX socket. The deb/rpm packages
              install a systemd unit; AppImage users can start the daemon via the shipped binary or{
                " "
              }
              <code>openburnbar-cli service</code>. Install to a trusted path (e.g.{" "}
              <code>/usr/bin</code>) so the daemon's peer-trust check accepts the client.
            </p>
          </div>
        </li>
        <li class="step">
          <span class="step__num mono">3</span>
          <div class="step__card">
            <h3>Verify the signature (optional)</h3>
            <p>
              Detached Ed25519 signatures ship beside each artifact, plus keyless cosign bundles.
              Public key:{" "}
              <a href={linuxPubKeyHref}>openburnbar-linux-ed25519.pub.pem</a>.
            </p>
          </div>
        </li>
      </ol>

      <details class="reqs">
        <summary>Requirements &amp; notes</summary>
        <div class="reqs__body">
          <h4>Linux</h4>
          <ul>
            <li>
              <strong>{SITE.linuxArch} only</strong> for 0.1.0. x86_64 lane is the next release milestone.
            </li>
            <li>GTK 3 + WebKit2GTK 4.1 (bundled in the AppImage).</li>
            <li>
              Signed with Ed25519 detached signatures and keyless Sigstore/cosign attestation
              bundles.
            </li>
            <li>
              Tagged as a signed prerelease so it can seed the auto-update chain; the app is fully
              downloadable and runnable today.
            </li>
          </ul>
        </div>
      </details>
    </div>
  </section>

  <!-- iOS -->
  <section id="ios" class="dlsec dlsec--quiet">
    <div class="container dlsec__shell">
      <div class="dlsec__head">
        <div class="dlsec__intro">
          <span class="tag tag--exact tag--plain mono">On the App Store</span>
          <h2>iPhone &amp; iPad companion</h2>
          <p class="lead">
            Requires {SITE.iosMin} or later on iPhone or iPad. Companion to the Mac app — the phone or
            tablet renders what the Mac records. Native widgets, Live Activity, Siri shortcut.
          </p>
        </div>
        <a
          class="btn btn--ember dlsec__primary"
          href={SITE.iosAppStoreUrl}
          target="_blank"
          rel="noopener noreferrer"
        >
          Get it on the App Store
          <span class="btn-arrow" aria-hidden="true">→</span>
        </a>
      </div>

      <div class="callout">
        <p>
          Free download; BurnBar Cloud subscriptions are available in-app. The iPhone and iPad app
          renders what your Mac's daemon records — install the macOS app first.
        </p>
      </div>
    </div>
  </section>

  <!-- Editor -->
  <section id="editor" class="dlsec">
    <div class="container dlsec__shell">
      <div class="dlsec__head">
        <div class="dlsec__intro">
          <span class="tag tag--estimated tag--plain mono">{SITE.cursorExtStatus}</span>
          <h2>Cursor &amp; VS Code extension</h2>
          <p class="lead">
            Activity-bar panel that talks to the same local daemon as the menu bar. No public
            marketplace listing yet — build from source and load unpacked.
          </p>
        </div>
      </div>

      <ol class="steps" data-animate-onview>
        <li class="step">
          <span class="step__num mono">1</span>
          <div class="step__card">
            <h3>Install &amp; launch OpenBurnBar on macOS</h3>
            <p>The daemon must be running — it's how the extension reads data.</p>
          </div>
        </li>
        <li class="step">
          <span class="step__num mono">2</span>
          <div class="step__card">
            <h3>Build the extension</h3>
            <pre><code class="mono">cd extensions/openburnbar
npm ci
npm run build</code></pre>
          </div>
        </li>
        <li class="step">
          <span class="step__num mono">3</span>
          <div class="step__card">
            <h3>Load unpacked into your editor</h3>
            <p>
              In Cursor or VS Code: <em>Extensions → "Install from VSIX…"</em> or
              <em>Run Extension</em> from the Run-and-Debug panel pointing at
              <code>extensions/openburnbar</code>.
            </p>
          </div>
        </li>
        <li class="step">
          <span class="step__num mono">4</span>
          <div class="step__card">
            <h3>Open the OpenBurnBar activity bar</h3>
            <p>
              The panel surfaces burn for the active workspace and the routed-provider gateway when
              you have it on.
            </p>
          </div>
        </li>
      </ol>
    </div>
  </section>

  <!-- Cursor Marketplace plugin -->
  <section id="cursor-plugin" class="dlsec dlsec--quiet">
    <div class="container dlsec__shell">
      <div class="dlsec__head">
        <div class="dlsec__intro">
          <span class="tag tag--exact tag--plain mono">Cursor Marketplace · plugin</span>
          <h2>Cursor Marketplace plugin</h2>
          <p class="lead">
            A separate Cursor Marketplace candidate (packaged under{" "}
            <code>plugins/openburnbar/</code>) that connects Cursor agents — including Cloud Agents
            — to hosted Remote MCP over HTTP. This is
            <strong>not</strong> the editor extension: the plugin talks to{" "}
            <code>https://mcp.burnbar.ai/mcp</code> with a bearer variable and needs no local binary,
            while the extension above stays a source-only, load-unpacked daemon sidebar with no marketplace
            listing.
          </p>
        </div>
      </div>

      <ol class="steps" data-animate-onview>
        <li class="step">
          <span class="step__num mono">1</span>
          <div class="step__card">
            <h3>Install the plugin</h3>
            <p>
              Once published, install from the Cursor Marketplace (<code
                >Imagine-That-Ai/openburnbar-cursor-plugin</code
              > is the marketplace git URL). Until then, symlink the package locally:
            </p>
            <pre><code class="mono">git clone https://github.com/Imagine-That-Ai/openburnbar-cursor-plugin.git
mkdir -p "$HOME/.cursor/plugins/local"
ln -sfn "$(pwd)/openburnbar-cursor-plugin" "$HOME/.cursor/plugins/local/openburnbar"</code></pre>
            <p>
              Then run <strong>Developer: Reload Window</strong> and open{" "}
              <strong>Settings → Customize → Plugins</strong>. There is no package-install step: the
              plugin tree is plain markdown, JSON, and a small validator script.
            </p>
          </div>
        </li>
        <li class="step">
          <span class="step__num mono">2</span>
          <div class="step__card">
            <h3>Set the access-token variable</h3>
            <p>
              The required <code>OPENBURNBAR_MCP_ACCESS_TOKEN</code> is a{" "}
              <strong>short-lived (~15 min) access token</strong>. Live setup is blocked until the
              attested <code>/link</code> flow is deployed and BurnBar exposes a safe copy/export action.
              <code>openburnbar mcp login</code> currently stores the token in Keychain and does not print
              a value to paste.
            </p>
          </div>
        </li>
        <li class="step">
          <span class="step__num mono">3</span>
          <div class="step__card">
            <h3>Inspect spend and hosted capabilities</h3>
            <p>
              The HTTP-only package supports spend and capability diagnostics. Conversation and
              knowledge search require keyed hashes or a cloaked vector from the optional local
              preprocessing/decrypt shim; the plugin does not fake those searches with plaintext.
            </p>
          </div>
        </li>
      </ol>
    </div>
  </section>

  <!-- Android -->
  <section id="android" class="dlsec dlsec--quiet">
    <div class="container dlsec__shell">
      <div class="dlsec__head">
        <div class="dlsec__intro">
          <span class="tag tag--beta tag--plain mono"
            >Remediation in progress · Play Store pending</span
          >
          <h2>Android companion</h2>
          <p class="lead">
            Android is an in-progress companion, not a full iPhone &amp; iPad parity claim. Source
            coverage is broad; physical-device, store, and VoiceOver evidence stay blocked. <code
              >productParityClaim</code
            > remains false. Build from source under <code>android/</code> until there is a Play listing
            of a validated candidate.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- System reqs -->
  <section class="reqs">
    <div class="container">
      <header class="sec-head">
        <span class="eyebrow eyebrow--ember">Requirements &amp; provenance</span>
        <h2>What you need, what we ship.</h2>
      </header>
      <div class="reqs__grid" data-animate-onview>
        <article class="reqcard">
          <h4>macOS</h4>
          <ul>
            <li>{SITE.macMin} or later</li>
            <li>Apple Silicon Mac</li>
            <li>~120 MB on disk</li>
            <li>First-party download host with immutable release provenance</li>
            <li>Developer ID signed, notarized, and stapled</li>
            <li>Direct-download releases publish checksums, SBOM, and Sparkle metadata</li>
          </ul>
        </article>
        <article class="reqcard">
          <h4>Linux</h4>
          <ul>
            <li>{SITE.linuxArch} for v{SITE.linuxReleaseLatest}</li>
            <li>AppImage, deb, or rpm from the signed GitHub Release</li>
            <li>GTK 3 + WebKit2GTK 4.1 (bundled in AppImage)</li>
            <li>Ed25519 + Sigstore/cosign attestation bundles</li>
            <li>Daemon AF_UNIX socket; trusted install path for peer auth</li>
          </ul>
        </article>
        <article class="reqcard">
          <h4>iOS / iPadOS</h4>
          <ul>
            <li>{SITE.iosMin} or later</li>
            <li>App Store-installed; subscriptions in-app</li>
            <li>BurnBar Cloud and Cloud Pro require Firebase sign-in</li>
            <li>Live Activity needs iOS 16.1+</li>
          </ul>
        </article>
        <article class="reqcard">
          <h4>Editor extension</h4>
          <ul>
            <li>Cursor or VS Code ^1.95.0</li>
            <li>Node + npm to build</li>
            <li>Loads unpacked — marketplace listing is roadmap</li>
          </ul>
        </article>
        <article class="reqcard">
          <h4>Cursor Marketplace plugin</h4>
          <ul>
            <li>Cursor (desktop Customize or Cloud Agents)</li>
            <li>BurnBar Cloud Pro/Ultra for hosted Remote MCP</li>
            <li>
              Requires <code>OPENBURNBAR_MCP_ACCESS_TOKEN</code> — short-lived access token, not a durable
              secret
            </li>
            <li>No package manifest, no install step — validator only</li>
          </ul>
        </article>
      </div>
    </div>
  </section>

  <section class="endcta endcta--inline">
    <div class="container container--narrow endcta__inner">
      <h2>Two extra resources before you install.</h2>
      <div class="endcta__ctas">
        <a class="btn btn--ghost" href="/privacy">Privacy model</a>
        <a class="btn btn--ghost" href="/providers">Provider matrix</a>
        <a class="btn btn--ghost" href={SITE.github} target="_blank" rel="noopener noreferrer"
          >GitHub repo</a
        >
      </div>
    </div>
  </section>
</BaseLayout>

<script>
  import "../scripts/animateOnView.ts";
</script>

<style>
  .pagehead {
    padding-block: var(--s-9) var(--s-6);
  }
  .pagehead__inner {
    width: 100%;
    max-width: 60rem;
    min-width: 0;
    margin: 0 auto;
    text-align: center;
    display: grid;
    gap: var(--s-3);
    justify-items: center;
  }
  .pagehead__h {
    margin-top: var(--s-3);
  }
  .pagehead__product {
    display: inline-block;
    white-space: nowrap;
    overflow-wrap: normal;
    word-break: keep-all;
  }
  .pagehead .lead,
  .pagehead__lead {
    margin-top: var(--s-4);
    max-width: 58ch;
  }

  /* ── at-a-glance platform cards ── */
  .platgrid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(10.5rem, 1fr));
    gap: var(--s-3);
    width: 100%;
    max-width: 56rem;
    min-width: 0;
    margin-top: var(--s-6);
    text-align: left;
  }
  .platcard {
    display: grid;
    grid-template-rows: auto auto auto 1fr;
    justify-items: start;
    gap: 0.25rem;
    padding: 1rem 1.05rem 0.95rem;
    border-radius: var(--r-md, 12px);
    border: 1px solid var(--ink-line);
    background: color-mix(in oklab, var(--glass-bg-elevated) 88%, transparent);
    text-decoration: none;
    color: inherit;
    transition:
      border-color var(--dur-base) var(--ease),
      transform var(--dur-base) var(--ease),
      box-shadow var(--dur-base) var(--ease);
  }
  .platcard:hover {
    border-color: color-mix(in oklab, var(--ember-core) 45%, var(--ink-line));
    transform: translateY(-2px);
    box-shadow: 0 10px 28px color-mix(in oklab, var(--ember-core) 12%, transparent);
  }
  .platcard:hover :global(.download-platform-icon) {
    transform: translateY(-2px) scale(1.04);
  }
  .platcard--live {
    border-color: color-mix(in oklab, var(--ember-core) 35%, var(--ink-line));
    background: linear-gradient(
      160deg,
      color-mix(in oklab, var(--ember-core) 14%, transparent),
      color-mix(in oklab, var(--glass-bg-elevated) 92%, transparent) 55%
    );
  }
  .platcard--pending {
    opacity: 0.78;
  }
  .platcard :global(.download-platform-icon) {
    margin-bottom: 0.55rem;
    transition: transform var(--dur-base) var(--ease);
  }
  .platcard__ver {
    font-size: 0.72rem;
    color: var(--text-mute);
  }
  .platcard__status {
    margin-top: 0.35rem;
    font-size: 0.82rem;
    color: var(--ember-bright, var(--ember-core));
    font-weight: var(--wght-semibold);
  }
  .platcard--pending .platcard__status {
    color: var(--text-mute);
    font-weight: var(--wght-display);
  }
  .platcard__cta {
    margin-top: 0.45rem;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    color: var(--text-mute);
  }
  .platcard--live .platcard__cta {
    color: var(--ember-bright, var(--ember-core));
  }

  /* platform jump links */
  .jump {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--s-2);
    margin-top: var(--s-5);
  }
  .jump a {
    display: grid;
    place-items: center;
    padding: 0;
    border-radius: 0.85rem;
    border: 0;
    background: transparent;
    transition:
      transform var(--dur-base) var(--ease),
      filter var(--dur-base) var(--ease);
  }
  .jump a:hover,
  .jump a.jump--live:hover {
    transform: translateY(-3px);
    filter: saturate(1.15);
  }
  .jump a.jump--live {
    opacity: 1;
  }

  @media (min-width: 64rem) {
    .pagehead__inner {
      max-width: 68rem;
    }
    .platgrid {
      grid-template-columns: repeat(6, minmax(0, 1fr));
      max-width: 68rem;
    }
  }

  /* ── Linux download tiles ── */
  .dlsec--linux {
    background: radial-gradient(
      80% 60% at 50% 0%,
      color-mix(in oklab, var(--ember-core) 10%, transparent),
      transparent 70%
    );
  }
  .linux-downloads {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: var(--s-3);
    margin-bottom: var(--s-4);
  }
  @media (max-width: 720px) {
    .linux-downloads {
      grid-template-columns: 1fr;
    }
  }
  .linux-dl {
    display: grid;
    gap: 0.3rem;
    padding: 1.15rem 1.2rem;
    border-radius: var(--r-md, 12px);
    border: 1px solid var(--ink-line);
    background: color-mix(in oklab, var(--glass-bg-elevated) 90%, transparent);
    text-decoration: none;
    color: inherit;
    transition:
      border-color var(--dur-base) var(--ease),
      transform var(--dur-base) var(--ease),
      box-shadow var(--dur-base) var(--ease);
  }
  .linux-dl:hover {
    border-color: color-mix(in oklab, var(--ember-core) 50%, var(--ink-line));
    transform: translateY(-2px);
    box-shadow: 0 12px 32px color-mix(in oklab, var(--ember-core) 14%, transparent);
  }
  .linux-dl--primary {
    border-color: color-mix(in oklab, var(--ember-core) 45%, var(--ink-line));
    background: linear-gradient(
      155deg,
      color-mix(in oklab, var(--ember-core) 18%, transparent),
      color-mix(in oklab, var(--glass-bg-elevated) 90%, transparent) 60%
    );
  }
  .linux-dl__kind {
    font-size: 0.68rem;
    letter-spacing: 0.08em;
    color: var(--ember-bright, var(--ember-core));
  }
  .linux-dl__title {
    font-size: 1.25rem;
    font-weight: var(--wght-bold);
    letter-spacing: -0.02em;
    --opsz: var(--opsz-lead);
  }
  .linux-dl__meta {
    font-size: 0.85rem;
    color: var(--text-mute);
  }
  .linux-dl__action {
    margin-top: 0.55rem;
    font-family: var(--font-mono);
    font-size: 0.78rem;
    color: var(--ember-bright, var(--ember-core));
  }
  .linux-note {
    margin-bottom: var(--s-6);
  }

  /* ---------- section shell — keeps content tasteful, not full-bleed ---------- */
  .dlsec {
    padding-block: var(--s-8);
    border-top: 1px solid var(--ink-line);
  }
  .dlsec--quiet {
    background: var(--ink-base);
  }
  .dlsec__shell {
    max-width: 60rem;
  }
  .dlsec__head {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    gap: var(--s-5);
    margin-bottom: var(--s-6);
    flex-wrap: wrap;
  }
  .dlsec__intro {
    flex: 1 1 28rem;
  }
  .dlsec__head h2 {
    margin-top: var(--s-3);
  }
  .dlsec__head .lead {
    margin-top: var(--s-3);
    max-width: 52ch;
    font-size: 1.1rem;
  }
  .dlsec__primary {
    white-space: nowrap;
  }
  .dlsec__primary.is-disabled {
    opacity: 0.55;
    cursor: not-allowed;
    pointer-events: none;
  }

  /* ---------- elegant numbered timeline ---------- */
  .steps {
    position: relative;
    display: grid;
    gap: var(--s-3);
    max-width: 46rem;
  }
  /* the connector line runs through the node centers (left 20px) */
  .steps::before {
    content: "";
    position: absolute;
    left: 19px;
    top: 18px;
    bottom: 18px;
    width: 2px;
    background: linear-gradient(
      to bottom,
      color-mix(in oklab, var(--ember-core) 55%, transparent),
      color-mix(in oklab, var(--ember-core) 12%, transparent)
    );
  }
  .step {
    position: relative;
    display: grid;
    grid-template-columns: 40px minmax(0, 1fr);
    gap: var(--s-4);
    align-items: start;
  }
  .step__num {
    position: relative;
    z-index: 1;
    width: 40px;
    height: 40px;
    display: grid;
    place-items: center;
    border-radius: 50%;
    font-size: 0.95rem;
    color: var(--ember-bright);
    border: 1px solid color-mix(in oklab, var(--ember-core) 45%, var(--ink-line));
    background:
      radial-gradient(
        circle at 50% 35%,
        color-mix(in oklab, var(--ember-core) 18%, transparent),
        transparent 70%
      ),
      var(--glass-bg-elevated);
    box-shadow: inset 0 0 0 4px color-mix(in oklab, var(--ink-void) 86%, transparent);
  }
  .step__card {
    padding: var(--s-4) var(--s-5);
    border: 1px solid var(--ink-line);
    border-top: 1px solid var(--ink-line-bright);
    border-radius: var(--r-3);
    background: var(--glass-bg-elevated);
    backdrop-filter: blur(20px) saturate(160%);
    -webkit-backdrop-filter: blur(20px) saturate(160%);
    box-shadow: var(--shadow-glass);
    display: grid;
    gap: var(--s-2);
    min-width: 0;
    transition:
      border-color var(--dur-base) var(--ease),
      transform var(--dur-base) var(--ease);
  }
  .step__card:hover {
    transform: translateX(3px);
    border-color: color-mix(in oklab, var(--ember-core) 28%, var(--ink-line));
  }
  .step__card h3 {
    font-family: var(--font-display);
    color: var(--text-bright);
    font-size: var(--t-h4);
    font-weight: var(--wght-semibold);
    --opsz: var(--opsz-lead);
  }
  .step__card p {
    color: var(--text-base);
    font-size: 0.95rem;
  }
  .step__card :global(pre) {
    margin: var(--s-1) 0 0;
  }

  pre {
    background: var(--ink-surface);
    border: 1px solid var(--ink-line);
    border-radius: var(--r-2);
    padding: var(--s-3) var(--s-4);
    overflow-x: auto;
  }
  pre code {
    background: transparent;
    border: 0;
    padding: 0;
  }

  /* build-from-source disclosure */
  .alt {
    margin-top: var(--s-5);
    max-width: 46rem;
    border: 1px solid var(--ink-line);
    border-top: 1px solid var(--ink-line-bright);
    border-radius: var(--r-3);
    background: var(--glass-bg-elevated);
    backdrop-filter: blur(20px) saturate(160%);
    -webkit-backdrop-filter: blur(20px) saturate(160%);
  }
  .alt summary {
    cursor: pointer;
    padding: var(--s-3) var(--s-5);
    color: var(--text-bright);
    font-family: var(--font-display);
    font-size: 1rem;
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
    --opsz: var(--opsz-body);
  }
  .alt summary::-webkit-details-marker {
    display: none;
  }
  .alt summary::after {
    content: "+";
    color: var(--ember-bright);
    font-family: var(--font-mono);
  }
  .alt[open] summary::after {
    content: "−";
  }
  .alt__body {
    padding: 0 var(--s-5) var(--s-5);
  }

  /* callout (iOS in-review) */
  .callout {
    max-width: 46rem;
    padding: var(--s-5);
    border-left: 3px solid var(--ember-bright);
    border-top: 1px solid var(--ink-line-bright);
    border-right: 1px solid var(--ink-line);
    border-bottom: 1px solid var(--ink-line);
    background: color-mix(in oklab, var(--ember-core) 5%, var(--glass-bg-elevated));
    backdrop-filter: blur(20px) saturate(160%);
    -webkit-backdrop-filter: blur(20px) saturate(160%);
    box-shadow: var(--shadow-glass);
    border-radius: 0 var(--r-3) var(--r-3) 0;
    display: grid;
    gap: var(--s-2);
  }
  .callout strong {
    color: var(--text-bright);
  }

  @media (max-width: 720px) {
    .pagehead__inner > * {
      min-width: 0;
      max-width: 100%;
    }
    .platgrid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
    .pagehead__h {
      max-width: 100%;
      font-size: clamp(2.1rem, 9vw, 2.4rem);
      letter-spacing: 0;
      overflow-wrap: normal;
      word-break: normal;
    }
    .pagehead__h > span {
      display: block;
    }
    .pagehead__product {
      overflow-wrap: normal;
      word-break: keep-all;
    }
    .dlsec__head {
      flex-direction: column;
      align-items: stretch;
    }
    .dlsec__primary {
      white-space: normal;
      text-align: center;
      width: 100%;
    }
    pre {
      font-size: 0.78rem;
    }
  }

  /* ---------- system requirements ---------- */
  .reqs {
    padding-block: var(--s-9);
    border-top: 1px solid var(--ink-line);
  }
  .sec-head {
    display: grid;
    gap: var(--s-3);
    margin-bottom: var(--s-6);
    max-width: 64ch;
  }
  .reqs__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--s-4);
  }
  .reqcard {
    padding: var(--s-5);
    border: 1px solid var(--ink-line);
    border-top: 1px solid var(--ink-line-bright);
    border-radius: var(--r-3);
    background: var(--glass-bg-elevated);
    backdrop-filter: blur(22px) saturate(170%);
    -webkit-backdrop-filter: blur(22px) saturate(170%);
    box-shadow: var(--shadow-glass);
    transition: border-color var(--dur-base) var(--ease);
  }
  .reqcard:hover {
    border-color: color-mix(in oklab, var(--ember-core) 26%, var(--ink-line));
  }
  .reqcard h4 {
    font-family: var(--font-display);
    color: var(--text-bright);
    font-size: 1.05rem;
    margin-bottom: var(--s-3);
    font-weight: var(--wght-semibold);
    --opsz: var(--opsz-body);
  }
  .reqcard ul {
    display: grid;
    gap: var(--s-2);
    font-size: 0.9rem;
    color: var(--text-base);
  }
  .reqcard li {
    padding-left: 1.1em;
    position: relative;
  }
  .reqcard li::before {
    content: "→";
    color: var(--ember-bright);
    position: absolute;
    left: 0;
    font-family: var(--font-mono);
    font-size: 0.85em;
  }
  @media (max-width: 880px) {
    .reqs__grid {
      grid-template-columns: 1fr;
    }
  }

  .small {
    font-size: var(--t-caption);
  }
  .endcta--inline {
    padding-block: var(--s-9);
    text-align: center;
    background: radial-gradient(60% 80% at 50% 50%, rgba(255, 106, 26, 0.05), transparent 70%);
    border-top: 1px solid var(--ink-line);
  }
  .endcta__ctas {
    display: flex;
    justify-content: center;
    gap: var(--s-3);
    margin-top: var(--s-5);
    flex-wrap: wrap;
  }
  .endcta__inner {
    text-align: center;
    display: grid;
    gap: var(--s-5);
    justify-items: center;
    padding-block: var(--s-4);
  }

  /* ---------- reveal motion ---------- */
  @keyframes downloadReveal {
    from {
      opacity: 0;
      translate: 0 22px;
    }
    to {
      opacity: 1;
      translate: 0 0;
    }
  }
  .steps[data-animate-onview] .step,
  .reqs__grid[data-animate-onview] .reqcard {
    opacity: 1;
    translate: 0 0;
  }
  .steps[data-animate-onview].is-visible .step,
  .reqs__grid[data-animate-onview].is-visible .reqcard {
    animation: downloadReveal 640ms var(--ease) both;
  }
  .steps[data-animate-onview].is-visible .step:nth-child(2) {
    animation-delay: 80ms;
  }
  .steps[data-animate-onview].is-visible .step:nth-child(3) {
    animation-delay: 160ms;
  }
  .steps[data-animate-onview].is-visible .step:nth-child(4) {
    animation-delay: 240ms;
  }
  .reqs__grid[data-animate-onview].is-visible .reqcard:nth-child(2) {
    animation-delay: 90ms;
  }
  .reqs__grid[data-animate-onview].is-visible .reqcard:nth-child(3) {
    animation-delay: 180ms;
  }

  @media (prefers-reduced-motion: reduce) {
    .step,
    .reqcard,
    .step__card {
      transition: none !important;
      animation: none !important;
    }
    .steps[data-animate-onview] .step,
    .reqs__grid[data-animate-onview] .reqcard,
    .steps[data-animate-onview].is-visible .step,
    .reqs__grid[data-animate-onview].is-visible .reqcard {
      opacity: 1 !important;
      translate: none !important;
    }
  }
  .dlsec__primarywrap {
    display: grid;
    gap: var(--s-2);
    justify-items: start;
  }
  .dlsec__verify {
    font-size: 0.78rem;
    color: var(--text-mute);
    text-decoration: underline;
    text-underline-offset: 3px;
  }
  .dlsec__verify:hover {
    color: var(--text-bright);
  }
</style>
