// SPDX-License-Identifier: AGPL-3.0-only
//
// Core-decomposition S0 (docs/CORE_DECOMPOSITION_PROGRAM.md): placeholder so
// SwiftPM accepts the OpenBurnBarSQLiteReader target before its move packet lands. SwiftPM
// rejects a target/product with no source files; this marker keeps the scaffold
// valid until `git mv` fills the target. Remove once the target has real
// sources (or leave it — an unreferenced internal enum has zero cost).
internal enum OpenBurnBarSQLiteReaderModuleMarker {}
