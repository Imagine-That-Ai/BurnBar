# Subagent D — UI/UX
**Goal:** Toggle in Settings + live session header.
**Deliverable:** PR titled `ui: visual surface toggle for Both providers`
**Files:** ConnectionsSettingsView.swift, VisualCaptureToggle.swift (new), ScreenShareViewer.swift, HermesSquareRoot.swift
**Acceptance:** Visual QA light/dark + simulator, VoiceOver, disabled when desktop not installed, no layout shift for CLI-only.
