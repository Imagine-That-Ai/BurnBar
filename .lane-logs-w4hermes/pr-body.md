Audit wave 4, item 14 (structural decomposition) + item 16 (dependency threading) — final two files. Pure line moves splitting two ~2k-line iOS service files into single-responsibility files, all ≤ 555 lines.

> **STACK NOTE:** base = `fix/audit-remediation-port-to-main` (PR #1343), deliberately — #1343 modifies both target files. This PR auto-retargets to `main` when #1343 merges.

> **⚠️ CROSS-AGENT RECEIPT (for the #1343 lane)**
> - **Saw:** base branch commit `878b086417` (audit waves 1-2) does not compile for `OpenBurnBarMobile`: `LiveCloudReader.swift:407` uses `Self.log` inside `LiveDeviceTrustGateway`, but `log` is `private static` on `LiveCloudReader` (a different type) → `type 'Self' has no member 'log'`.
> - **Reaction:** fixed here in isolated commit `3c382408be` (adds a per-type `Logger` to `LiveDeviceTrustGateway`; 1 line).
> - **Status:** OpenBurnBarMobile builds green on this branch with that commit.
> - **Next owner:** #1343 lane — either cherry-pick `3c382408be` or land this PR; a comment was posted on #1343.

## Commits

1. `c1d4357b50` — split `HermesGatewayAPI.swift` (1,984 lines → 6 files)
2. `9f10f6a641` — split `CLIAgentMissionDispatcher.swift` (1,830 lines → 7 files)
3. `3c382408be` — base-inherited build unbreak (see receipt above; not part of the split)

Each split commit regenerates `OpenBurnBar.xcodeproj/project.pbxproj` via `xcodegen` and repins the privacy scanner paths that followed moved code, so each commit is self-consistent.

## Review map — HermesGatewayAPI.swift (original line ranges → destination)

| Original lines | Content | Destination (all in `OpenBurnBarMobile/Services/`) | Lines |
|---|---|---|---|
| 1–553 | callable client class, init, stored `client` provider, private callable response envelopes | **stays** in `HermesGatewayAPI.swift` (header doc updated: `decodedRatchetText(` pin now names the record file) | 555 |
| 555–674 | `HermesGatewayRepository` protocol + default-argument extension | `HermesGatewayRepository.swift` | 133 |
| 676–1104, 1908–1980 | `SealedGatewayPayload`, `HermesGatewayMessageRecord` (incl. `decodedText`/`decodedRatchetText` open paths), `HermesGatewayMessageResolver` | `HermesGatewayMessageRecord.swift` | 520 |
| 1106–1562 | `HermesGatewayClientRecord` + Firestore-doc init extension, `HermesGatewayModelOptionRecord`, `HermesGatewayQueuedEvent` | `HermesGatewayClientRecord.swift` | 474 |
| 1564–1690 | `HermesGatewayApprovalRecord` + doc-init extension | `HermesGatewayApprovalRecord.swift` | 144 |
| 1692–1906 | `HermesGatewayOpenedAttachment`, `HermesGatewayAttachmentRecord`, `HermesGatewayAttachmentManifest` | `HermesGatewayAttachmentRecord.swift` | 232 |
| 1982–1984 | file-private `String.nilIfEmpty` helper | replicated into each of the 4 record files (repo-wide per-file pattern, cf. `CLIAgentMissionRequestListener+Mirrors.swift`) | 3×4 |

## Review map — CLIAgentMissionDispatcher.swift (original line ranges → destination)

| Original lines | Content | Destination (all in `OpenBurnBarMobile/Services/`) | Lines |
|---|---|---|---|
| 1–10, 222–491, 1139–1164, 1828–1830 | imports, logger, class decl + stored `firestoreProvider` + init, `dispatch()`, `initialQueuedEventSource`, `observe()`, `fetchMissionSnapshot()`, `DispatchError`, `String.nilIfEmpty` | **stays** in `CLIAgentMissionDispatcher.swift` | 311 |
| 11–220 | `CLIAgentMissionPrivatePayload`, `CLIAgentMissionEventPrivatePayload`, `CLIAgentMissionCloudSealer` | `CLIAgentMissionDispatcher+Sealing.swift` | 223 |
| 493–944 | `dispatchFanOut`, mission-group synthesis draft/prompt builders, Wand model routing (`selectedModelID`/`resolvedWandPolicy`/`runtimeID`), `FanOutDispatchResult`, `MissionGroupSynthesisDraft` — wrapped in `extension CLIAgentMissionDispatcher` | `CLIAgentMissionDispatcher+FanOut.swift` | 471 |
| 946–1137 | `observeMissionGroup`, `mergeMissionGroup` (+ both static update builders), `respondToApproval`, `cancelMission` (+ static update builder) — wrapped in `extension CLIAgentMissionDispatcher` | `CLIAgentMissionDispatcher+MissionControl.swift` | 207 |
| 1166–1262 | `AgentHarnessImportJobDispatcher` + `AgentHarnessImportJobSnapshot` | `AgentHarnessImportJobDispatcher.swift` | 114 |
| 1264–1568 | `CLIAgentMissionRequestPayloadFactory` (incl. sealed private-payload application) | `CLIAgentMissionRequestPayloadFactory.swift` | 322 |
| 1570–1826 | `CLIAgentMissionObservation`, `CLIAgentMissionEvent`, `CLIAgentMissionSnapshot` | `CLIAgentMissionModels.swift` | 274 |

The source import block is replicated verbatim at the top of every new file; the 3-line private `String.nilIfEmpty` helper is replicated into new files that use it (root, +FanOut, import-job, factory, models / 4 Hermes record files).

## Losslessness evidence

Method: both splits were **generated by script** (`split_hermes.py` / `split_dispatcher.py`, archived in the lane logs), not hand-copied. Each script:
1. asserts the expected declaration at every segment boundary line,
2. slices the original into contiguous 1-based line segments and asserts a coverage map — **every original line lands in exactly one destination segment** (uncovered lines proven blank separators),
3. re-extracts each segment from the written output files and asserts it is **byte-identical contiguous** to the original slice,
4. applies the 6 visibility edits (below) only afterward, each via exact-string replace asserted to match exactly once.

Result: `LOSSLESSNESS OK: all 1985/1831 original physical lines accounted for verbatim exactly once` for both files. The only non-move deltas: the 6 widened declaration lines, replicated imports/`nilIfEmpty` helper, added file headers + `extension CLIAgentMissionDispatcher {}` wrappers, and the updated `HermesGatewayAPI.swift` header comment (scanner-pin location).

## Visibility changes (all `private` → internal; each required by cross-file use after the split)

| Symbol | Why |
|---|---|
| `cliMissionSignalLogger` (top-level let) | logged from root + `+Sealing` + `+FanOut` |
| `CLIAgentMissionDispatcher.firestoreProvider` (stored let) | used by `+FanOut` / `+MissionControl` extensions |
| `CLIAgentMissionPrivatePayload` | built/opened by sealer, factory, `+MissionControl`, models |
| `CLIAgentMissionEventPrivatePayload` | built/opened by sealer, factory, models |
| `CLIAgentMissionCloudSealer` | called from root, factory, `+MissionControl`, models |
| `CLIAgentMissionDispatcher.selectedModelID(forRequestedRuntime:wandPolicy:)` (static) | called from root `dispatch()` and `+FanOut` |

HermesGatewayAPI split required **zero** visibility changes (cross-file helpers were already internal; `SealedGatewayPayload` and the callable response envelopes stay private with their sole users).

## Item 16 (dependency threading) audit within the decomposed area

- `CLIAgentMissionDispatcher`: every Firestore access already rides the stored `firestoreProvider` (kept, threaded into the extensions via the widened stored property). No extracted code re-reads a `.shared` for a dep the type holds; **no new `.shared` reads introduced**. Pre-existing `HermesService.shared`/`PiService.shared`/`OpenClawService.shared` reads in the Wand routing moved verbatim (the type never held those deps — out of scope per brief).
- `HermesGatewayAPI`: all callable access rides the stored `client` provider. Clean.

## Gate/plumbing updates (follow-the-code, same commit as each move)

- `scripts/privacy/scan-chat-cloud-plaintext.mjs`: `decodedRatchetText(` executable pin → `HermesGatewayMessageRecord.swift` (commit 1); the 3 mission-sealer pins (sealed-field allowlist, `payload.removeValue(forKey: key)`, `sealedPayload`) → `CLIAgentMissionRequestPayloadFactory.swift` (commit 2). The plaintext-payload **bans** stay pinned to `HermesGatewayAPI.swift`, which retains all gateway event construction.
- `budgets/swift-file-size-baseline.json`: no change needed — neither file is baselined (target 2000; originals were 1984/1830) and the gate passes: `OK: no new oversized files and no baselined file grew.`

## Validation matrix

| Check | Result |
|---|---|
| `FIREBASE_SOURCE_FIRESTORE=1 xcodebuild build -scheme OpenBurnBarMobile` (iPhone 17 Pro Max sim, pinned SPM cache, `-disableAutomaticPackageResolution`) | BUILD_RESULT_PLACEHOLDER |
| Targeted tests via `scripts/test-openburnbar-mobile.sh` on a dedicated simulator (`w4-hermes-lane`, iOS 27.0): `OpenBurnBarMobileTests`, `CLIAgentMissionDispatcherSealTests`, `MissionGroupObserverTests`, `HermesGatewayApprovalDetailIndexTests`, `CLIAgentChatReaderTests`, `HermesServiceTests`, `IntelligenceBriefWiringTests` | TEST_RESULT_PLACEHOLDER |
| Scripted losslessness verification (both files) | PASS (see above) |
| `node scripts/privacy/scan-chat-cloud-plaintext.mjs` | parity with base: only pre-existing `firestore.rules: validCliAgentMissionRequest lacks keys().hasOnly allowlist` finding (present on clean base HEAD; verified by stash-and-rerun); zero new findings |
| `scripts/debt/check-swift-file-size-budget.sh` | PASS |
| `scripts/ci/check-twin-basenames.sh` | PASS (pre-existing stale-allowlist warning only) |
| Repo-unique basenames (`git ls-files | basename | uniq -d` diff before/after) | no new duplicates |
| `Package.resolved` drift | none (source-Firestore pins intact) |
| `xcodegen` regenerated `project.pbxproj` | committed in each split commit |

## Risks

- **Pure-move risk is low** by construction (scripted slices + byte-identity checks). Main residual risk is the 6 visibility widenings enlarging module-internal surface; all are app-target internal, no public API.
- The privacy scanner pin moves assume the mission sealed-field allowlist lives in the factory file and the ratchet open path in the message-record file — both true post-split; a future move must re-point them again (scanner fails red if not).
- Stacked base: if #1343 rebases/force-pushes, this branch needs a rebase; conflicts confined to the two split files + pbxproj + scanner.

## Rollback

Revert the three commits (`git revert 3c382408be 9f10f6a641 c1d4357b50`) and run `xcodegen`; no schema, wire-format, or persisted-state impact (no behavior change shipped).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
